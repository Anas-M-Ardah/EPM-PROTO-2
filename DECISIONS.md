# DECISIONS — business-rule questions resolved in the prototype

One entry per decision so the final build inherits the reasoning, not just the code.
Mirrors the handoff's open questions (`07-POC-BUILD-PLAN.md` §9). Items marked **CONFIRM** still need client sign-off before production.

| # | Question | Prototype decision | Source | Status |
|---|---|---|---|---|
| D-01 | Rate bands after several orders — does the second order's 20% threshold use the *original* or *current effective* quantity? | Uses the **original** contract quantity (`line.contractedQty`). | 07 §9.1 | **CONFIRM** |
| D-02 | Delay penalty rate | **0.1%/day, capped at 10%** of the effective contract value (`configuration.rules.penaltyPerDayPct/penaltyCapPct`). | 02 §10 | **CONFIRM** against contract template |
| D-03 | Per-stage SLA | Uniform **5 days** (`configuration.rules.slaDaysPerStage`); a stage past SLA is `overdue`, raises needs-action, and escalates. | 02 §12 | **CONFIRM** per committee |
| D-04 | Delegate authority to cancel | The rapporteur may record a cancellation only for external parties flagged `canCancel: true` (Endorsement Review Cmte, Admin & Financial Dept). | 03 §4 | **CONFIRM** |
| D-05 | Distribution vs. decrease | When a decrease would push distributed total above the revised quantity, the order **cannot be applied** until the distribution is revised. Revising party not yet fixed. | 02 §8 | **CONFIRM** (RE dept vs beneficiary) |
| D-06 | "Now" reference | Derived from the **project data date** (`configuration.time.dataDate = 2026-08-02`) in demo mode; real clock in production. Never `Date.now()`. | CLAUDE.md, 06 §12 | Resolved |
| D-07 | BOQ weight rounding | **Largest-remainder** to exactly 100.00%; `toFixed(2)` is a bug. Denominator is the **contract's** BOQ rows. | 02 §1 | Resolved |
| D-08 | Approved value authority | The **approved value** comes only from the pricing/rate-fixing committee, entered at financial review. The RE-dept proposal governs display; the contractor proposal is shown labelled until then. | 02 §6 | Resolved |
| D-09 | Approved ≠ Applied ≠ Closed | Approving changes nothing. Applying creates a contract amendment and moves quantities/dates/penalty baseline. Closing verifies the application. Approved-unapplied orders shown as a **projection**, never folded into effective figures. | 03 §6 | Resolved |
| D-10 | External parties | Statuses inside the owning stage (recorded by a delegate against an official letter number+date), **not** workflow stages. Attribution: deciding party, delegate as recorder. | 03 §3–§4 | Resolved |
| D-11 | Money type | Displayed as integer IQD; production must use `decimal`/integer minor units — never `float`. Tier splits and largest-remainder must be exact. | 07 §2 | Resolved (prototype), enforce in prod |
| D-12 | Contract scoping | A BOQ item / activity belongs to exactly one contract; the project is derived from the contract. One order may not span two contracts (validation gate `cross-contract`). | 01, 02 §7 | Resolved |

## Assumptions made to fill gaps

- **Seed figures.** Concrete BOQ/activity amounts were chosen to be internally consistent (`amount = qty × rate`) and to exercise the rules; they are illustrative, not real ministry data. The documented worked examples (56.13%/43.87%, the qty-100-add-30 tier split) run in the **Rules Reference** route from their own example inputs, independent of the seed.
- **Man-hours basis** is stubbed to fall back to `budgetedCost` where P6 man-hours are absent (the cost/man-hours toggle is present in the BOQ activity-assignment view).
- **3D/BIM viewer, standalone Claims, Quality (NCR/Lab), HSE, GIS** are out of Phase 1 (07 §8) — tabs are kept and stubbed.

---

# Full-stack port decisions (Angular + .NET + SQL Server)

Decisions taken while porting the React prototype. One entry per decision, with the reasoning,
so the next person inherits the *why* and not just the code.

| # | Question | Decision | Rationale | Status |
|---|---|---|---|---|
| P-01 | Storage shape | **Flat tables, plain ID columns.** No navigation properties, no foreign keys, no cascade rules, no indexes. | A normalised EF model needed ten `OnDelete(Restrict)` overrides purely to satisfy SQL Server's multiple-cascade-path rule — pure ceremony, first thing a reader hits. Relationships now live in the endpoint's `Where()` clause where they can be read. | Resolved |
| P-02 | Schema evolution | **No migrations.** `EnsureCreated()` on boot; `POST /api/dev/reset` to apply a change. | Migration files are the top merge-conflict source for parallel agents, and there is no production data to preserve. Cost: reset wipes anything typed in. | Resolved |
| P-03 | Seeding | **Nothing on boot.** The `06 §12` fixture loads only via `POST /api/dev/load-fixture`. | The reference figures are illustrative, not ministry data — the original `DECISIONS.md` says so itself. Making them opt-in stops invented numbers being mistaken for real ones. | Resolved |
| P-04 | Test isolation | Domain rule tests use **inline worked examples** from `02-BUSINESS-RULES.md` and never read the database. | If a fixture figure turns out wrong, no test starts lying. The spec, not the data, is the oracle. | Resolved |
| P-05 | Identity | **Persona header (`X-Epm-User`), no authentication.** | The permission *model* from `03 §7` is real and resolved server-side; only the identity is fake. `03 §7` itself asks for the persona switcher as the fastest way to review the model. | Resolved |
| P-06 | Build unit | **One page at a time, end to end** — table, endpoint, Angular trio, UML, TRACE row. | Building 30 tables up front from a spec was speculative: columns existed for screens nobody had built. Page-at-a-time means no column exists on a guess. | Resolved |
| P-07 | Design fidelity | **Copy the stylesheets verbatim** (2,947 lines) rather than re-derive tokens. | The reference JSX uses `className` strings, so porting a screen is mechanical and the design *matches* rather than approximates. | Resolved |
| P-08 | Canonical status keys vs reference CSS | API speaks `06 §1` canonical keys (`delayed`, `cancelled`); a `statusClass()` map translates to the stylesheet's older `stalled` / `withdrawn` classes. | The spec keys are correct and must not be bent to the CSS; editing the verbatim stylesheet would break the copied-not-re-derived guarantee. | Resolved |
| P-09 | Derived values not yet derivable | Return **null and render an em dash**, never a zero. | Physical % is weight-rolled BOQ progress (BR-04). Until the BOQ page exists it cannot be derived, and a 0% bar asserts something false. | Resolved |
| P-10 | Reference component vs written spec | **Open the reference component before writing a page.** | PAGE-01 was built from the written spec alone and had to be redone: it invented a column set, used the wrong CSS classes, and missed that the Workspace column is hidden when workspace-scoped. The spec gives the rules; the component gives the screen. | Resolved |
| P-11 | Where the value lists live | **`Features/Lookups/LookupCatalog.cs`**, called by `Fixture.Load()` — not inline in `Fixture.cs`. | `Fixture.cs` is illustrative demo data and says so at the top. The `06 §1–§11` lists are the opposite: they are the specification's own enumerations, every stored code comes from one of them, and a wrong row is a wrong label on every screen. Putting them beside their endpoint keeps one grep for `boq-change-type` finding the list, the endpoint and the Angular caller. They still load only via `POST /api/dev/load-fixture`, because nothing is seeded on boot (P-03). | Resolved |
| P-12 | Which execution-stage list | The **12 construction stages of `06 §2`** (`design` … `handover`), which are the codes the fixture already stores. | `06 §2` names 12 construction-progress stages *and* says to carry the client's Arabic wording from `reference/app/data.jsx`. But `data.jsx`'s `PROJECT_STAGES` is a different list — an administrative lifecycle (دراسة · إعلان وإحالة · سحب عمل · تسوية حسابات) with no correspondence to the construction stages. The two cannot both be `execution-stage`. `06 §2`'s own enumeration wins because `Projects.ExecutionStage` already stores its codes; the Arabic wording is ours. | **CONFIRM** — which list the client means, and whether the administrative one is a second field |
| P-13 | `co-lifecycle` is missing two states | Added **`approved`** and **`cancelled`** to the `06 §7` list of six. | `03 §6`'s lifecycle diagram requires `approved` — the state whose entire purpose is *the values were agreed and **the contract has not changed*** (non-negotiable #2, D-09). Without it, approved-≠-applied cannot be represented at all. `03 §5`'s fourth decision (`cancel`, used when an external party rejects) terminates an order, which needs a terminal state distinct from `rejected`. `06 §7` lists neither; this is a gap in the data dictionary, not a design change. | **CONFIRM** the two Arabic labels |
| P-20 | v1.1 declares Inter but never loads it | **We load Inter.** | `tokens.css` sets `--font-en: 'Inter', system-ui, sans-serif`, but the branch's `index.html` loads **Roboto** and no Inter at all — so its Latin text silently falls back to `system-ui` and the token is decorative. Loading the font the tokens actually name is the fix; the alternative (change the token to Roboto) would be re-deriving the design. A defect in the branch, worth reporting upstream. | Resolved |
| P-21 | v1.1 breaks the binding accessibility contract on two tokens | **Corrected on adoption** — `--outline` and `--viz-base` to `#858E9C` (3.31:1) light and `#6B7484` (3.43:1) dark, overridden in `web/src/styles.css`. | v1.1 ships both at `#A9B1BC` = **2.16:1** on white; `05 §7.1` requires ≥3:1 for borders, icons and graphics, and the palette they replaced sat at 3.03:1 — the old values were chosen to pass. Dark theme was worse: `#454C57` = 1.87:1. These are not decorative: `05 §1` defines `--outline` as "borders and graphics only" and `04 §5` uses `--viz-base` for the Gantt data-date line and bar edges, so WCAG 1.4.11 applies. The chosen values are the **lightest** on v1.1's own neutral ramp that clear the floor, so the intended look survives as far as the contract allows. Overridden rather than edited into `tokens.css` so the copied sheets stay verbatim (P-07). `--outline-variant` / `--stroke-default` left alone — decorative separators are exempt. | **REVERTED by client decision — known defect** |
| P-22 | `--tertiary` is now the same blue as `--status-ongoing-tx` | **Flagged, not resolved.** | v1.1 sets `--tertiary: #1748B0`, identical to the `ongoing` status text colour; the Redwood red it used to carry is gone from the palette. `05 §7.5` forbids one colour carrying two meanings, so using `--tertiary` as a decorative accent anywhere would read as *ongoing*. Nothing in the one screen built today uses it that way, so nothing is broken yet — but every Phase 2 screen could introduce it. | **CONFIRM** — audit `--tertiary` usages before Phase 2 fans out |
| P-17 | The copied stylesheet pins the summary strip, `05 §8` forbids it | **`.d-grid.stats.fit` in `src/styles.css`** overrides only the column count to `repeat(auto-fit, minmax(120px,1fr))`. | `desktop.css` pins `.d-grid.stats` to `repeat(4, 1fr)` and `.d-grid.stats5` to `repeat(5, 1fr)`, with media queries pinning 3 and 2 below — while `05 §8` and CLAUDE.md §6 both require auto-fit. Measured in the browser: 5 stats under the pinned rule lay out **2 + 2 + 1**, the ballooned-orphan defect §8 was written about; under `.fit` they lay out as 5 equal tracks. Overriding one property in the additions file keeps every other `.d-grid.stats .d-stat` rule — hover, watermark, staggered reveal, reduced-motion — and leaves the verbatim copy intact (P-07). | Resolved |
| P-18 | Field grid: reference is 2 pinned columns, ROADMAP asked for auto-fill | **Kept the reference's `.d-form-grid`** (2 columns, collapsing to 1 at ≤900px). | ROADMAP 1.3 specified `repeat(auto-fill, minmax(240px,1fr))`, but nothing in `05` requires it — §8's grid rule is about *figure strips*, not field grids. The reference's cell borders depend on the 2-column count (`.d-form-i:nth-child(2n)` drops the inline-end border, `:nth-last-child(2):nth-child(2n+1)` the last row's bottom), so auto-fill breaks them, and the reference never renders more than two columns. CLAUDE.md §1 is explicit that the reference component gives the screen. | **CONFIRM** — if wide workspace panes should show 3–4 field columns, the border rules need rewriting too |
| P-19 | A CSS syntax error inside the "verbatim" stylesheet | **Fixed in `web/src/styles/desktop.css:739`**; the reference copies are left untouched. | The base prototype's own `styles/desktop.css:739` ends `.d-modnav` with `scrollbar-width: thin; }; }` — a stray `; }`. Browser error recovery skips it, so the reference prototype renders correctly and nobody noticed; Angular's production CSS parser rejects it and **`ng build` failed outright**. Removing the stray tokens changes no declaration and no rendering, so this is a syntax fix, not a re-derivation of the design. Marked in place with a comment. **The same typo is still in `../epm/styles/desktop.css` and in `docs/spec/reference/styles/desktop.css`.** | Resolved |
| P-15 | `02 §3`'s assigned amounts do not match its own rule | **The rule is binding; the prose figures are an illustration.** `Allocation.Shares([5.8, 5.2], 26,730,000)` gives **14,094,000 / 12,636,000**; `02 §3` states 14,092,710 / 12,637,290. | The spec quotes the weights rounded to 1dp (5.8 / 5.2) but computed the amounts from the UNROUNDED underlying weights — `02 §4` says exactly this about its own example ("client's figure 14,057,931 — same rule, unrounded shares"). The delta is 1,290 IQD on a 26.7M line, 0.005%. Bending the arithmetic to match the prose would break the rule for every other input. `AllocationTests` asserts both the rule's answer and the divergence, so nobody "fixes" it later. **ROADMAP 4.2's exit criterion quotes the prose figures and has been annotated.** | Resolved |
| P-16 | Is amendment no. 0 `original` or `superseded` once amendment 1 is applied? | **Always `original`.** `superseded` means an earlier applied AMENDMENT (no ≥ 1). | `02 §9` words the list as "original (no. 0) · superseded (an earlier applied one) · effective (the last applied)" — `original` is keyed to the number, so it is the row's identity, and the chain shows it as العقد الأصلي whether or not it is still in force. The "which version is in force" question is answered by `Amendments.Effective()`, not by this label. Noticed because a first pass wrote the two cases inconsistently and the tests caught it. | Resolved |
| P-26 | `06` defines no value lists for alerts, but `Alerts.Severity` and `Alerts.Kind` are stored codes that need AR/EN labels | **Added three lists to `LookupCatalog.cs` in a clearly-marked ADDENDUM section** (`alert-severity`, `alert-kind`, `alert-status`), with a comment stating they are ours and not the data dictionary's. Codes come from the `Alert` entity's own documented vocabularies. | P-11's argument is that stored codes belong in the catalog: one mechanism (`EP-LKP-01` → `core/lookups.ts`) then labels every enum in the app, the AR↔EN toggle works without a per-screen map, and the client can correct wording without a code change. The alternative — a label map inside the Alerts page — would have been the second mechanism, reachable by no grep. Marking the section rather than silently merging it keeps "`06 §1–§11` verbatim" true of everything above the marker. **`06` should gain these three sections.** | **CONFIRM** — the nine Arabic labels, and whether `06` adopts the lists |
| P-27 | The reference uses **two Arabic forms of each severity** — `حرِج` in the table cell, `حرِجة` in the cards and chips — and two English ones for the top severity (`High` on the chip, `High severity` on the card) | **Both carried.** The singular is the lookup label (one alert's severity, agreeing with «تنبيه»); the plural is a group heading and lives in `core/lang.ts` with the rest of the chrome. | Not an inconsistency in the reference — each form is correct where it stands, and collapsing them to one would put a grammatical error on one of the two. It does mean severity is the one enum whose label does not come from exactly one place, so both sites are cross-referenced in comments. | Resolved |
| P-28 | The reference Alerts feed carries dates **after its own stated data date** (`2026-09-08` under a header reading *بيانات حتى 2026-08-10*) | **Every fixture alert sits on or before the project data date `2026-08-02`.** | D-06 already forbids the wall clock; this is the same rule applied to seeded rows. The reference's dates come from an offset against `Date.now()`, which is why they drift ahead. A demo where alerts are raised in the future undermines the one screen whose entire job is *what needs attention now*. | Resolved |
| P-29 | Is acknowledgement client state or a write? | **A write.** `EP-ALR-02` persists `Acknowledged` and stamps the persona into `AcknowledgedByUserId`. | The reference keeps an `ackMap` in component state — correct for a clickable prototype, wrong for a record. An acknowledgement is somebody accepting that they have seen an alert; one that evaporates on refresh, or that records no one, is indistinguishable from the alert never having been read. Makes `Alerts` the first table a screen writes to. | Resolved |
| P-30 | The reference colours the delay figure `--error` when late and `--success` when not | ~~Not ported.~~ **REVERTED — the reference's colouring is reproduced** (`.epm-sc-late` / `.epm-sc-ontime` / `.epm-sc-fc-late`, tokens not literals). | `CLAUDE.md §6` and `05 §7.9` forbid colouring a magnitude by threshold in as many words — "the neutral branch is `--on-surface`" — and `05 §7.5` reserves the status hues for status. A delay is a magnitude, not a state; the reference also paints the forecast DATE red, which makes a date look like an error condition. **The client chose visual fidelity over the §7 floor** (see the REVERTED block below). | **REVERTED by client decision — known defect** |
| P-31 | Import status has no source — the Activities table is not registered | **Returns `pending` for every project**, from a `ScheduleImported` bool that is currently constant, and the endpoint says why. | This is *true*, not a placeholder: with no Activities table, no P6 schedule can have been imported for anyone. It reads as a real finding — nothing in the portfolio has a published baseline — rather than as a fabricated column. Becomes a genuine `db.Activities.Any(...)` in Phase 4.3 with no change to the DTO or the screen. Labels added as `schedule-import-status` in the LookupCatalog addendum (P-26's section). | Resolved |
| P-34 | The shell was missing twelve of the reference's chrome regions, all of them already styled in the copied sheets | **Built all twelve with real behaviour**: `.d-fill` · `.d-appfoot` · sidebar collapse (`data-side`, persisted) · workspace switcher · account block · `.d-side-sep` · global search + ⌘K palette · `.d-nav-count`. Page-head action clusters restored on all five screens, each firing the reference's own "— تجريبي / — demo" toast. | Diffing the two rendered DOMs turned up twelve `d-*` classes the reference renders and we never did. **Every one already had CSS** — 11 rules for `.d-appfoot`, 14 for `.d-cmdk`, 11 for `.d-side-acct` — so this was unbuilt markup, not missing design. Nothing here is a dead prop: collapse persists, the switcher navigates with `?ws=` (which every endpoint already accepts), and the palette is built from the route table so it can only offer a page that exists. The persona switcher moved from the topbar into `.d-side-acct`, which is the same semantic slot and lets the topbar match the reference exactly. | Resolved |
| P-35 | v1.1 asks for a `support_agent` icon its own `icons.js` does not define | **Used `help`.** | Identical to P-25: the reference renders a fallback glyph in its own footer. Worth reporting upstream. | Resolved |
| P-36 | Angular wraps every component in a custom element, so a component whose ROOT must be a grid child gets a wrapper in between | **`epm-app-footer { display: contents; }`** in `styles.css`. | `.d-appfoot` carries `grid-column: 1 / -1`. With `<epm-app-footer>` in between, the wrapper became the grid item and the footer inherited the 256px sidebar track instead of spanning — measured at 256px wide before the fix, 1440px after. Any future component in the same position needs the same treatment. | Resolved |
| P-32 | **`--fg-subtle` breaches the 4.5:1 text floor in both themes** — measured **3.07:1** on white, 2.86 on `--surface`, 2.76 on `--background`; dark 4.44 on `--surface-container` | **Corrected on discovery** — `#666F7F` light (5.07 on white, 4.55 on `--background`) and `#8D97A3` dark (6.01 on `--surface`, 4.81 on `--surface-container-high`), overridden in `web/src/styles.css`. | `05 §7.1` is binding and 12px/400 is not large text. v1.1 uses this token as TEXT colour in about a hundred `desktop.css` rules, two of which every register already built depends on: `.d-stat-foot` (the KPI foot line) and `.d-table tbody td.d-cell-sub` (the secondary column on Projects, Contracts, Entities and Alerts). It escaped the Phase 1.5 audit because a `.d-cell-sub` nested INSIDE a `td` picks up a different, compliant rule at 6.56:1 — only a `td` carrying the class itself is affected. Chosen by the same method as P-21: the lightest value on v1.1's own cool-grey ramp that clears the floor on every surface the token actually lands on. `--fg-disabled` deliberately untouched — `05 §7` exempts disabled state. | **REVERTED by client decision — known defect** |
| P-33 | **`--fs-100` is 10px**, below the type scale's floor | **Raised to 11px** in `web/src/styles.css`. | `05 §2` is binding and `CLAUDE.md §6` repeats it: the scale is exactly 11/11.5/12/13/15/18/21/24 and nothing is smaller than 11px. `desktop.css:387` sets the filter-chip count to a literal 11px and `desktop.css:3079` then overrides it with `var(--fs-100)`, so every chip count in the app rendered at 10px — measured on Schedule Control, Projects, Contracts and Alerts. 11px is the scale's own floor, so this snaps to the nearest legal step rather than inventing a size. | **REVERTED by client decision — known defect** |
| P-37 | The v1.1 `DReports` is not the same screen as the pre-v1.1 one, and `04 §2` describes the old one | **Ported the v1.1 catalog.** SCR-E7 is a register of the twelve reports a user can run — search, project scope, five category chips, pager — not the four-chart board of `docs/spec/reference/app/desktop-reports.jsx:39`. | Identical to the Alerts Center at Phase 2.4 (card feed → register): the roadmap's own warning says the `docs/spec/reference/` copy is pre-v1.1 and the sibling repo has the screen. Two further reasons this is the right way round. First, the old board's four charts already exist on SCR-E1 — trend, status split, entity ranking — so porting it would have built a second dashboard, and `04 §2` gives SCR-E1 that job. Second, the charts were driven by `window.EPM.buildProjects()`, a generator; against real data three of the four would have had nothing to draw. **`04 §2`'s one-line description of SCR-E7 is stale and should be updated to the catalog.** | **CONFIRM** — that `04 §2` adopts the catalog wording |
| P-38 | A report is a promise about data, and nine of the twelve read a table this build has not registered | **Every catalog row declares the tables it `Reads`; the endpoint compares that against the tables actually registered in `EpmDb` — read off the EF model, not a second list — and returns `available` plus a reason naming the missing source and the phase that builds it.** A blocked row gets no Run button, an explicit in-row note, and «غير متاح» in the action cell. | The reference makes every row runnable because in a clickable prototype every row toasts. Here that asserts twelve capabilities the system does not have, on the one screen whose whole subject is what the system can tell you about itself — and the register would be twelve identical demo toasts, which is the "button that silently does nothing" ToastService.demo exists to prevent. This is P-09's rule ("never render a figure for a missing input — show unavailable + reason") applied to a row rather than a KPI tile, and CLAUDE.md's rule for a gated action ("an explicit locked note, never a bare disabled button") applied outside the persona gating it was written for. Reading the EF model rather than keeping a hand-maintained list means Phase 4.1 registering `Payments` flips RPT-01/02/03/04 with **no edit to this feature** — the alternative goes stale the first time somebody forgets. Today: 3 available (RPT-06 · RPT-08 · RPT-10), 9 blocked. | Resolved |
| P-39 | Angular's `[value]` on a `<select>` whose `<option>`s come from `@for` binds before the options exist | **Carry the selection on each `<option [selected]>` instead.** | Measured on SCR-E7: with the page scoped to `PRJ-0148` — scope bar shown, catalog narrowed 12 → 9 — the dropdown read «كل المشاريع» and `select.value` was `""`. The control was contradicting the screen. The options render after the select's own bindings are applied, and re-fetching the project list re-runs `@for`, so the value lands on an empty list every time. `<epm-pager>` has the same shape and is not affected only because its first option *is* its default. **Any future `<select>` fed by `@for` needs the same treatment**, or `[ngModel]`, which this project does not use. | Resolved |
| P-40 | The v1.1 workspace is not three panes, and both `04 §3` and ROADMAP Phase 3 say it is | **Ported the v1.1 shell**: topbar project picker · one detail pane · grouped module rail · Z2 identity bar. **And the rail shows all fifteen modules, the thirteen unbuilt ones disabled with the phase that builds them** — ROADMAP's "only tabs that exist appear" would have left two entries and four empty group headings. | Same situation as P-37, one level larger. v1.1's `DWorkspace` renders `<div className="d-three" data-ctx="off">` containing a single `.d-pane.d-detail`, with the comment *"DETAIL — actions now live in the page header, no side pane"*; `DProjectContext` is exported and rendered by nothing. The per-module actions it held moved into Z6, beside the view they act on. On the rail: the grouping IS how the workspace states what a project consists of, and a reviewer needs that whole before any of it is built — so it renders, and each unbuilt entry says which phase. Nothing is a dead link: `built: false` is not routable and has no route entry either, which is the half of the guarantee `app.routes.ts` owns. **`04 §3` should be updated to the v1.1 anatomy.** Both halves were put to the client and chosen deliberately. | **CONFIRM** — that `04 §3` adopts the v1.1 workspace |
| P-41 | `desktop.css:865` puts `letter-spacing: .6px` on the module rail's group labels | **Overridden to `normal` in `web/src/styles.css`.** `text-transform: uppercase` left alone. | `CLAUDE.md §6` and `05 §2` forbid letter-spacing in as many words, and the reason is not aesthetic: Arabic is cursive and spacing the glyphs severs the joins. That makes it the same class of defect as the missing `<bdi>` isolation, which Phase 2.9 explicitly did **not** revert because it is a correctness bug in Arabic rather than a contrast one. `text-transform` is a no-op on Arabic (no case) and keeps the reference's look on the English labels, so it stays. **Separately noted, not acted on:** `.d-stat-val` is **28px** and its suffix **16px** — neither is on the 11/11.5/12/13/15/18/21/24 scale, and it affects every KPI band already merged. Flagged like P-22 rather than changed, because changing it moves six screens. | Resolved (letter-spacing) · **CONFIRM** (the 28px/16px scale breach) |
| P-42 | A module page reads `:id` off `route.parent`, and the parent route OUTLIVES the module | **`takeUntilDestroyed()` on the parent-route subscription** in both module pages. | Measured: on Information, switching project fired `GET …/PRJ-0148/overview` *and* `…/information` — the destroyed Overview component's subscription was still live and re-fetched for a component that was no longer on screen. Every enterprise page subscribes to its own `route.queryParamMap` without unsubscribing and is fine, because there the ActivatedRoute dies with the component; a CHILD reading its parent is the case where that stops being true. **Every workspace module added from Phase 4 on needs the same treatment** — it is load-bearing, not hygiene. | Resolved |
| P-43 | A .NET feature namespace `Epm.Api.Features.Contract` shadows the `Contract` ENTITY for every file under `Epm.Api.Features.*` | **The feature is `ContractTab` / `contract-tab` on both sides.** The route segment stays `contract` — it is the module id the rail uses. | `Fixture.cs` stopped compiling with *"'Contract' is a namespace but is used like a type"*: from inside `Epm.Api.Features.Dev`, the sibling namespace wins over `Epm.Api.Data.Entities.Contract`. CLAUDE.md §2's one-slug rule bends here rather than breaks: both stacks say `contract-tab`, so one grep still crosses them, and the alternative — qualifying `Entities.Contract` at every use — would have to be repeated in every future feature file that touches a contract. **Any feature named after an entity has the same problem** (`Payment`, `Project`, `Alert`): name it `<Entity>Tab` or after the screen. | Resolved |
| P-44 | `02 §4` says financial % "comes from payments" but never fixes the denominator | **No financial % is rendered on SCR-W3.** The money is shown — disbursed, certified, retention, advance recovery, remaining — and the part-to-whole bar is unlabelled geometry beside figures already printed in words. | The candidates are the EFFECTIVE value and the TOTAL contract cost (award + reserve + supervision), and they differ by ~7.5% on `CNT-0279`. Picking one would put a number on a guess, on a screen whose whole subject is which figure is which. `01 §2.2` calls financial % derived and points at `02 §6`, which is about proposals, not disbursement. Recorded here rather than guessed, per CLAUDE.md §8. | **CONFIRM** — which denominator, and whether reserve and supervision are in it |
| P-45 | **The v1.1 reference computes the delay penalty by a different rule than `02 §10`** | **`Domain/Penalty.cs` is unchanged — 0.1%/day capped at 10% (D-02) — and the screen shows that.** The reference's formula is recorded here, not implemented. | v1.1's `penaltyOf()` is `perDay = value × rate / duration`, `cap = value × rate`, with `rate` a per-contract band of 10–25% defaulting to 10%, and a comment citing *"Regs 2/2014"*. That is a total ceiling reached when the delay equals the full contract duration — a different rule, not a different rounding. On `CNT-0279` (250,000,000 effective, 531 days, 16 days late) the spec's rule gives 4,000,000 and the reference's gives ~120,000; on `CNT-0207` the spec's rule caps at 3,120,000 where the reference's would too, but for a different reason. CLAUDE.md §1 settles which wins — *"the written spec gives you the rules, the reference component gives you the screen"* — and `PenaltyTests` asserts `02 §10`'s worked example, so changing it silently would break the spec's own oracle. **But the reference is newer than `02` and cites a regulation, which is evidence about what the ministry actually applies.** D-02 was already marked CONFIRM; this is the concrete alternative to confirm it against. | **CONFIRM** — which penalty rule is contractual, and whether the 10–25% band is per contract |
| P-14 | Rounding scope | **BOQ weights round (largest remainder, BR-01); schedule weights do not.** | `02 §1` states BOQ weights must sum to exactly 100.00% and gives 56.13/43.87 as the worked example, so rounding is part of that rule. Activity absolute weights are different: `02 §3` feeds them straight into the allocation share and `02 §11` into earned value, so rounding them at source would push error into every derived figure. `Domain/ScheduleWeights` returns exact decimals; a screen that shows a weight column rounds for display with `Rounding.LargestRemainder`. | Resolved |

| P-46 | **`04 §4` gates the BOQ tab behind a contract selector; the v1.1 component deliberately does not** | **Gated, with one exception: a project with exactly ONE contract is not asked.** The gate renders «اختر عقداً للبدء» with one button per contract, each carrying its item count. | The reference's own comment argues the other way in as many words — *"open on the first contract rather than gating the page behind a picker: the selector in Z6 is right there, and an empty page teaches nothing"* — and it is a fair point about a clickable prototype where every contract has data. It is the wrong call here. `01 §1` makes the contract the scope of a BOQ item, so "the project's BOQ" is not a thing that exists; opening on `contracts[0]` silently picks one of two bills of quantities and shows 240,000,000 of civil works under a heading that says nothing about which contract it belongs to. `04 §4` and ROADMAP 4.2 both ask for the gate, and CLAUDE.md §1 gives the written spec the rules. The single-contract exception is not a compromise: there is no choice to make, and it is the same call SCR-W3's register already makes (P-16's neighbour). The selector stays in Z6 either way, so switching contract is still one control. | Resolved |
| P-47 | Is the allocation override per LINK or per BOQ ITEM? `BoqActivityLink.IsManual` is a per-row column, but `02 §3` says "manually overridable **per BOQ item** and persisted" | **Per ITEM.** One manual link puts the whole line into override mode: every share on it is then the stored value, and a reset clears `IsManual` on all of them. | The per-row column suggests mixing, and mixing is incoherent. BR-03's computed share is `absWeight / Σ(absWeights of the LINKED activities) × 100` — a share **of the set**. Freeze one row at 60 and recompute the others against the same denominator and the total is 60 + 100 = 160%, a figure nobody chose and no rule produced. `02 §3`'s wording is therefore not loose, it is the answer. It also has a consequence worth stating: **a computed set always sums to exactly 100**, so `partial` and `over` — two of `06 §11`'s four states — are reachable *only* through an override. That is why the fixture's partial and over lines are manual ones, and it is what the assignment screen exists to let someone fix. `IsManual` stays per row because that is where EF can store it; the endpoint reads it as `mine.Any(l => l.IsManual)`. | Resolved |
| P-48 | `02 §2` says the weight basis is chosen at schedule import, and nothing stores that choice | **The register always computes on COST. The man-hours toggle on the assignment screen is a what-if that must be SAVED to become binding**, and saving it is — correctly — an override (P-47). | Leaving the basis free would let the register and the assignment screen show two different coverages for one line, which is the disagreement the single `Derive()` function exists to prevent: on `BQ-003` the cost basis gives 52.7 / 47.3 and man-hours gives 57.9 / 42.1, and both are right on their own terms. Cost is the honest default because it is the basis every activity has — `BudgetedManHours` is nullable precisely because a P6 file may not carry it, and the toggle is hidden when any activity lacks it. Making the what-if explicit also matches what the toggle really is: a person asking whether the schedule should have been imported on the other basis. **The stored choice belongs on the schedule import (Phase 4.3), and when it exists this becomes reading a column instead of assuming one.** | **CONFIRM** — whether the ministry imports on cost or on man-hours |
| P-49 | Derived decimals arrive over JSON with 26-digit tails — a share of 5.8/11 is `52.727272727272727272727272730`, and an assigned amount of exactly 14,094,000 arrives as `14094000.000000000000000000001` | **Rounded at the transport boundary: money to 2dp, quantities and percentages to 4dp** — the precisions `EpmDb` stores those columns at. Totals are summed at full precision and rounded once, at the end. | It is a projection, not arithmetic: nothing downstream re-derives from these numbers, and rounding a column *after* summing it is what stops a rounded column failing to add up. The alternative — sending the raw decimal — pushes the problem onto every consumer, and the browser is the worst place for it: JavaScript has no decimal type, so the tail becomes a float artefact rather than staying exact. **The related trap is on the client and was measured**: the assignment table originally recomputed the assigned amount from the share it was displaying and printed **14,094,007** where the rule says 14,094,000 — `Domain/Allocation` divides before it multiplies precisely to avoid that. The page now shows the server's figure until the user starts typing, and only then previews. | Resolved |

## REVERTED — visual fidelity chosen over the 05 §7 floors

**Client decision.** Where the reference prototype breaks the binding
accessibility contract, the prototype wins: the screens must be
indistinguishable from the one the client signed off. All four corrections
below were measured, applied, and then removed again.

They are recorded rather than deleted for two reasons. The measurements are the
expensive part and re-taking them later would be waste; and restoring any of
them is a few lines in `web/src/styles.css`, which is only true while the
values are written down.

| # | What v1.1 ships | Floor | Where it bites | Restoring it |
|---|---|---|---|---|
| P-21 | `--outline` / `--viz-base` `#A9B1BC` light, `#454C57` dark | 3:1 graphics (`05 §7.1`) | **2.16:1** light, **1.87:1** dark. Gantt data-date line (`04 §5`), chart baselines, every hairline that carries meaning | `--outline: #858E9C` light / `#6B7484` dark |
| P-30 | delay figure `--error` / `--status-completed-tx`, forecast date `--error` | no threshold colouring (`05 §7.9`) | SCR-E5. A magnitude coloured by threshold, and a date that reads as an error | drop `.epm-sc-*` from the delay and forecast cells |
| P-32 | `--fg-subtle` `#8B94A1` light, `#7D8794` dark, used as TEXT in ~100 rules | 4.5:1 text (`05 §7.1`) | **3.07:1** on white. `.d-stat-foot` and `.d-table td.d-cell-sub` — the KPI foot line and the secondary column on **every register** | `--fg-subtle: #666F7F` light / `#8D97A3` dark |
| P-33 | `--fs-100: 10px` | 11px (`05 §2`) | every filter-chip count in the app | `--fs-100: 11px` |

**`NFR-A11Y-01` is currently NOT met, by choice.** `ROADMAP.md` Phase 7's
accessibility pass is where this is settled before production — that pass
should start from this table.

Two things were NOT reverted, because neither is a fidelity question:

- **`.epm-sr-only`** on the Alerts row-action column header. The reference
  leaves that `<th>` empty; naming it for a screen reader changes nothing
  visually.
- **`<bdi>` isolation** everywhere (`05 §5.2`). Without it Arabic text
  reorders around numbers — that is a correctness bug, not a contrast one.

## Carried forward, still open

`07-POC-BUILD-PLAN.md §9` open questions D-01 … D-05 remain **CONFIRM** — rate bands after
several orders, the penalty rate, per-stage SLA, delegate cancellation authority, and who
revises a distribution when a decrease forces it. None are resolved by this port.
