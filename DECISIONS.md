# DECISIONS — business-rule questions resolved in the prototype

One entry per decision so the final build inherits the reasoning, not just the code.
Mirrors the handoff's open questions (`07-POC-BUILD-PLAN.md` §9). Items marked **CONFIRM** still need client sign-off before production.

| # | Question | Prototype decision | Source | Status |
|---|---|---|---|---|
| D-01 | Rate bands after several orders — does the second order's 20% threshold use the *original* or *current effective* quantity? | Uses the **original** contract quantity (`line.contractedQty`). | 07 §9.1 | **CONFIRM** |
| D-02 | Delay penalty rate | **(value ÷ duration) × 10% per day, capped at 10%** of the effective contract value (`Domain/Penalty.RatePct/CapPct`). **Changed from the 0.1%/day form the port carried** — see P-81. Both terms of the fraction are the values in force (BR-09), so an applied order moves the daily penalty in either direction, and the cap is reached after exactly one contract duration of delay. | 02 §10 · العرض الفني §11 · الشكل 10 | **Resolved on the formula** — still CONFIRM whether the 10% rate is fixed or الشكل 10's «النطاق القانوني 10%–25%» is a range |
| D-03 | Per-stage SLA | Uniform **5 days** (`configuration.rules.slaDaysPerStage`); a stage past SLA is `overdue`, raises needs-action, and escalates. | 02 §12 | **CONFIRM** per committee |
| D-04 | Delegate authority to cancel | The rapporteur may record a cancellation only for external parties flagged `canCancel: true` (Endorsement Review Cmte, Admin & Financial Dept). | 03 §4 | **CONFIRM** |
| D-05 | Distribution vs. decrease | When a decrease would push distributed total above the revised quantity, the order **cannot be applied** until the distribution is revised. Revising party not yet fixed. | 02 §8 | **CONFIRM** (RE dept vs beneficiary) |
| D-06 | "Now" reference | Derived from the **project data date** (`configuration.time.dataDate = 2026-08-02`) in demo mode; real clock in production. Never `Date.now()`. | CLAUDE.md, 06 §12 | Resolved |
| D-07 | BOQ weight rounding | **Largest-remainder** to exactly 100.00%; `toFixed(2)` is a bug. Denominator is the **contract's** BOQ rows. | 02 §1 | Resolved |
| D-08 | Approved value authority | The **approved value** comes only from the pricing/rate-fixing committee, entered at financial review. The RE-dept proposal governs display; the contractor proposal is shown labelled until then. | 02 §6 | Resolved |
| D-09 | Approved ≠ Applied ≠ Closed | Approving changes nothing. Applying creates a contract amendment and moves quantities/dates/penalty baseline. Closing verifies the application. Approved-unapplied orders shown as a **projection**, never folded into effective figures. | 03 §6 | Resolved |
| D-10 | External parties | Statuses inside the owning stage (recorded by a delegate against an official letter number+date), **not** workflow stages. Attribution: deciding party, delegate as recorder. | 03 §3–§4 | Resolved |
| D-11 | Money type | Displayed as integer IQD; production must use `decimal`/integer minor units — never `float`. Tier splits and largest-remainder must be exact. | 07 §2 | Resolved (prototype), enforce in prod |
| D-13 | Project types — which list? | **Three, the client's own split**: `construction` المشاريع الإنشائية · `equipment` مشاريع التجهيز · `design-studies` مشاريع التصنيف (التصاميم والدراسات الفنية). Replaces the earlier eight (new-build · extension · rehabilitation · maintenance · equipment-supply · infrastructure · studies-design · consultancy), which were inferred from the documents rather than given. Consequences: (a) the `expenditure-category` suggestion at المسار 1 step 4 now maps 1:1 construction→تشييد, equipment→تجهيز, design-studies→دراسات, and **nothing suggests صيانة any more** — maintenance work registers as an إنشائي project and the specialist overrides the suggested category by hand, which the field already allows; (b) all five fixture projects now carry `construction`, because none of them is a supply or a design project. | Client, 06 §3 | **CONFIRM** — the Arabic label of the third type reads «مشاريع التصنيف» (classification) while its own parenthetical says التصاميم والدراسات الفنية; if «التصميم» was meant, it is a one-line label change and no code moves |
| D-14 | Does a BOQ have sub-types, and what selects one? | **Base + sub-type, selected by PROJECT type (D-13).** `construction` → `works` · `equipment` → `supply` · `design-studies` → no bill. The shared bill columns (code, description, unit, quantity, rate, division, source) stay in `BoqItems`; the device half (manufacturer, country, model, serial range, supplied/received quantity, warranty) goes in `SupplyItemDetails`, one row per line. Weight, amount and the 20% rule run on the base, so a supply bill prices itself through exactly the rules a works bill does. `Domain/BoqKind` is the only place the shape is decided; an unknown project type returns `none`, never `works`. | Client; reference `../epm/app/supply-items.jsx:30` normalises every فقرة to "the inherited BOQ shape"; الأشكال 50–52 | **CONFIRM** — see the two costs below |
| D-14a | Cost of keying the shape off the project | **A supply contract inside a construction project gets a WORKS bill.** The fixture's `CNT-0279-EM` (الأعمال الكهروميكانيكية, carrying a `supply`-type change order) sits in `PRJ-0279`, typed `construction`; الشكل 12 shows the same pairing. A contract-level `Kind` column was offered and declined. Recorded so this is not later read as an oversight. | — | **CONFIRM** |
| D-14b | Neither document defines a studies bill | Studies contracts accept no BOQ at all; `EP-BOQ-12` refuses before validating any field and the register renders an empty state. Modelling deliverables-with-lump-sums was offered and declined as invented. | — | **CONFIRM** — what a مشاريع التصنيف bill contains |
| D-12 | Contract scoping | A BOQ item / activity belongs to exactly one contract; the project is derived from the contract. One order may not span two contracts (validation gate `cross-contract`). | 01, 02 §7 | Resolved |
| D-15 | Site overhead in the schedule impact estimate | **15% of the activity’s daily rate.** الشكل 23 prints «أثر الكلفة (تقديري)» and its own explainer card gives the shape of the equation — daily rate × overhead × slip days — but names no percentage. 15% is the reference’s own constant (`schedule-module.jsx` `OVERHEAD = 0.15`) and is stated on screen in the «طريقة احتساب أثر الكلفة» card, so a reader sees the rule that produced the figure. `Domain/ScheduleImpact.OverheadPct` is the one place it lives. **Nothing downstream reads the result**: it is a planning figure, not a claim, and it enters neither the contract value nor any payment. | الشكل 23; reference | **CONFIRM** — does a ministry contract state a site-overhead percentage, and is it this one? |
| D-16 | Do the registers hand-write their tables, or share one? | **One `<epm-data-table>` driven by a column CONFIG**, at the client’s instruction after الفقرات التجهيزية shipped a register that differed from الشكل 50 in three ways — the BOQ weight without its label, the الاستلام cell without its percentage, and a totals row the plate does not print. None of the three was a decision; all three were omissions that a hand-written `<thead>`/`<tbody>`/`<tfoot>` invites once per register, and this build has fifteen of them. `web/src/app/shared/data-table.component.ts` owns the `.d-tablewrap`/`.d-table` markup, the `<bdi>` isolation (`05 §5.2`), the money/quantity/date formatting, `04 §9`’s four states and the footer; a column is `{ key, label, kind, width, numeric, value, sub, total }`, and anything that is not a value — a status pill, a progress cell, a link — comes through `<ng-template epmCell="…">`. **This is a deliberate departure from CLAUDE.md’s governing constraint**, «readability and traceability, not architectural correctness — where a best practice adds a hop, it is not wanted here». A column config IS that hop: a reader can no longer see a screen’s columns as markup in that screen’s own template, and must open two files to answer «what does this register show». It is recorded here rather than left silent because the rule it breaks is a standing one. **Scope is الفقرات التجهيزية only.** The other fourteen registers keep their hand-written tables until the shared one is proved on a real screen; converting them is mechanical and is deliberately NOT bundled with the screen that motivated it. The component adds no CSS — it emits exactly the markup the registers already emit. | Client, this session; الشكل 50 | **CONFIRM** — whether the remaining registers convert, and whether register sorting (8.3, currently in `boq.page.ts`) moves into the shared component when they do |

## Assumptions made to fill gaps

- **Seed figures.** Concrete BOQ/activity amounts were chosen to be internally consistent (`amount = qty × rate`) and to exercise the rules; they are illustrative, not real ministry data. The documented worked examples (56.13%/43.87%, the qty-100-add-30 tier split) run in the **Rules Reference** route from their own example inputs, independent of the seed.
- **Man-hours basis** is stubbed to fall back to `budgetedCost` where P6 man-hours are absent (the cost/man-hours toggle is present in the BOQ activity-assignment view).
- **3D/BIM viewer, standalone Claims, Quality (NCR/Lab), HSE, GIS** are out of Phase 1 (07 §8) — tabs are kept and stubbed.

---

# Full-stack port decisions (Angular + .NET + SQL Server)

Decisions taken while porting the React prototype. One entry per decision, with the reasoning,
so the next person inherits the *why* and not just the code.

| # | Question | Decision | Rationale | Status |
|---|---|---|---|---|
| P-01 | Storage shape | **Flat tables, plain ID columns.** No navigation properties, no foreign keys, no cascade rules, no indexes. | A normalised EF model needed ten `OnDelete(Restrict)` overrides purely to satisfy SQL Server's multiple-cascade-path rule — pure ceremony, first thing a reader hits. Relationships now live in the endpoint's `Where()` clause where they can be read. | Resolved |
| P-02 | Schema evolution | **No migrations.** `EnsureCreated()` on boot; `POST /api/dev/reset` to apply a change. | Migration files are the top merge-conflict source for parallel agents, and there is no production data to preserve. Cost: reset wipes anything typed in. | Resolved |
| P-03 | Seeding | **Nothing on boot.** The `06 §12` fixture loads only via `POST /api/dev/load-fixture`. | The reference figures are illustrative, not ministry data — the original `DECISIONS.md` says so itself. Making them opt-in stops invented numbers being mistaken for real ones. | Resolved |
| P-04 | Test isolation | Domain rule tests use **inline worked examples** from `02-BUSINESS-RULES.md` and never read the database. | If a fixture figure turns out wrong, no test starts lying. The spec, not the data, is the oracle. | Resolved |
| P-05 | Identity | **Persona header (`X-Epm-User`), no authentication.** | The permission *model* from `03 §7` is real and resolved server-side; only the identity is fake. `03 §7` itself asks for the persona switcher as the fastest way to review the model. | Resolved |
| P-06 | Build unit | **One page at a time, end to end** — table, endpoint, Angular trio, UML, TRACE row. | Building 30 tables up front from a spec was speculative: columns existed for screens nobody had built. Page-at-a-time means no column exists on a guess. | Resolved |
| P-07 | Design fidelity | **Copy the stylesheets verbatim** (2,947 lines) rather than re-derive tokens. | The reference JSX uses `className` strings, so porting a screen is mechanical and the design *matches* rather than approximates. | Resolved |
| P-08 | Canonical status keys vs reference CSS | API speaks `06 §1` canonical keys (`delayed`, `cancelled`); a `statusClass()` map translates to the stylesheet's older `stalled` / `withdrawn` classes. | The spec keys are correct and must not be bent to the CSS; editing the verbatim stylesheet would break the copied-not-re-derived guarantee. | Resolved |
| P-09 | Derived values not yet derivable | Return **null and render an em dash**, never a zero. | Physical % is weight-rolled BOQ progress (BR-04). Until the BOQ page exists it cannot be derived, and a 0% bar asserts something false. | Resolved |
| P-10 | Reference component vs written spec | **Open the reference component before writing a page.** | PAGE-01 was built from the written spec alone and had to be redone: it invented a column set, used the wrong CSS classes, and missed that the Workspace column is hidden when workspace-scoped. The spec gives the rules; the component gives the screen. | Resolved |
| P-11 | Where the value lists live | **`Features/Lookups/LookupCatalog.cs`**, called by `Fixture.Load()` — not inline in `Fixture.cs`. | `Fixture.cs` is illustrative demo data and says so at the top. The `06 §1–§11` lists are the opposite: they are the specification's own enumerations, every stored code comes from one of them, and a wrong row is a wrong label on every screen. Putting them beside their endpoint keeps one grep for `boq-change-type` finding the list, the endpoint and the Angular caller. They still load only via `POST /api/dev/load-fixture`, because nothing is seeded on boot (P-03). | Resolved |
| P-12 | Which execution-stage list | The **12 construction stages of `06 §2`** (`design` … `handover`), which are the codes the fixture already stores. | `06 §2` names 12 construction-progress stages *and* says to carry the client's Arabic wording from `reference/app/data.jsx`. But `data.jsx`'s `PROJECT_STAGES` is a different list — an administrative lifecycle (دراسة · إعلان وإحالة · سحب عمل · تسوية حسابات) with no correspondence to the construction stages. The two cannot both be `execution-stage`. `06 §2`'s own enumeration wins because `Projects.ExecutionStage` already stores its codes; the Arabic wording is ours. | **CONFIRM** — which list the client means, and whether the administrative one is a second field |
| P-13 | `co-lifecycle` is missing two states | Added **`approved`** and **`cancelled`** to the `06 §7` list of six. | `03 §6`'s lifecycle diagram requires `approved` — the state whose entire purpose is *the values were agreed and **the contract has not changed*** (non-negotiable #2, D-09). Without it, approved-≠-applied cannot be represented at all. `03 §5`'s fourth decision (`cancel`, used when an external party rejects) terminates an order, which needs a terminal state distinct from `rejected`. `06 §7` lists neither; this is a gap in the data dictionary, not a design change. | **CONFIRM** the two Arabic labels |
| P-20 | v1.1 declares Inter but never loads it | **We load Inter.** | `tokens.css` sets `--font-en: 'Inter', system-ui, sans-serif`, but the branch's `index.html` loads **Roboto** and no Inter at all — so its Latin text silently falls back to `system-ui` and the token is decorative. Loading the font the tokens actually name is the fix; the alternative (change the token to Roboto) would be re-deriving the design. A defect in the branch, worth reporting upstream. | Resolved |
| P-21 | v1.1 breaks the binding accessibility contract on two tokens | **Corrected on adoption** — `--outline` and `--viz-base` to `#858E9C` (3.31:1) light and `#6B7484` (3.43:1) dark, overridden in `web/src/styles.css`. | v1.1 ships both at `#A9B1BC` = **2.16:1** on white; `05 §7.1` requires ≥3:1 for borders, icons and graphics, and the palette they replaced sat at 3.03:1 — the old values were chosen to pass. Dark theme was worse: `#454C57` = 1.87:1. These are not decorative: `05 §1` defines `--outline` as "borders and graphics only" and `04 §5` uses `--viz-base` for the Gantt data-date line and bar edges, so WCAG 1.4.11 applies. The chosen values are the **lightest** on v1.1's own neutral ramp that clear the floor, so the intended look survives as far as the contract allows. Overridden rather than edited into `tokens.css` so the copied sheets stay verbatim (P-07). `--outline-variant` / `--stroke-default` left alone — decorative separators are exempt. | **REVERTED by client decision — known defect** |
| P-22 | `--tertiary` is now the same blue as `--status-ongoing-tx` | **Flagged, not resolved.** | v1.1 sets `--tertiary: #1748B0`, identical to the `ongoing` status text colour; the Redwood red it used to carry is gone from the palette. `05 §7.5` forbids one colour carrying two meanings, so using `--tertiary` as a decorative accent anywhere would read as *ongoing*. Nothing in the one screen built today uses it that way, so nothing is broken yet — but every Phase 2 screen could introduce it. | **CONFIRM** — audit `--tertiary` usages before Phase 2 fans out |
| P-17 | The copied stylesheet pins the summary strip, `05 §8` forbids it | **`.d-grid.stats.fit` in `src/styles.css`** overrides only the column count to `repeat(auto-fit, minmax(120px,1fr))`. | `desktop.css` pins `.d-grid.stats` to `repeat(4, 1fr)` and `.d-grid.stats5` to `repeat(5, 1fr)`, with media queries pinning 3 and 2 below — while `05 §8` and CLAUDE.md §6 both require auto-fit. Measured in the browser: 5 stats under the pinned rule lay out **2 + 2 + 1**, the ballooned-orphan defect §8 was written about; under `.fit` they lay out as 5 equal tracks. Overriding one property in the additions file keeps every other `.d-grid.stats .d-stat` rule — hover, watermark, staggered reveal, reduced-motion — and leaves the verbatim copy intact (P-07). | Resolved |
| P-18 | Field grid: reference is 2 pinned columns, ROADMAP asked for auto-fill | **Kept the reference's `.d-form-grid`** (2 columns, collapsing to 1 at ≤900px). | ROADMAP 1.3 specified `repeat(auto-fill, minmax(240px,1fr))`, but nothing in `05` requires it — §8's grid rule is about *figure strips*, not field grids. The reference's cell borders depend on the 2-column count (`.d-form-i:nth-child(2n)` drops the inline-end border, `:nth-last-child(2):nth-child(2n+1)` the last row's bottom), so auto-fill breaks them, and the reference never renders more than two columns. CLAUDE.md §1 is explicit that the reference component gives the screen. | **CONFIRM** — if wide workspace panes should show 3–4 field columns, the border rules need rewriting too |
| P-19 | A CSS syntax error inside the "verbatim" stylesheet | **Fixed in `web/src/styles/desktop.css:739`**; the reference copies are left untouched. | The base prototype's own `styles/desktop.css:739` ends `.d-modnav` with `scrollbar-width: thin; }; }` — a stray `; }`. Browser error recovery skips it, so the reference prototype renders correctly and nobody noticed; Angular's production CSS parser rejects it and **`ng build` failed outright**. Removing the stray tokens changes no declaration and no rendering, so this is a syntax fix, not a re-derivation of the design. Marked in place with a comment. **The same typo is still in `../epm/styles/desktop.css` and in `docs/spec/reference/styles/desktop.css`.** | Resolved |
| P-15 | `02 §3`'s assigned amounts do not match its own rule | **The rule is binding; the prose figures are an illustration.** `Allocation.Shares([5.8, 5.2], 26,730,000)` gives **14,094,000 / 12,636,000**; `02 §3` states 14,092,710 / 12,637,290. | The spec quotes the weights rounded to 1dp (5.8 / 5.2) but computed the amounts from the UNROUNDED underlying weights — `02 §4` says exactly this about its own example ("client's figure 14,057,931 — same rule, unrounded shares"). The delta is 1,290 IQD on a 26.7M line, 0.005%. Bending the arithmetic to match the prose would break the rule for every other input. `AllocationTests` asserts both the rule's answer and the divergence, so nobody "fixes" it later. **ROADMAP 4.2's exit criterion quotes the prose figures and has been annotated.** | Resolved |
| P-16 | Is amendment no. 0 `original` or `superseded` once amendment 1 is applied? | **Always `original`.** `superseded` means an earlier applied AMENDMENT (no ≥ 1). | `02 §9` words the list as "original (no. 0) · superseded (an earlier applied one) · effective (the last applied)" — `original` is keyed to the number, so it is the row's identity, and the chain shows it as العقد الأصلي whether or not it is still in force. The "which version is in force" question is answered by `Amendments.Effective()`, not by this label. Noticed because a first pass wrote the two cases inconsistently and the tests caught it. | Resolved |
| P-26 | `06` defines no value lists for alerts, but `Alerts.Severity` and `Alerts.Kind` are stored codes that need AR/EN labels | **Added three lists to `LookupCatalog.cs` in a clearly-marked ADDENDUM section** (`alert-severity`, `alert-kind`, `alert-status`), with a comment stating they are ours and not the data dictionary's. Codes come from the `Alert` entity's own documented vocabularies. | P-11's argument is that stored codes belong in the catalog: one mechanism (`EP-LKP-01` → `core/lookups.ts`) then labels every enum in the app, the AR↔EN toggle works without a per-screen map, and the client can correct wording without a code change. The alternative — a label map inside the Alerts page — would have been the second mechanism, reachable by no grep. Marking the section rather than silently merging it keeps "`06 §1–§11` verbatim" true of everything above the marker. **`06` should gain these three sections.** | **CONFIRM** — the nine Arabic labels, and whether `06` adopts the lists |
| P-27 | The reference uses **two Arabic forms of each severity** — `حرِج` in the table cell, `حرِجة` in the cards and chips — and two English ones for the top severity (`High` on the chip, `High severity` on the card) | **Both carried.** The singular is the lookup label (one alert's severity, agreeing with «تنبيه»); the plural is a group heading and lives in `core/lang.ts` with the rest of the chrome. | Not an inconsistency in the reference — each form is correct where it stands, and collapsing them to one would put a grammatical error on one of the two. It does mean severity is the one enum whose label does not come from exactly one place, so both sites are cross-referenced in comments. | Resolved |
| P-28 | The reference Alerts feed carries dates **after its own stated data date** (`2026-09-08` under a header reading *بيانات حتى 2026-08-10*) | **Every fixture alert sits on or before the project data date `2026-08-02`.** | D-06 already forbids the wall clock; this is the same rule applied to seeded rows. The reference's dates come from an offset against `Date.now()`, which is why they drift ahead. A demo where alerts are raised in the future undermines the one screen whose entire job is *what needs attention now*. | Resolved |
| P-29 | Is acknowledgement client state or a write? | **A write.** `EP-ALR-02` persists `Acknowledged` and stamps the persona into `AcknowledgedByUserId`. | The reference keeps an `ackMap` in component state — correct for a clickable prototype, wrong for a record. An acknowledgement is somebody accepting that they have seen an alert; one that evaporates on refresh, or that records no one, is indistinguishable from the alert never having been read. Makes `Alerts` the first table a screen writes to. | Resolved |
| P-30 | The reference colours the delay figure `--error` when late and `--success` when not | ~~Not ported.~~ **REVERTED — the reference's colouring is reproduced** (`.epm-sc-late` / `.epm-sc-ontime` / `.epm-sc-fc-late`, tokens not literals). | `CLAUDE.md §6` and `05 §7.9` forbid colouring a magnitude by threshold in as many words — "the neutral branch is `--on-surface`" — and `05 §7.5` reserves the status hues for status. A delay is a magnitude, not a state; the reference also paints the forecast DATE red, which makes a date look like an error condition. **The client chose visual fidelity over the §7 floor** (see the REVERTED block below). | **REVERTED by client decision — known defect** |
| P-31 | Import status has no source — the Activities table is not registered | **Returns `pending` for every project**, from a `ScheduleImported` bool that is currently constant, and the endpoint says why. | This is *true*, not a placeholder: with no Activities table, no P6 schedule can have been imported for anyone. It reads as a real finding — nothing in the portfolio has a published baseline — rather than as a fabricated column. Becomes a genuine `db.Activities.Any(...)` in Phase 4.3 with no change to the DTO or the screen. Labels added as `schedule-import-status` in the LookupCatalog addendum (P-26's section). | Resolved |
| P-34 | The shell was missing twelve of the reference's chrome regions, all of them already styled in the copied sheets | **Built all twelve with real behaviour**: `.d-fill` · `.d-appfoot` · sidebar collapse (`data-side`, persisted) · workspace switcher · account block · `.d-side-sep` · global search + ⌘K palette · `.d-nav-count`. Page-head action clusters restored on all five screens, each firing the reference's own "— تجريبي / — demo" toast. | Diffing the two rendered DOMs turned up twelve `d-*` classes the reference renders and we never did. **Every one already had CSS** — 11 rules for `.d-appfoot`, 14 for `.d-cmdk`, 11 for `.d-side-acct` — so this was unbuilt markup, not missing design. Nothing here is a dead prop: collapse persists, the switcher navigates with `?ws=` (which every endpoint already accepts), and the palette is built from the route table so it can only offer a page that exists. The persona switcher moved from the topbar into `.d-side-acct`, which is the same semantic slot and lets the topbar match the reference exactly. | Resolved |
| P-35 | v1.1 asks for a `support_agent` icon its own `icons.js` does not define | **Used `help`.** | Identical to P-25: the reference renders a fallback glyph in its own footer. Worth reporting upstream. | Resolved |
| P-36 | Angular wraps every component in a custom element, so a component whose ROOT must be a grid child gets a wrapper in between | **`epm-app-footer { display: contents; }`** in `styles.css`. | `.d-appfoot` carries `grid-column: 1 / -1`. With `<epm-app-footer>` in between, the wrapper became the grid item and the footer inherited the 256px sidebar track instead of spanning — measured at 256px wide before the fix, 1440px after. Any future component in the same position needs the same treatment. | Resolved |
| P-32 | **`--fg-subtle` breaches the 4.5:1 text floor in both themes** — measured **3.07:1** on white, 2.86 on `--surface`, 2.76 on `--background`; dark 4.44 on `--surface-container` | **Corrected on discovery** — `#666F7F` light (5.07 on white, 4.55 on `--background`) and `#8D97A3` dark (6.01 on `--surface`, 4.81 on `--surface-container-high`), overridden in `web/src/styles.css`. | `05 §7.1` is binding and 12px/400 is not large text. v1.1 uses this token as TEXT colour in about a hundred `desktop.css` rules, two of which every register already built depends on: `.d-stat-foot` (the KPI foot line) and `.d-table tbody td.d-cell-sub` (the secondary column on Projects, Contracts, Entities and Alerts). It escaped the Phase 1.5 audit because a `.d-cell-sub` nested INSIDE a `td` picks up a different, compliant rule at 6.56:1 — only a `td` carrying the class itself is affected. Chosen by the same method as P-21: the lightest value on v1.1's own cool-grey ramp that clears the floor on every surface the token actually lands on. `--fg-disabled` deliberately untouched — `05 §7` exempts disabled state. | **REVERTED by client decision — known defect** |
| P-33 | **`--fs-100` is 10px**, below the type scale's floor | **Raised to 11px** in `web/src/styles.css`. | `05 §2` is binding and `CLAUDE.md §6` repeats it: the scale is exactly 11/11.5/12/13/15/18/21/24 and nothing is smaller than 11px. `desktop.css:387` sets the filter-chip count to a literal 11px and `desktop.css:3079` then overrides it with `var(--fs-100)`, so every chip count in the app rendered at 10px — measured on Schedule Control, Projects, Contracts and Alerts. 11px is the scale's own floor, so this snaps to the nearest legal step rather than inventing a size. | **REVERTED by client decision — known defect** |
| P-37 | The v1.1 `DReports` is not the same screen as the pre-v1.1 one, and `04 §2` describes the old one | **Ported the v1.1 catalog.** SCR-E7 is a register of the twelve reports a user can run — search, project scope, five category chips, pager — not the four-chart board of `docs/spec/reference/app/desktop-reports.jsx:39`. | Identical to the Alerts Center at Phase 2.4 (card feed → register): the roadmap's own warning says the `docs/spec/reference/` copy is pre-v1.1 and the sibling repo has the screen. Two further reasons this is the right way round. First, the old board's four charts already exist on SCR-E1 — trend, status split, entity ranking — so porting it would have built a second dashboard, and `04 §2` gives SCR-E1 that job. Second, the charts were driven by `window.EPM.buildProjects()`, a generator; against real data three of the four would have had nothing to draw. **`04 §2`'s one-line description of SCR-E7 is stale and should be updated to the catalog.** | **CONFIRM** — that `04 §2` adopts the catalog wording |
| P-38 | A report is a promise about data, and nine of the twelve read a table this build has not registered | **Every catalog row declares the tables it `Reads`; the endpoint compares that against the tables actually registered in `EpmDb` — read off the EF model, not a second list — and returns `available` plus a reason naming the missing source and the phase that builds it.** A blocked row gets no Run button, an explicit in-row note, and «غير متاح» in the action cell. | The reference makes every row runnable because in a clickable prototype every row toasts. Here that asserts twelve capabilities the system does not have, on the one screen whose whole subject is what the system can tell you about itself — and the register would be twelve identical demo toasts, which is the "button that silently does nothing" ToastService.demo exists to prevent. This is P-09's rule ("never render a figure for a missing input — show unavailable + reason") applied to a row rather than a KPI tile, and CLAUDE.md's rule for a gated action ("an explicit locked note, never a bare disabled button") applied outside the persona gating it was written for. Reading the EF model rather than keeping a hand-maintained list means Phase 4.1 registering `Payments` flips RPT-01/02/03/04 with **no edit to this feature** — the alternative goes stale the first time somebody forgets. Today: 3 available (RPT-06 · RPT-08 · RPT-10), 9 blocked. | Resolved |
| P-39 | Angular's `[value]` on a `<select>` whose `<option>`s come from `@for` binds before the options exist | **Carry the selection on each `<option [selected]>` instead.** | Measured on SCR-E7: with the page scoped to `PRJ-0148` — scope bar shown, catalog narrowed 12 → 9 — the dropdown read «كل المشاريع» and `select.value` was `""`. The control was contradicting the screen. The options render after the select's own bindings are applied, and re-fetching the project list re-runs `@for`, so the value lands on an empty list every time. `<epm-pager>` has the same shape and is not affected only because its first option *is* its default. **Any future `<select>` fed by `@for` needs the same treatment**, or `[ngModel]`, which this project does not use. | Resolved |
| P-40 | The v1.1 workspace is not three panes, and both `04 §3` and ROADMAP Phase 3 say it is | **Ported the v1.1 shell**: topbar project picker · one detail pane · grouped module rail · Z2 identity bar. **And the rail shows all fifteen modules, the thirteen unbuilt ones disabled with the phase that builds them** — ROADMAP's "only tabs that exist appear" would have left two entries and four empty group headings. | Same situation as P-37, one level larger. v1.1's `DWorkspace` renders `<div className="d-three" data-ctx="off">` containing a single `.d-pane.d-detail`, with the comment *"DETAIL — actions now live in the page header, no side pane"*; `DProjectContext` is exported and rendered by nothing. The per-module actions it held moved into Z6, beside the view they act on. On the rail: the grouping IS how the workspace states what a project consists of, and a reviewer needs that whole before any of it is built — so it renders, and each unbuilt entry says which phase. Nothing is a dead link: `built: false` is not routable and has no route entry either, which is the half of the guarantee `app.routes.ts` owns. **`04 §3` should be updated to the v1.1 anatomy.** Both halves were put to the client and chosen deliberately. | **CONFIRM** — that `04 §3` adopts the v1.1 workspace |
| P-41 | `desktop.css:865` puts `letter-spacing: .6px` on the module rail's group labels | **Overridden to `normal` in `web/src/styles.css`.** `text-transform: uppercase` left alone. | `CLAUDE.md §6` and `05 §2` forbid letter-spacing in as many words, and the reason is not aesthetic: Arabic is cursive and spacing the glyphs severs the joins. That makes it the same class of defect as the missing `<bdi>` isolation, which Phase 2.9 explicitly did **not** revert because it is a correctness bug in Arabic rather than a contrast one. `text-transform` is a no-op on Arabic (no case) and keeps the reference's look on the English labels, so it stays. **Separately noted, not acted on:** `.d-stat-val` is **28px** and its suffix **16px** — neither is on the 11/11.5/12/13/15/18/21/24 scale, and it affects every KPI band already merged. Flagged like P-22 rather than changed, because changing it moves six screens. | Resolved (letter-spacing) · **CONFIRM** (the 28px/16px scale breach) |
| P-42 | A module page reads `:id` off `route.parent`, and the parent route OUTLIVES the module | **`takeUntilDestroyed()` on the parent-route subscription** in both module pages. | Measured: on Information, switching project fired `GET …/PRJ-0148/overview` *and* `…/information` — the destroyed Overview component's subscription was still live and re-fetched for a component that was no longer on screen. Every enterprise page subscribes to its own `route.queryParamMap` without unsubscribing and is fine, because there the ActivatedRoute dies with the component; a CHILD reading its parent is the case where that stops being true. **Every workspace module added from Phase 4 on needs the same treatment** — it is load-bearing, not hygiene. | Resolved |
| P-43 | A .NET feature namespace `Epm.Api.Features.Contract` shadows the `Contract` ENTITY for every file under `Epm.Api.Features.*` | **The feature is `ContractTab` / `contract-tab` on both sides.** The route segment stays `contract` — it is the module id the rail uses. | `Fixture.cs` stopped compiling with *"'Contract' is a namespace but is used like a type"*: from inside `Epm.Api.Features.Dev`, the sibling namespace wins over `Epm.Api.Data.Entities.Contract`. CLAUDE.md §2's one-slug rule bends here rather than breaks: both stacks say `contract-tab`, so one grep still crosses them, and the alternative — qualifying `Entities.Contract` at every use — would have to be repeated in every future feature file that touches a contract. **Any feature named after an entity has the same problem** (`Payment`, `Project`, `Alert`): name it `<Entity>Tab` or after the screen. | Resolved |
| P-44 | `02 §4` says financial % "comes from payments" but never fixes the denominator | **No financial % is rendered on SCR-W3.** The money is shown — disbursed, certified, retention, advance recovery, remaining — and the part-to-whole bar is unlabelled geometry beside figures already printed in words. | The candidates are the EFFECTIVE value and the TOTAL contract cost (award + reserve + supervision), and they differ by ~7.5% on `CNT-0279`. Picking one would put a number on a guess, on a screen whose whole subject is which figure is which. `01 §2.2` calls financial % derived and points at `02 §6`, which is about proposals, not disbursement. Recorded here rather than guessed, per CLAUDE.md §8. | **RESOLVED at the project level by P-180 — the client's own document answers it.** العرض الفني §23-1 defines الإنجاز المالي as «المصروف التراكمي نسبةً إلى الكلفة المعدلة», so the denominator is the RECORDED revised budget, and `Domain/BudgetBasis` is where SCR-W1, SCR-W6 and SCR-W7 all read it. **Still CONFIRM at the CONTRACT level**: §23-1 speaks of the project, and SCR-W3 still renders no financial % because a contract has no recorded budget of its own — only an effective value and a total cost, which is the pair this row opened on. |
| P-45 | **The v1.1 reference computes the delay penalty by a different rule than `02 §10`** | **`Domain/Penalty.cs` is unchanged — 0.1%/day capped at 10% (D-02) — and the screen shows that.** The reference's formula is recorded here, not implemented. | v1.1's `penaltyOf()` is `perDay = value × rate / duration`, `cap = value × rate`, with `rate` a per-contract band of 10–25% defaulting to 10%, and a comment citing *"Regs 2/2014"*. That is a total ceiling reached when the delay equals the full contract duration — a different rule, not a different rounding. On `CNT-0279` (250,000,000 effective, 531 days, 16 days late) the spec's rule gives 4,000,000 and the reference's gives ~120,000; on `CNT-0207` the spec's rule caps at 3,120,000 where the reference's would too, but for a different reason. CLAUDE.md §1 settles which wins — *"the written spec gives you the rules, the reference component gives you the screen"* — and `PenaltyTests` asserts `02 §10`'s worked example, so changing it silently would break the spec's own oracle. **But the reference is newer than `02` and cites a regulation, which is evidence about what the ministry actually applies.** D-02 was already marked CONFIRM; this is the concrete alternative to confirm it against. | **CONFIRM** — which penalty rule is contractual, and whether the 10–25% band is per contract |
| P-14 | Rounding scope | **BOQ weights round (largest remainder, BR-01); schedule weights do not.** | `02 §1` states BOQ weights must sum to exactly 100.00% and gives 56.13/43.87 as the worked example, so rounding is part of that rule. Activity absolute weights are different: `02 §3` feeds them straight into the allocation share and `02 §11` into earned value, so rounding them at source would push error into every derived figure. `Domain/ScheduleWeights` returns exact decimals; a screen that shows a weight column rounds for display with `Rounding.LargestRemainder`. | Resolved |

| P-46 | **`04 §4` gates the BOQ tab behind a contract selector; the v1.1 component deliberately does not** | **Gated, with one exception: a project with exactly ONE contract is not asked.** The gate renders «اختر عقداً للبدء» with one button per contract, each carrying its item count. | The reference's own comment argues the other way in as many words — *"open on the first contract rather than gating the page behind a picker: the selector in Z6 is right there, and an empty page teaches nothing"* — and it is a fair point about a clickable prototype where every contract has data. It is the wrong call here. `01 §1` makes the contract the scope of a BOQ item, so "the project's BOQ" is not a thing that exists; opening on `contracts[0]` silently picks one of two bills of quantities and shows 240,000,000 of civil works under a heading that says nothing about which contract it belongs to. `04 §4` and ROADMAP 4.2 both ask for the gate, and CLAUDE.md §1 gives the written spec the rules. The single-contract exception is not a compromise: there is no choice to make, and it is the same call SCR-W3's register already makes (P-16's neighbour). The selector stays in Z6 either way, so switching contract is still one control. | Resolved |
| P-47 | Is the allocation override per LINK or per BOQ ITEM? `BoqActivityLink.IsManual` is a per-row column, but `02 §3` says "manually overridable **per BOQ item** and persisted" | **Per ITEM.** One manual link puts the whole line into override mode: every share on it is then the stored value, and a reset clears `IsManual` on all of them. | The per-row column suggests mixing, and mixing is incoherent. BR-03's computed share is `absWeight / Σ(absWeights of the LINKED activities) × 100` — a share **of the set**. Freeze one row at 60 and recompute the others against the same denominator and the total is 60 + 100 = 160%, a figure nobody chose and no rule produced. `02 §3`'s wording is therefore not loose, it is the answer. It also has a consequence worth stating: **a computed set always sums to exactly 100**, so `partial` and `over` — two of `06 §11`'s four states — are reachable *only* through an override. That is why the fixture's partial and over lines are manual ones, and it is what the assignment screen exists to let someone fix. `IsManual` stays per row because that is where EF can store it; the endpoint reads it as `mine.Any(l => l.IsManual)`. | Resolved |
| P-48 | `02 §2` says the weight basis is chosen at schedule import, and nothing stores that choice | **The register always computes on COST. The man-hours toggle on the assignment screen is a what-if that must be SAVED to become binding**, and saving it is — correctly — an override (P-47). | Leaving the basis free would let the register and the assignment screen show two different coverages for one line, which is the disagreement the single `Derive()` function exists to prevent: on `BQ-003` the cost basis gives 52.7 / 47.3 and man-hours gives 57.9 / 42.1, and both are right on their own terms. Cost is the honest default because it is the basis every activity has — `BudgetedManHours` is nullable precisely because a P6 file may not carry it, and the toggle is hidden when any activity lacks it. Making the what-if explicit also matches what the toggle really is: a person asking whether the schedule should have been imported on the other basis. **The stored choice belongs on the schedule import (Phase 4.3), and when it exists this becomes reading a column instead of assuming one.** | **CONFIRM** — whether the ministry imports on cost or on man-hours |
| P-49 | Derived decimals arrive over JSON with 26-digit tails — a share of 5.8/11 is `52.727272727272727272727272730`, and an assigned amount of exactly 14,094,000 arrives as `14094000.000000000000000000001` | **Rounded at the transport boundary: money to 2dp, quantities and percentages to 4dp** — the precisions `EpmDb` stores those columns at. Totals are summed at full precision and rounded once, at the end. | It is a projection, not arithmetic: nothing downstream re-derives from these numbers, and rounding a column *after* summing it is what stops a rounded column failing to add up. The alternative — sending the raw decimal — pushes the problem onto every consumer, and the browser is the worst place for it: JavaScript has no decimal type, so the tail becomes a float artefact rather than staying exact. **The related trap is on the client and was measured**: the assignment table originally recomputed the assigned amount from the share it was displaying and printed **14,094,007** where the rule says 14,094,000 — `Domain/Allocation` divides before it multiplies precisely to avoid that. The page now shows the server's figure until the user starts typing, and only then previews. | Resolved |
| P-50 | **What is an activity's ABSOLUTE weight the share OF?** `02 §2` says "÷ ALL activities" without saying all of what, and the client's lite prototype divides by the PROJECT | **÷ the CONTRACT.** `Domain/ScheduleWeights.For(value, allTotal, parentTotal)` is passed the contract's total, and SCR-W5's «الوزن المطلق» column is that figure. | `02 §3` settles it by arithmetic rather than by wording: A5 is 13,920,000 of `CNT-0279`'s 240,000,000 and the spec prints **5.8%**. Against the project's 340,000,000 the same activity is 4.09%, a number that appears nowhere in the specification. It is also the only reading consistent with `01 §1` — an activity belongs to exactly one contract, so "its share of the programme" is a question about that contract's programme. **The divergence changes no money**, which is why it can be recorded rather than escalated: BR-03 divides one absolute weight by a sum of absolute weights over the same set, so a common scale factor cancels exactly. It would only bite if a figure were ever compared across contracts — and `01 §2.4` forbids the row that would do it. | Resolved |
| P-51 | **The reference rolls a WBS node's progress up by original DURATION; `02 §4` says by WEIGHT** | **By weight.** `ProgressReflection.Rollup(Σ basis, Σ basis × progress)` — the same function SCR-W4 already uses for a contract total, applied to a subtree. | `02 §4`'s sentence is "project physical % rolls up by weight", and the rule has a reason a duration roll-up does not: weight is what the contract is *paid* on, so a weighted roll-up is the only one that agrees with earned value (BR-11) and with the BOQ progress BR-04 derives from the same activities. Rolling up by duration lets a 210-day supply activity of 40,000,000 outrank a 30-day commissioning activity of 15,869,000 by seven to one on time alone, and on `CNT-0279`'s node 3 the two methods differ by several points. The reference is not wrong for a clickable prototype — duration is the field a Gantt already has on screen — but this system reports to a ministry that pays against progress. **The visible consequence**: node 2 reads 67% by weight where duration gives a different figure, and nothing on screen explains the difference, so the roll-up basis belongs in the eventual user documentation. | Resolved |
| P-52 | **`desktop.css:637` paints a critical Gantt bar `--error`, contradicting `04 §5`, `CLAUDE.md §6` and the reference's own legend** | **The bar keeps its STATUS fill and wears a 2px `--on-surface` ring**, overridden in `web/src/styles.css`. The fill travels as a `--bar-status` custom property that the rule itself reads. | This one is not a judgement call: `04 §5` says "critical path is a 2px `--on-surface` ring, not a colour — the legend shows the ring", `CLAUDE.md §6` repeats it ("criticality is a ring… the colour channel belongs to status"), and `desktop.css:605` already renders `.d-gantt-legend i.crit` as exactly that ring. So the reference's chart disagrees with the reference's own key, and a delayed critical activity and an on-time critical activity are drawn identically — which is the one distinction the status channel exists to make. **Why a custom property rather than an inline `background`:** an inline style would merely out-specify the red rule, leaving a stylesheet that is still wrong and a later edit able to remove the only thing hiding it. Reading `var(--bar-status)` inside the rule makes the STYLESHEET correct on its own. | Resolved |
| P-53 | **BR-11 takes `planned` as an input and `02` never says where it comes from.** Without it there is no SPI | **`Domain/PlannedProgress.PlannedPct` — an activity earns its weight LINEARLY across its own BASELINE span**, rolled up on the same cost weights physical % uses. Recorded as an ASSUMPTION, not a rule. | The question has to be answered somewhere, and the honest place is a rule file that says what it assumed. The two alternatives are worse: a **constant offset** (`planned = actual + 8`, which the reference's own comment says was tried and abandoned) makes every project identically behind, which measures nothing; an **S-curve over the project span** is unfalsifiable in detail and would disagree with the delay days BR-10 computes from the same baseline dates, with no second source to settle which is right. Linear-per-activity has the property both lack: it is computed from the SAME baseline dates and the SAME cost weights physical % uses, so SPI compares like with like and a reader who disbelieves it can re-derive it from two columns already on SCR-W5. **The guard order matters and a test caught it**: a milestone has `start == finish`, so on the day it falls due both guards match — testing `now <= start` first reported a milestone as demanding nothing on the very day it was due. The finish test comes first. If the ministry's own progress curve exists, this is one function to replace. | **CONFIRM** — whether the ministry has a defined planned-progress curve |
| P-54 | SCR-W6, SCR-W7 and SCR-W1 all need a BOQ line's progress and achieved amount, which `BoqEndpoints.Derive` already computes for SCR-W4 — and CLAUDE.md §3.2 forbids a service class | **`Derive` became `internal` rather than being duplicated.** Three features now call it. | Its own comment states the invariant: *"The register and the assignment screen are two views of ONE model, so they are derived by one function. Two of them would be two chances for the coverage on the grid to disagree with the coverage in the editor."* Re-deriving in `ProgressEndpoints` would be exactly that trap one screen further on — and the figure at stake is the one the client's own worked example turns on. **This is not the service class §3.2 rules out**: it holds no state, injects nothing, is not registered in DI, and every piece of arithmetic inside it is still `Domain/`'s. It is one feature reading another feature's projection, and the anchor comment at the call site says so. The alternative was ~90 lines copied three times. **The visible payoff**: SCR-W1's physical %, SCR-W6's and SCR-W7's are the same number by construction, not by coincidence. | Resolved |
| P-55 *(SCR-W5 half SUPERSEDED by P-192)* | **`04 §3` and the v1.1 reference make Progress a READ-ONLY dashboard** — its header comment says so in as many words — while `07 §M3` and ROADMAP 4.4 ask for *"change an activity's progress, watch BOQ progress, achieved quantity and achieved amount update"* | **Both.** The dashboard is the reference's; the editor sits beneath it in the same view, with the BOQ lines it moves visible. SCR-W5 refuses the same edit. **And SCR-W6 is NOT gated on a contract**, unlike SCR-W4 and SCR-W5. | The two are not in conflict once you ask *where* the edit belongs. `02 §4` makes progress reflect straight onto the BOQ, so the screen that edits it must be the screen that shows the consequence — on SCR-W5 the consequence is off-screen entirely, which is why the Gantt's activity record carries a read-only note pointing here instead. Putting the editor under the dashboard also gives the reference's tiles something to be about: they stop being a report and start being the thing that just moved. **The gate is a separate question with a different answer**: a bill of quantities and a programme each belong to one contract (`01 §1`), but `02 §4` ends "project physical % rolls up by weight" — across every contract. Gating this page would put the project's own headline figure behind a picker. The reflection tables carry their contract per row, and the column is dropped when there is only one. | Resolved |
| P-56 | The reference's financial module has six tabs; two of them — **annual allocation** and **advance audit SLA** — have no source in this data model | **Rendered as "unavailable + reason", not invented.** No table records a yearly ministry allocation, and no column records when a certificate entered each audit stage. | The same call SCR-E1 made about physical % and SCR-E5 made about the critical path (P-09), and it is the one this system has to keep making: `01` defines neither, so building them means choosing figures. The SLA is the sharper case — the reference derives it from `DATA_DATE − 9 days`, i.e. it assumes a submission date rather than reading one, and the output is a **legal pay-by date**. A fabricated legal deadline on a ministry screen is not a placeholder, it is a liability. Both reasons live on the SERVER beside the rule that owns them, so the screen only picks a language. | **CLOSED — the allocation half by P-92, the SLA half by P-97. `EP-FIN-01` has no `unavailable` entries left.** |
| P-57 | `01 §2.3` calls `award_amount`, `reserve_amount` and `supervision_amount` "the three expense items", and the reference renders them as an indented sub-tree under each contract — but they do not sum to it | **They are NOT a partition and are no longer drawn as one.** The cost sheet keeps the contract row; the three travel beside it under their own heading. | `01 §2.3` defines `original_value` as *"the awarded value"*, and `award_amount` is that same money seen again — on every fixture contract the two are equal. So award + reserve + supervision exceeds the contract by exactly the two allowances: `CNT-0279` reads 240,000,000 as a contract and 258,000,000 as three components. A tree whose children out-total their parent is worse than no tree, and the reference knows it — it prints a message bar underneath explaining why its own numbers disagree ("two bases: approved budget vs contracted commitments"). Making the parent Σ components instead would have been the other way out, and worse: the contract row would then read 258,000,000 where SCR-W3, SCR-W1, SCR-E3 and every total in the system say 250,000,000. **Measured before the change**: the sheet's own footer did not equal its rows. | **SUPERSEDED by P-89 — the plates show the three items partitioning the contract; the fixture was wrong, not the model.** |
| P-58 | ROADMAP names SCR-W8's reference as `DModVO` `project-modules.jsx:1142`, and there is a component of that name there — but v1.1 has a SECOND one | **`vo-record.jsx:454` is the real component.** ROADMAP and TRACE are corrected. | `vo-record.jsx:4` says it outright: *"Loaded after project-modules.jsx so this DModVO replaces the earlier one."* The pre-v1.1 one is dead code that still parses, which is the worst kind of stale reference — building from it would have produced a screen that looks plausible and matches nothing the client signed off. The ROADMAP already warns that `docs/spec/reference/` is the pre-v1.1 copy and that line numbers are stale for every screen v1.1 rewrote; this row is the case where the FILE moved too, not just the line. Same class of error as P-37 (SCR-E7's two different `DReports`). | Resolved |
| P-59 | `03 §10` lists «متأخر» and «يحتاج إجراء» as exception chips, and BR-12 defines a PER-STAGE SLA. Are they the same measurement? | **No — two clocks.** «متأخر» is the WHOLE ORDER, 14 days since the incoming letter. «تجاوزت السقف» is the CURRENT STAGE against its own `SlaDays` (BR-12). An order can breach either without the other. | `06 §12` seeds the proof and says why: VO-02 at 22 days and a young pending order at 5 exist *"to prove pending and overdue are different sets"*. **That young order is now VO-07, not VO-06** — VO-06 was seeded applied to give `BoqRateBands` its first row, and minting VO-07 was the condition of doing so without collapsing this proof (P-160). Measured after the change: pending = {VO-02, VO-07}, overdue = {VO-02, VO-03}, so the sets differ in BOTH directions. If the two chips shared one clock, «متأخر» would be a synonym for «قيد الاعتماد» on any order older than the ceiling, and the register's whole attention model would collapse into the lifecycle column it sits beside. The stage clock also survives a stage changing hands — a fresh stage starts a fresh SLA while the order's own age keeps running, which is exactly the distinction a committee chair needs to see. **A related split**: the «قيد الاعتماد» INDICATOR counts the `pending` lifecycle only, so it agrees with the group of the same name, while needs-action and overdue count pending **and** returned, because a returned order is back with its originator and that is somebody's work. Measured before the change: the indicator said 3 while its own group tab said 2. | Resolved |
| P-60 | `03 §7` gives the delegate relation `recorder` — "the viewer is the delegate and an external party is pending" — but `ChangeOrderExternalParty` is a Phase 5.4 table | **`externalPartyPending: false` is passed EXPLICITLY, so `recorder` is unreachable in 5.1 rather than guessed at.** | The alternative — inferring "an external party is probably pending" from a stage owner or a lifecycle — would fabricate an authorisation. `recorder` is one of only two relations that ENABLE actions (`03 §7`'s gating rule), so a false positive there is a permission granted to someone the specification did not grant it to. Passing the `false` at the call site with a comment is better than omitting the parameter: it records that the input is missing rather than that the case does not exist. The same P-09 instinct applied to a boolean instead of a figure — and it becomes a real query in 5.4 with no change to `Domain/ViewerRelation`, which already takes it as an argument. | Resolved |
| P-61 | The audit found no link at all between a user and a workspace, so `EP-ENT-01` returned every workspace to everyone and `?workspace=` was a filter the CALLER chose | **A persona carries `Workspaces` + `MinistryWide` (BR-15).** `Domain/WorkspaceAccess` holds the rule, `Features/Workspaces/WorkspaceScope.Deny` is the one-line guard, and every scoped endpoint calls it. **No group, role, permission, policy or scope table.** | `§7` states the rule as a ضابط أساسي — «نطاق رؤية المستخدم هو اتحاد تكليفاته … ولا يرى بيانات خارج تشكيله» — and `§24` defines assignment as user → role → scope. The prototype has to demonstrate the VISIBILITY half, and the role half already exists as `Persona.Party` (BR-14). Modelling the other four levels would have added five tables and a matrix screen to prove a rule that two fields prove. The identity is still the spoofable `X-Epm-User` header (P-05) and deliberately so: what is enforced is the business rule, not the session. | Resolved |
| P-62 | Should the enterprise view — no `?workspace=` at all — show the whole ministry or only the viewer’s workspaces? | **Only theirs.** `WorkspaceScope.Effective` resolves an absent workspace to the union of assignments, so "all projects" means all of MINE. Ministry-level personas are assigned to everything and therefore still see everything. | `§7`’s rule is about the USER, not about a filter they happen to have set. If the unscoped view showed the whole portfolio, «لا يرى بيانات خارج تشكيله» would hold only while a workspace was selected — which is the opposite of a control. Measured: `user.re-dept` unscoped now returns 3 projects / 420,000,000, identical to `?workspace=ub`, while `user.senior-mgmt` returns 5 / 451,200,000. | Resolved |
| P-63 | Selecting a workspace in the switcher merged `?ws=` onto whatever page you were standing on | **Selecting a workspace ENTERS it and lands on its overview; «المساحة الرئيسية» returns to the ministry portfolio.** New route `/workspace?ws=`, new screen SCR-E8. | The reference does exactly this — its switcher rows call `openWorkspace(w)`, which sets `view=’overview’` (desktop-shell.jsx:302, :422) — and الشكل 1 → الشكل 2 documents the transition. The merge behaviour produced the state the audit flagged: a user inside a project of workspace A with a breadcrumb reading workspace B, and a project picker that did not contain the open project. Scope moved and nothing landed. **Cost:** switching from Contracts no longer keeps you on Contracts. Accepted — the workspace nav is one click away and coherence beats place-keeping. | Resolved |
| P-64 | `?ws=` is the app’s only scope mechanism, so where does the workspace overview live? | **`/workspace?ws=ub`, not `/workspaces/ub`.** No `?ws` redirects to the register. | A path segment would be a SECOND way to say which workspace is in scope, and the switcher, every sidebar link, six endpoints and every module rail link already speak `?ws=`. Two mechanisms means two things to keep in sync and a class of bug where they disagree. The Angular folder is `features/workspaces/` (plural, the enterprise workspace) beside the existing `features/workspace/` (singular, the project workspace shell) — the same distinction the documents draw between «مساحات العمل» and «مساحة المشروع». | Resolved |
| P-65 | A `?ws=` the persona may not open — pasted, bookmarked, or left over after a persona switch | **The shell corrects it once: toast + redirect to the register.** The API still 403s independently. | Six pages each rendering their own error is not an answer to "you are not assigned to this workspace", and the register is the screen that lists what you MAY open. The guard waits for `EP-ENT-01` before judging, so it never redirects on a list it has not seen. It is convenience only — `WorkspaceScope.Deny` does not care what the client decided, which is why the direct-API tests still return 403 with no browser involved. | Resolved |
| P-66 | `.d-stat` is `overflow: hidden`, `.d-stat-val` is 28px, and the strip floor is `minmax(120px, 1fr)` | **A strip carrying a ≥ 9-character figure raises its own floor to 180px** (`SummaryStripComponent.needsWide` + `.epm-stat-wide`). | Measured on SCR-E8 at a 900px viewport: the tile was 147px, the number 148px, and "420,000,000" rendered as **"20,000,000"** — not truncated with an ellipsis, silently beheaded into a different and entirely plausible number. Widening the track rather than shrinking the type keeps the 11/11.5/12/13/15/18/21/24 scale intact; one tile in a band set smaller than its neighbours is its own defect. Fixes SCR-E1 at narrow widths too, since both use the same component. | Resolved |
| P-67 | `.d-ctx-emblem` sets `color: #fff` and no background — the reference supplies one inline per workspace from a palette this port does not have | **Each plane gets a neutral chip:** translucent white on the charcoal sidebar, `--surface-container-high` in the popover, and `.ent` keeps its white chip with a `--primary` mark. | Measured: the workspace code in the switcher popover was `rgb(255,255,255)` on `rgba(0,0,0,0)` — white on white, present in the DOM and readable by nothing — and the ministry emblem was a white icon on `.ent`’s white chip. Inventing a colour per workspace was rejected: `05 §1` has every colour channel already assigned and identity is not one of them. The neutral chip also makes the register, the sidebar and the popover show one object three times instead of three treatments. | Resolved |
| P-68 | The workspace register labelled its kind column from `beneficiary-type` | **New `workspace-kind` lookup with الشكل 1’s own four values**; `Workspace.Kind` migrated to them. | A beneficiary RECEIVES distributed quantity (`01 §2.1`); a workspace OWNS projects (P-24). The two lists share the word "university" and nothing else, so a directorate rendered as the raw string `directorate` — latent only because the fixture had three universities and nothing else. الشكل 1 names the four the register filters by: جامعة حكومية · جامعة تقنية · وحدة مركزية · مديرية تجهيز. The fixture now carries four of them, which is what makes the filter chips real. | Resolved |

| P-69 | `docs/spec/reference/` is a **pre-v1.1 snapshot**, and الشكل 5's screen had moved on inside it | **The LIVE prototype at `infinite-azaiton.github.io/epm` is the visual authority for الشكل 5**; the checked-in copy is kept for everything not yet re-audited. | The checked-in `DModInformation` groups fields with a regex over ENGLISH labels and renders `.d-sec`. The live one (v338) renders `.d-fgroup` cards, puts its tabs in a `.d-pz5` zone, adds a `.d-z9` action bar and edits INLINE — and the appendix's own الشكل 5 screenshot matches the LIVE one, not the checked-in one. Same class of staleness as P-58, one level worse: there the component moved file, here the whole screen was redrawn. **The rule this sets:** before building a screen from `docs/spec/reference/`, diff it against the live prototype — the ROADMAP already warns line numbers are stale, but the SHAPE can be stale too. | Resolved |
| P-70 | Editing a project lived at `/projects/:id/edit` — a full-page form outside the project workspace, and **nothing in the UI linked to it** | **الشكل 5 edits IN PLACE.** «تعديل» flips each cell to a control on the same screen; `/projects/:id/edit` is deleted and `project-form.page` is create-only. | The document puts «تحرير البيانات» inside «مساحة المشروع › معلومات المشروع», and the route put it outside — dropping the module rail and the identity bar, which are what the documented breadcrumb is made of. It was also unreachable: `grep -rn "/edit" web/src` returned the route, a shell guard and comments, and no navigation. So the app had a complete, working, invisible editor and a read-only screen where the editor belonged. **What did NOT change:** the write path. `EP-PRJ-04` loads the definition and `EP-PRJ-03` saves it, exactly as before — الشكل 5 is a second CALLER of المسار 1's endpoint, never a second copy. The create form still posts the same `ProjectDefinitionInput` to `EP-PRJ-02`. | Resolved |
| P-71 | الشكل 5 stars TEN fields; المسار 1's one-line gate («اكتمال الحقول الإلزامية وصحة سنة الإدراج والكلفة») names fewer, and omits four the screen marks | **The RULE was extended to meet the document**, not the stars trimmed to meet the rule. `status`, `formation`, `orgStructure` and `consultantParty` are now refused when empty. | A star the save does not enforce is a false statement on a screen the documents call «المصدر الوحيد» for this data, and this screen is where a specialist learns what the system requires. The reverse fix — unmarking four fields — would have made the card disagree with its own reference screenshot. `RequiredFields` (what الشكل 5 stars) and `EnforcedFields` (what `Validate` refuses) are kept as two sets precisely because they are not identical: **`plannedCost` and `workspaceCode` are enforced and NOT starred**, because المسار 1 requires them and الشكل 5 does not show them at all. A test asserts each of the ten is refused when empty, so the star and the 422 cannot drift apart. The fixture gained a consultant on four projects that had none — otherwise editing them would 422 on a field that was blank before anyone touched it. | Resolved |
| P-72 | The «مقترح» prose names TWO fields (رمز المشروع · المنطقة الجغرافية); the screen marks FIVE, adding أولوية المشروع · الفئة الإنفاقية · رقم اعتماد الموازنة | **Five** — the screen is what renders the badge. The test behind it stays `has a value`. | Prose and screenshot are the same document and the screenshot is the more specific statement. What is NOT resolved is the badge's MEANING: nothing records who put a value there, so a code the specialist typed is tagged «مقترح» too, permanently. A provenance column was deliberately not added, because the documents describe no confirm step that would ever clear it — inventing an acceptance workflow to justify a badge is a business rule invented to complete a UI. Only two of the five are actually derived (`Suggest()` fills the code and the expenditure category on create); the region has no source at all, since `Workspaces` carries no governorate column. **The visible consequence:** the card cannot yet keep its promise to «تمنع اعتماد قيم غير مؤكدة دون قصد». | **CONFIRM** — is there a confirm step, and what derives the other three? |
| P-73 | `.d-pz7` scrolled on **none** of the seven built workspace modules | **`display: contents` on the module host elements** (`web/src/styles.css`), the same fix that file already documents for `<epm-app-footer>`. | `.d-pframe` is written as `flex: 1; min-height: 0` and expects to be a direct flex child of `.d-detail-main`, so `.d-pz7` is the one scrolling box. Angular puts TWO component hosts on that chain — `<epm-workspace-page>` and the module's own — and a plain block host takes `height: auto`, so the frame grew to its content and the overflow travelled up to `.d-fill { overflow: hidden }`, which clipped it. Measured at 910px with the fixture loaded: overview · progress · financial · changeorders · information overflowed `.d-main` by up to 780px, and the bottom of each was unreachable. **It predates الشكل 5** and was only found because that screen's six cards plus its edit bar are the tallest content in the workspace. Every module added later needs its selector in that rule, or its page will not scroll. | Resolved |
| P-74 | Native `<select>` draws its option list with the OPERATING SYSTEM | **`shared/select.component.ts`** — a combobox on the existing `<epm-popover>`, with `.d-pop-row` options. | Everything above the popup was themed and the popup was not: our font, our RTL, our spacing and our dark theme all stopped at its edge. The trigger stays `.d-form-input` so a select and a text box sit on the same line, and the panel keeps the surface the switcher and account menu already use. Three details were measured rather than guessed: `.d-pop`'s 7px padding put option text **17px** from the panel edge against the field's **10px**, `min-width: 260px` made the panel wider than any narrower field, and its `overflow-y: auto` nested a second scroller inside the list's. All three are neutralised for select panels only, via `.d-pop:has(> .epm-select-list)`. **A popover bug fell out of it:** `<epm-popover>` closes on scroll to stay anchored, but its listener CAPTURES, so it also caught scrolls originating inside itself — the option list closed the panel on the first pixel and snapped back to 0. It now ignores scrolls from within; page scroll still closes it. | Resolved |
| P-75 | الشكل 7's header prints **«مبلغ الإحالة 520,200,000»**, and the SAME screen's تفصيل الكلفة prints **«الإحالة 479,400,000»** — one label, two amounts, one card apart | **The plate's NUMBER, الشكل 6's LABEL.** The header field renders `originalValue` under «القيمة الأصلية»; «الإحالة» stays the cost item it is in تفصيل الكلفة and in الشكل 8. | 520,200,000 is 479,400,000 + 25,500,000 + 15,300,000 — the contract's original value, which is what الشكل 6 calls «الأصلية» and what the header pairs against «القيمة النافذة» on the line below it. العرض الفني §11 is the tiebreak: «القيمة الأصلية (مبلغ الإحالة عند التعاقد)» — the documents themselves treat the two names as one figure, and only one of the two names is free on this screen. Rendering the plate's label verbatim would have put «الإحالة» against two different amounts three rows apart, which is the one thing a contract card may not do. | Resolved |
| P-76 | `02 §4` never fixed the denominator of **الإنجاز المالي**, so EP-CON-02 shipped a `financial-pct` "unavailable + reason" and the overview drew its own bar over the EFFECTIVE value | **الشكل 7 fixes it: كلفة العقد الكلية.** One figure — `money.spentPct` — is the card's headline «22 % من كلفة العقد الكلية» AND «الإنجاز المالي», and المتبقي is `totalCost − disbursed`. | The plate states the denominator in words and then proves it in figures: 112,841,143 ÷ 520,200,000 = 21.69% → the 22% it prints twice, and 520,200,000 − 112,841,143 = 407,358,857, the متبقي it prints. Against the effective value (587,673,564) the same spend reads 19% and 474,832,421 — neither number appears anywhere on the plate. This closes the CONTRACT half of P-44, matching what the الشكل 6 pass already did to the card's المصروف %; the PROJECT bar keeps the effective value, because الشكل 6 labels that one «الصرف من القيمة النافذة». Two bars, two questions, each naming its own denominator on screen. | Resolved |
| P-77 | Local dev could not reach the API **at all**: `environment.ts` points the client at `http://localhost:5080` directly, and the API's CORS list allowed only `:4200` — the one port CLAUDE.md §7 says this project does NOT use | `:4300` and `:4301` added to the dev origins in `Program.cs`; `.claude/launch.json` serves web on **4300**, the port §7 documents, with `autoPort` off. | Every `/api` call from a locally served page failed the preflight, so the whole app rendered «تعذّر تحميل البيانات» — found while verifying الشكل 7 in the browser. `autoPort` made it worse rather than better: when 4300 was taken it silently moved to a random port, which CORS could never allow. The `proxy.conf.json` path is untouched and still points at the deployed API for anyone who wants it. | Resolved |
| P-78 | الشكل 9's payments table has **no status column** — five columns, and a total row that sums every one of them. But only a PAID certificate is spend (P-26), and on the fixture's own contract الإجمالي is 117,925,000 against a المصروف of 76,700,000 | **The plate's five columns, plus the status pill as a sub-line under the payment number** — and the footer strip prints المصروف beside الإجمالي whenever the two differ. | The plate's own contract has three paid payments, so its total and its spend coincide and the question never arises on the screenshot. Ours does not, and a table whose total contradicts نظرة عامة's المصروف by 41 million with nothing on screen to explain it is the failure this codebase is most careful about. Neither figure was bent: الإجمالي is the register's total exactly as الشكل 9 defines it, المصروف is البند الشكل 7's, and the pill is what makes the gap legible. **The pill mapping was broken and this is what found it:** `payment-status` had no entry in `StatusPillComponent`, so مصروف and مصادق عليه both fell through to the neutral pill and rendered IDENTICALLY — on this screen and on the financials certificate panel. Now paid = settled, certified = in motion, pending = held. | Resolved |
| P-79 | الشكل 9 prints payment codes — `PAY-100`, `PAY-101` — and `Payment` carries a sequential `No` and no code column | **«دفعة 1».** No `PAY-` code is invented, and no column is added to carry one. | The plate's codes are illustrative fixture data, not a documented identifier scheme (`06` defines no payment code, and المسار 8 does not ask the user for one). Minting `PAY-100` from `No + 99` would put a fabricated ministry identifier on a legal record to match a screenshot. The number is real, the label names it, and if the ministry does have a code scheme it becomes a column and a fixture change, not a format string. | **CONFIRM** — is there an official payment code? |

| P-80 | الشكل 10 draws **one** «سجل التعديلات التعاقدية» containing the approved-but-unapplied ملحق as a row, under a column headed «قيمة العقد» — the last row of that column reads 597,949,017, which is a value the contract does not have | **One table, as the plate draws it**, with a state pill per row and the pending rows' قيمة/تغيير rendered subdued. The two lists stay two lists in the API (`versions` · `pending`) and in every total. | The alternative — keeping the two separate tables this tab used to have — is safer in isolation and was wrong here: the plate is the binding visual for this screen (the same client instruction that reshaped الشكل 6), a reader comparing screen to document would flag the split, and «الحالة» is a real column that answers exactly the question the split was protecting. What the rule actually forbids is FOLDING the projection into the effective figures, and nothing here does: no column on this table is summed, `ContractRegisterTotals` and `ContractMoney` both exclude the pending deltas, and the section above the table carries the explicit «أوامر معتمدة لم تُطبَّق بعد» warning with the projected value and date. | Resolved |
| P-81 | **The penalty formula in the client documents is not the one we implemented.** الشكل 10 prints a daily penalty of 161,449 on a 587,673,564 contract of 364 days, and العرض الفني §11 states the rule in words: «غرامة اليوم = (قيمة العقد ± تغيّر المبلغ) ÷ (مدة العقد ± تغيّر المدة) × نسبة الغرامة» — i.e. **value ÷ duration × 10%**. `02 §10` (BR-10, D-02) said **value × 0.1% per day, capped at 10%**, which on the same contract gives 587,674 — 3.6× larger | **BR-10 was rewritten to the client formula, and `02 §10` with it.** `Domain/Penalty.For` now takes the contract duration; `Compare` passes the before/after durations `Amendments.Effective` already carries. `PenaltyTests` opens with الشكل 10's own 161,449, and «كيف تُحتسب الغرامة» is now the plate's equation rather than ours. `AppConfiguration.PenaltyPerDayPct` became `PenaltyRatePct = 0.10`. | The two documents are the client's own and they agree with each other; `02 §10` carried its rule flagged `D-02 CONFIRM` from the day of the port and never had a source behind it. Once the client was asked, the written spec was the thing that was wrong, and CLAUDE.md §8's instruction is to fix the spec rather than build around it. **Three consequences worth naming.** (a) The cap now falls after exactly one contract duration of delay instead of after 100 days regardless of duration — `perDay × durationDays = value × 10% = cap` identically — so a short contract exhausts its penalty faster than a long one of equal value. (b) An applied extension can now **lower** the daily penalty, because it raises the denominator as well as the numerator; under the old rule it could only ever rise, and the قبل/بعد table has a case it could not previously show. (c) A contract with no recorded duration charges **zero** a day rather than dividing — an undefined fraction is not a small penalty. Every screen followed with no further edits: الشكل 10's before/after table, SCR-E5's delay days and SCR-W3's penalty section all read `Domain/Penalty` already. | **Resolved on the formula** — open: is the 10% rate fixed, or is الشكل 10's «النطاق القانوني 10%–25%» a range? |
| P-82 | **Q-F5-3 answered, for contracts.** The project activity log deliberately records no per-field diff — «a diff would be an invented one, and it needs new columns». But الشكل 11 asks for one in as many words: «عرض التغيير بصيغة القيمة السابقة مشطوبة ← القيمة الجديدة» | **`ContractActivityEvent` gains `Field` · `Before` · `After` · `Source` · `RefId` · `Note`, and `EP-CON-04` writes ONE ROW PER CHANGED FIELD.** | Q-F5-3's reasoning was that the DOCUMENTS gave a count and no schema, so a diff would be invented — that reasoning does not survive a document that specifies the diff, names the strike-through, and prints a worked example («مبلغ الإحالة 1,349,706,163 ← 1,391,449,653 د.ع بواسطة مهندس مشروع»). Values are stored as the strings the field held rather than as typed columns: a log row is a statement about what a screen said, and one nullable decimal per field type would be null four times out of five. **A save that changes nothing writes nothing** — a row saying an edit happened with no edit in it is noise in the one place that must be auditable. The diff itself is in `ContractEndpoints`, not Domain: it is neither a rule nor arithmetic, and Domain may not see a Features DTO. **الشكل 5's project log can now have the same treatment** and does not yet — `ProjectActivityEvent` is untouched. | Resolved for contracts |
| P-83 | الشكل 11 shows event kinds NOTHING in this build writes: applied change orders («أمر تغييري VO-01 — زيادة كميات أعمال الكهرباء») and progress updates («تحديث الإنجاز المادي إلى 31%»). Both come from other modules | **Seeded in the fixture and marked `system`**, with the `change-order` and `progress` actions rendering as the plate draws them. | The alternative was a timeline with two of the plate's four event kinds and no way to see the other two, on a screen whose whole subject is that everything lands in one place. The fixture already carries amendments, payments and change orders that no UI writes either — this is the same illustrative scenario, and `Fixture.cs` says so at the top. The rows are honest about what they are: `Source = "system"`, attributed to «النظام · حدث آلي», which is exactly how الشكل 11 attributes them. When 5.2 and the progress module write their own events, they write these actions and the screen does not change. | Resolved |
| P-84 | الشكل 12 opens a **بطاقة البند** with six tabs (عام · التخصيص · التوزيع · الإنجاز · الكلفة · السجل). SCR-W4 could EDIT an item, distribute it and assign it — and could not show it: every figure was a column, and nothing gathered them into the item | **The card reads the register row it opens from.** No second request: `BoqRow` already carries all six tabs' figures, and the two tabs with a deeper view of their own (التوزيع · التخصيص) OPEN it rather than restate it. | A `GET …/items/{code}` would have re-derived weights, coverage and progress for a record already on screen and given the card its own chance to disagree with the row beside it — the P-54 failure, one screen down. The card also closes when «تعديل البند» is pressed: the edit happens in the row, and a panel left open beside a row that has become a set of inputs shows one item twice, in two states. **The card is opened by the CODE cell as a button**, not by a row click — the row already carries an inline editor, a delete confirmation and an overflow menu, and a whole-row handler would fire from inside all three (`05 §7`). | Resolved |
| P-85 | الشكل 12 lists three Z6 actions — «إدخال يدوي · استيراد · الجهات المستفيدة» — and none of the three has a code path behind it | **Rendered as demo stubs**, the same treatment تصدير already gets, rather than left off the screen. | Each is blocked on something real, and the honest thing is to say which: **إدخال يدوي** needs a create endpoint (`EP-BOQ-03` updates by code and 404s on an unknown one, so there is no way to add a line); **استيراد** IS الشكل 13, the next screen in the appendix; **الجهات المستفيدة** at project level has no figure of its own anywhere in the document, and the per-item drawer (BR-08) is the only beneficiary UI specified. Leaving them off would have made the toolbar disagree with the document; wiring them to nothing would have been worse. | **Open — إدخال يدوي needs EP-BOQ-09** |
| P-86 | الشكل 13 is an **Excel** import — «تحليل Excel» is one of its five step names — and reading a real `.xlsx` needs a spreadsheet parser this build does not have (an .xlsx is a zip of XML) | **CSV / TSV is parsed natively; `.xlsx` is refused with the fix.** The upload step says: save the sheet as CSV from Excel and upload again. | Three options, and only this one is honest. Adding SheetJS is a dependency decision with a bundle and supply-chain cost that belongs to whoever owns deployment, not to a screen task — and this system may end up on a ministry network where that matters. Hand-rolling a zip+XML reader is ~120 lines of fiddly code (stored vs deflated entries, shared strings, inline strings, date serials) that nothing in the repo could verify without a real workbook to test against. Faking the parse was never an option. **Everything downstream of the parse is real**: mapping, validation, comparison, submission and the version record all work today, so wiring a real .xlsx reader later changes ONE function and no rule. The CSV parser is ~40 readable lines with quoted-field and Arabic-numeral handling, which is why it is inline rather than a dependency. | **CONFIRM — add a spreadsheet library, or is CSV acceptable?** |
| P-87 | المسار 3 step 6 is «تقديم النسخة للاعتماد — لا استبدال للإصدار السابق», and الشكل 13 prints it inside the dialog. Where does a submitted-but-unapproved bill LIVE, given that `BoqItems` is what every weight, link and earned-value figure derives from? | **Two new tables — `BoqImportVersions` · `BoqImportVersionItems` — and `BoqItems` is not written at all.** `EP-BOQ-10` records a version; nothing moves into the register. | A version column on `BoqItems` would have been less schema and one forgotten `Where` away from putting an unapproved bill into BR-01's denominator, into the activity links, and into every earned-value figure on four screens. Here an unapproved import is unreachable BY CONSTRUCTION: no screen that reads the bill reads these tables. **`Amount` IS stored on a version item**, against the never-store-derived rule (§3.5) and deliberately — a version is a record of what someone submitted, and re-deriving qty × rate later would silently restate a signed document. **Steps 7 and 8 («اعتماد الإصدار الجديد» · «تحديث أوزان البنود وقيمة العقد المرجعية») have no screen in the appendix**, so nothing approves a version yet and the table only grows — which is exactly «يحمي البيانات التاريخية: لا يُمحى إصدار سابق». | **Open — approval has no figure yet** |
| P-88 | The comparison needs the bill «القائم» to compare against, and `BoqItems` carries contracted quantities while the register SHOWS effective ones (a re-priced line has a blended rate under BR-05) | **Compared against `BoqEndpoints.Derive`, the same model the register renders.** | Comparing a file against the raw columns would have reported a change on every banded item that nobody made — P-54's failure one screen down, and on the screen whose entire job is to say truthfully what an import would do. | Resolved |
| P-89 | **P-57 is superseded.** It found that award + reserve + supervision out-totalled the contract they belong to, concluded they are not a partition, and split them out of the cost sheet — so الشكل 14's tree could not be drawn | **The three items ARE a partition, and the fixture was the defect.** `Apply()` now derives a new contract's value as award + reserve + supervision, and the fixture's awards were reduced so each contract's three items add to its value. | Three plates say it in figures and none of them was available when P-57 was decided: الشكل 7 prints «مبلغ الإحالة 479,400,000 + الاحتياط 25,500,000 + الإشراف 15,300,000» against «القيمة الأصلية 520,200,000»; الشكل 8 shows the same three; الشكل 14 draws them as children that add to their parent row. `01 §2.3`'s «the awarded value» is the value AWARDED BY the contract, not the award LINE inside it. **P-57 considered and rejected changing the PARENT** — the contract row would have moved to 258,000,000 where four screens say 250,000,000 — and that rejection still stands; what changed is the CHILD. Every contract value on every screen is untouched: only `AwardAmount` moved, from 240,000,000 to 222,000,000 on `CNT-0279`. Verified after the change: Σ components = the contract row on both fixture contracts. | Resolved |
| P-90 | الشكل 14 prints «عند الإنجاز» and «الفرق» on the component rows as well as the contract rows, and its component forecasts sum to the contract's — but by no ratio any document states (award and supervision share 1.0968 while reserve reads 0.914) | **Forecast at CONTRACT level only; components print «—».** | BR-11 forecasts from a CPI, and a CPI needs an earned value: a contract has one (its BOQ progress against its own disbursement), an expense item does not. Reverse-engineering the plate's apportionment would have meant inventing an allocation rule for money on a legal record from three ratios in a screenshot. The contract forecast is real — `EarnedValue.For(revised, planned, physical, disbursed)`, the same rule SCR-W6 and SCR-W1 read — and null until the contract has spent, because a CPI with a zero denominator is not a forecast of zero (P-09). | **CONFIRM — how is a component's forecast apportioned?** |
| P-91 | الشكل 14's note box compares «الكلفة المعدلة المسجّلة» with «إجمالي العقود», and the two are different numbers with different meanings | **Both are sent and both are named**: `projectBudget` is `Projects.PlannedCost` (what المسار 1 recorded), `contractCommitments` is BR-00 over BR-09, and `budgetGap` is signed. | This is the same class of thing as الشكل 6's two denominators: one screen, two questions, and a reader who takes the wrong one is wrong about whether the project can pay for itself. The gap is what the box exists to raise — commitments above budget need a cost revision or a transfer BEFORE the spending stalls. `projectBudget` is nullable and the box says so when المسار 1 recorded no cost: no budget is a state, not a gap of zero (P-09). | Resolved |
| P-92 | **P-56 is closed.** The financials screen carried an `allocation` "unavailable + reason" saying «يتطلب جدول التخصيصات السنوية للوزارة — لا يسجّله هذا النموذج بعد», and الشكل 15 is that tab | **`ProjectAllocations` exists** — one row per project per fiscal year — and only the ALLOCATION is stored. | Three different figures live on this module and none is the others: **الكلفة المعدلة** (the approved cost, `Projects.PlannedCost`), **إجمالي العقود** (what has been committed, BR-00 over BR-09) and **التخصيص السنوي** (what the ministry actually released this year). الشكل 14's note box compares the first two; الشكل 15 is the third, and it is the only one of them that can stop a payment in October. **The spend is NOT stored on the allocation**: a year's المصروف is Σ of the payments whose money moved in it (`Payment.PaidDate`), derived at projection time like every other spend figure — so this tab and جدول الكلف cannot report different figures for one year. Verified on the fixture: 2025 closed at 96% of a 90,000,000 release, and both screens agree its spend was 86,700,000. | Resolved |
| P-93 | الشكل 15's total row needs a consumption % over several years, and the obvious reading — average the per-year percentages — is wrong | **Σ spent ÷ Σ allocated**, never the mean of the rates. | A 100%-consumed year of 12,000,000 and a 10%-consumed year of 300,000,000 do not average to 55% of anything; the mean would report a project as comfortable when it has exhausted a small release and barely touched a large one, or the reverse. Same shape as BR-01's denominator rule and الشكل 6's weighted progress: a ratio of sums, not a sum of ratios. On the fixture the honest figure is 41% (86,700,000 ÷ 210,000,000) where the mean of 0% and 96% would have read 48%. | Resolved |
| P-94 | الشكل 16 shows a payment covering **two contracts** («عقدان») with a declared split, and `Payment` carries exactly one `ContractId` — one row cannot span two | **No schema change: the register groups by the FUNDING LETTER.** A payment already records the official letter it was released against (`Payment.FinanceLetterNo`), and two contracts sharing one letter is one row with «عقدان». | The letter is what the ministry actually files by — `Payment.FinanceLetterNo`'s own note says so, and it is why the column exists rather than an internal certificate number. So the grouping is a reading of data already recorded, not a new parent table with the drift that invites: no id to keep in sync, nothing to orphan, and the per-contract view (الشكل 9) keeps reading exactly the same rows. **The split shown is the apportionment الشكل 9 records on each payment** — one apportionment, two screens, and neither can disagree with the other about what a contract received. The fixture gained one certified certificate on the EM contract sharing `0931/2026` with CNT-0279's third, so the «عقدان» case is visible; certified and not paid, so it moves nothing in المصروف, in الشكل 15's consumed allocation, or in الشكل 14's actual columns (P-26). | Resolved |
| P-95 | الشكل 16's register leads with «الرمز» (`PAY-102`), and a letter that groups several certificates has no single number of its own | **The letter number IS the row's identity**; the الرمز column is dropped. | The same reasoning as P-79, applied one level up: `Payment` has a per-contract sequence and no code, `06` defines no payment-code scheme, and a group spanning two contracts could not carry one of their numbers even if it did. Minting `PAY-102` at project level would be inventing an identifier for a record the system does not issue. The letter number is real, official, and already how the row would be found in a filing cabinet. | Resolved |
| P-96 | **Who registers a payment?** الشكل 16 names the parties on a certificate and nothing in the build recorded one — SCR-W7 read what the finance department had entered and could not originate it | **`EP-FIN-02` and الشكل 20's five-step wizard.** The capacity is **دائرة المهندس المقيم or مدير المشروع** (`Personas.CanRegisterPayment`); «المستخدم المختص» is refused, and the screen prints the reason where the button would be. | A certificate is measured against WORKS ON SITE: the ذرعة is the document behind it, and الشكل 17 puts دائرة المهندس المقيم at the first desk on its route — so the party that measures is the party that raises. The specialist is excluded by the same separation `CanApproveBoqImport` keeps: a party that could both define a contract and raise money against it would make two documents one signature. **What it registers is a `pending` certificate and its audit route, not a payment.** المسار 8's review, certification and disbursement are decisions with owners and dates that no screen asks for, and P-26's rule — المصروف counts PAID only — is unmoved by this endpoint. The wizard requires the finance letter AND at least one ذرعة, which is الشكل 20's own stated purpose («فيمنع تسجيل دفعة ناقصة المستندات») rather than a validation bolted on afterwards; the server enforces both, plus that the split across the three expense items equals the net. **P-79 stays open** — no payment code is invented, and «دفعة N» is still the sequential number on the contract. | Resolved |
| P-97 | **P-56 is fully closed.** Its second gap — «يتطلب تواريخ مراحل تدقيق المستخلص — لا تسجّلها جداول الدفعات بعد» — was الشكل 17 | **`PaymentAuditStages`: one row per DESK a certificate sits at**, recording four facts (which desk, its cap, when it received the file, when it let it go) and deriving nothing. | A column pair per desk would have fixed the route in the schema and needed a migration the first time a ministry adds a stage; a row per stage makes the route DATA, which is what it is — the appendix names two desks «by way of example» and never claims the list is closed. **The arithmetic is BR-12's** (`Domain/SlaLeadTime`), measured against the project's data date and never a clock (D-06): a finished stage is measured to its own finish, the one holding the file to today-as-the-project-knows-it. **`CapDays` is per stage, not `SlaLeadTime.SlaDaysPerStage`** — the plate shows 7 where D-03 assumes 5, and a cap that varies by desk is what an instruction changes without changing code. With both gaps closed, `EP-FIN-01`'s `unavailable` list is now EMPTY. | Resolved |
| P-98 | الشكل 17 prints «الموعد القانوني للصرف», and the obvious derivation — certified date + Σ stage caps — does not reproduce the plate's own date | **Recorded on the payment (`Payment.LegalDueDate`), not derived.** | The plate's certificate is certified 2026-07-09 with caps of 7 and 7, which would give 2026-07-23, and it prints 2026-08-12. So the date comes from the instruction governing the certificate, not from the desks it passes. Deriving it would have invented a rule AND made a date the ministry can be held to move whenever someone edited a cap. Null when none is set, and the box simply does not render. | Resolved |
| P-99 | «السلفة الجارية» — which certificate does this screen watch? | **The one certified and not yet paid**, most recently certified first. | A paid certificate has no lead time left to watch and a `pending` one has not entered the route; the certified-not-paid state IS the transaction in flight, and it is the one P-26 already keeps separate everywhere else. When there is none, the tab says so rather than drawing empty stage cards (04 §9). | Resolved |
| P-100 | **The fixture's stage chains were not `03 §2`'s.** They ran five stages named «دراسة دائرة المهندس المقيم · لجنة أوامر الغيار · لجنة تثبيت الأسعار · لجنة المراجعة المصادقة · المستوى الإداري الأعلى» — two of which are not stages at all | **The six documented stages, taken from `Domain/WorkflowMachine.Stages` rather than typed into the fixture.** لجنة المراجعة المصادقة becomes what `03 §3` says it is: an EXTERNAL PARTY inside stage 4. المستوى الإداري الأعلى keeps its persona and owns nothing. | `03 §2` and الشكل 33 agree on the six names, their owners and their order — «دراسة الطلب · لجنة أوامر الغيار · تثبيت الأسعار · المصادقة والتخصيص · الأمر الوزاري وملحق العقد · التنفيذ» — and الشكل 29 prints «الأمر الوزاري وملحق العقد» in the register's own stage column. The invented chain put the wrong party in «الجهة المسؤولة» on a screen whose whole job is naming who holds the file, and it had no execution stage, so «معتمد ≠ مطبَّق» had no row to live in. Seeding from the domain table means a stage cannot be named one thing and rendered another. **Two consequences the register shows:** stage 6 التنفيذ is owned by دائرة المهندس المقيم, so the RE department — not the committee — is now `awaiting` on VO-04 and VO-05; and the per-stage ceilings become الشكل 33's 3 · 5 · 7 · 10 · 14 · 7 instead of a flat 5, which is why D-03's flat figure is a DEFAULT and not the rule. | Resolved |
| P-101 | `03 §6` lists **seven** application steps; الشكل 30 prints **nine** and names them | **Nine, each carrying its `03 §6` number where it has one.** `WorkflowMachine.ApplyStep` gained `SpecStep`, null on the two the plate adds. | The two extra steps are not decoration: **إصدار ملحق العقد** is BR-09's amendment row, which is the thing that makes an approved figure effective — without it "approved ≠ applied" has no step that separates them — and **إعادة احتساب الغرامات التأخيرية** is BR-10's baseline moving with an approved extension. Both are operations this system genuinely performs. Rendering seven would have made the screen disagree with a plate that enumerates nine; renumbering the spec's seven would have broken every `03 §6` citation. Two numbers on one step costs nothing and keeps both readings greppable. | Resolved |
| P-102 | الشكل 32 prints THREE day counts — 30 requested, **21 from the analysis**, 21 approved — and the analysis figure is neither the sum of the affected activities' days (9 + 6) nor their maximum | **`ChangeOrder.AnalysisDays` — stored on the ORDER, not derived from its activities.** | An activity extension absorbed by float moves the project by less than itself, and two extensions on one path move it by more than either: the project-level effect is what the schedule analysis CONCLUDES, and no arithmetic over the activity rows reproduces it. Deriving it by `Max()` (which the first cut did) would have printed a number the schedule analysis never produced, on the screen whose standing note exists to say exactly that — «مدة النشاط ليست مدة المشروع». | Resolved |
| P-103 | `03 §1` records two letters that PRECEDE entry — the contractor's request and the consultant's opinion, «each with an official letter number and date» — and `ChangeOrder` stored neither | **Four columns: `ContractorLetterNo/Date` and `ConsultantLetterNo/Date`**, rendered in الشكل 30's «مدخلات سابقة لإدخال الأمر». | The first cut derived them from the order's own `IncomingNo` (`IN-3312/2025`, `IN-3312/2025/1`), which put ONE letter's number on THREE different documents in a legal record — الشكل 30 prints three distinct numbers precisely because they are three distinct letters. **VO-06 has no consultant letter and the section shows one input**: a supply order's technical opinion comes from the inspection side, and inventing a second row to keep the layout symmetrical would be inventing a document. | Resolved |
| P-104 | Which amendment did an order produce? `EP-CHG-02` first matched on the pair (delta value, delta days) | **`ContractAmendment.SourceChangeOrderId`** — the column already existed and nothing was setting it; the fixture now stamps it. | Two orders on one contract moving the same figures is not unusual, and the pair-match would then have paired an order with somebody else's amendment — on the section that states whether the contract has actually changed. The fixture stamps amendment 1 ← VO-01 (applied) and amendment 2 ← VO-05 (approved, waiting), which is the pair «معتمد ≠ مطبَّق» is demonstrated with; VO-05's approved days moved 0 → 12 and its requested value 3,400,000 → 3,375,000 so that the order and its pending amendment carry ONE set of figures, not two. | Resolved |
| P-105 | VO-04's seeded weight-recalculation failure said «مجموع الأوزان 99.94% — لا يبلغ 100.00%», and the record's own weight panel recomputes the same figures and prints **100.00% · مطابق** | **The failure message says what failed to RUN, not a sum the screen contradicts two tabs away.** | VO-04 moves 500 م² between two lines at the same rate, so the contract value does not move and BR-01 still totals 100.00% — the seeded sum was fiction the moment the panel started recomputing instead of reading a stored number. `03 §6` asks for a failed step, not a wrong total: the step now fails because the recalculation could not complete against a re-numbered activity link, which keeps فشل التطبيق real on the register and the weight panel honest. | Resolved |

| P-106 | The reference wizard computes the 20% split, the revised quantities and the impacts **in the browser** (`vo-wizard.jsx` `bOne` / `bCalc`), and الشكل 39 recalculates as the two proposals are typed | **`EP-WIZ-02`, a preview endpoint called on a 300 ms debounce.** The wizard holds the draft; every figure on it is computed by `Domain/ChangeOrderRecord` — the same function the RECORD page reads. | CLAUDE.md §3.1 admits no exception for "it is only a preview": a number a user approves on the strength of is a number the rule must have produced. The alternative — porting `bOne` to TypeScript — would have created a SECOND implementation of BR-05 that agrees with the first only as long as both are maintained, and the divergence would surface as a wizard that promised one impact and a record that showed another. **What the round trip buys:** what a user saw when they pressed «إرسال للمراجعة» is what the record shows afterwards, by construction. The cost is one request per pause in typing, against a local database. | Resolved |
| P-107 | `ChangeOrder.No` is documented as unique **within the contract**, and the record's URL is project-scoped (`…/projects/{id}/changeorders/VO-06`). The first wizard build minted VO-06 on CNT-0279 while VO-06 already existed on CNT-0279-EM | **The next free number is taken across the PROJECT.** Still unique per contract — that rule is a weaker claim — and now addressable. | Caught by submitting through the wizard and finding two rows in one register both labelled VO-06, one of which the record page could not open. The alternative was a contract segment in the URL, which the plates do not show and which makes a change-order reference unpasteable into a letter. `06 §12`'s own fixture already numbers VO-01…VO-06 across two contracts of one project, so this is what the documented dataset assumes. | Resolved |
| P-108 | `03 §8` collects no TITLE, and the register's «الأمر التغييري» column shows one. الشكل 42 prints the justification where a title would be | **The title is the justification's first line**, capped at 120 characters, falling back to the type when it is empty. | Inventing a title field the document does not ask for would put text on a legal record that nobody was asked to write; leaving the column blank would break the register a phase earlier. The first line of «الأسباب الموجبة» is what a reader writes there anyway, and it is a value the user actually authored. | Resolved |
| P-109 | Does a DRAFT enter the approval chain? | **No.** `kind=draft` writes the order and its lines and NO `ChangeOrderStage` rows; `kind=submit` plans all six through BR-13 and opens the first applicable one. | A draft has not been referred to anybody, so a chain would put it in somebody's «بانتظار إجرائي» with a clock already running (BR-12 measures from the incoming letter, and the register's «متأخر» would start counting against a document nobody has seen). It also keeps `03 §6`'s lifecycle honest: `draft` is before `pending`, not a quiet version of it. **The gates are not skipped for a draft either** — they are simply not blocking: `02 §7` blocks SUBMISSION, and a draft is where an incomplete order is supposed to live. | Resolved |
| P-110 | الشكل 58 draws the supply variant of step 2 — redistributing a تجهيز item **between beneficiaries** (ITM-002), with a from→to→quantity table per transfer | **The type card is there and the supply flow is scoped to it; the beneficiary-level redistribution table is NOT built.** A supply order can still move quantity BOQ-line to BOQ-line. | الفقرات التجهيزية is its own module (الأشكال 50–56) and this build has no supply-item register, no beneficiary allocations per supply item and no receipt records — so the from→to picker would have nothing real to pick from and the balance check nothing to check against. What IS honoured is the rule that comes with it: a supply order has no 20% tier and no rate-fixing stage (`02 §5`), and step 2 says so in its own explainer instead of showing a limit that does not apply. | **CLOSED** — الأشكال 50–56 built (Phase 10); الشكل 58’s beneficiary redistribution is Phase 11 |

| P-111 | Where does the PENDING amendment come from? `02 §9`'s projection is rendered from `ContractAmendments` rows with a null `AppliedAt`, and until 5.4 nothing wrote one — the fixture seeded VO-05's by hand | **Approval creates it (`EP-WFL-01`), applying FLIPS it (`EP-WFL-03`).** One row per order, never two. | The first cut had apply INSERT an amendment, which on VO-05 — whose pending row the fixture already carried — produced two rows numbered 2 on one contract, and the record page then read the wrong one. Creating it at approval is also what makes the projection true the moment it becomes true: SCR-E3, SCR-W1 and SCR-W3 show «أوامر معتمدة لم تُطبَّق بعد» from these rows, and before this an approved order was invisible to all three until somebody seeded a row for it. | Resolved |
| P-112 | `03 §6` calls the weight recalculation failable. What does a failure DO — mark the step and carry on, or stop? | **Nothing is written at all.** `Domain/ChangeOrderApply.Plan` computes every change as a value first; if BR-01 does not total 100.00% the endpoint records the nine steps with step 5 `fail`, leaves the order in `applied_partial`, and returns 422 having touched no contract, no BOQ line and no date. | A half-applied contract is the worst outcome available here: the value moved and the quantities did not, or the other way round, with no single row to say which. Computing the plan as a value before writing anything is what makes "all or nothing" true rather than intended — and it is why the steps after 5 stay `todo` instead of `na`, because they did not run rather than not applying. | Resolved |
| P-113 | Who may CANCEL an order (`03 §5`'s fourth decision)? D-04 says the two stage-4 external parties, and they are not system users | **Only the delegate**, and only as the recorded outcome of a party that carries `CanCancel`. `EP-WFL-01` refuses `cancel` from anyone who is not a delegate; `EP-WFL-02` ends the order when such a party's outcome is recorded as «أُعيد». | The decision belongs to a party that cannot log in, so the only honest path is the one `03 §4` already defines for every other external outcome: the delegate records it against an official letter, and the attribution keeps both names. Offering «إلغاء الموضوع» to a stage owner would have handed a committee an authority the document gives to the finance directorate and the endorsement committee. | Resolved |
| P-114 | A RETURN moves the order back — but back to what state, and what happens to the stage that returned it? | **The returning stage keeps its `returned` status and its decision; the stage it goes back to reopens with a fresh clock.** `resubmit` then reopens the first applicable stage and clears the return. | `03 §5` requires full history and prior versions preserved, and the natural implementation — resetting the returning stage to `pending` — erases the fact that a return happened at all. Keeping it is what lets الشكل 33 draw VO-03's المصادقة والتخصيص as `returned` with its own comment while stage 1 is active again, which is exactly what the plate shows. | Resolved |
| P-115 | The record page decides which actions to render, and the endpoints decide whether to accept them. Two lists of the same rule | **Both read `Domain/WorkflowMachine.Available`** — the endpoint calls it directly, the page mirrors it in one `computed`. The page hiding a control is COURTESY; the endpoint refusing the call is the rule (`03 §7`). | The alternative — shipping the available set in the record payload — was rejected for one reason: it makes the browser's list authoritative-looking, and a client that edited it would get a 403 rather than a screen that quietly disagrees. Mirroring costs eight lines in one `computed` and is checked by the same tests that check the endpoint. **The gate is verified live**: `user.univ-specialist` posting a decision on VO-02 gets 403 with no control ever rendered. | Resolved |

| P-116 | Is «متأخر» on الشكل 45's actions register a STATUS or a derivation? The first cut derived it from the due date against the project data date, the way BR-12 derives an SLA breach | **A stored value.** `action-status` carries مفتوح · قيد التنفيذ · متأخر · مغلق, and nothing computes lateness on this screen. | الشكل 45 refutes the derivation on its own face: ACT-02 is due 2026-05-10 against a data date of 2026-08-11 and reads «قيد التنفيذ», while ACT-01 (due 2026-04-25) reads «متأخر». Both are past due; only one is marked. So this register is maintained by whoever keeps the minutes, and lateness on it is their judgement — unlike a change-order stage or a payment desk, where the system owns the clock and BR-12 measures it. **Caught by building the derivation first and watching it label a row the plate does not label.** | Resolved |

| P-117 | الشكل 43's risk register has a THREE-level probability and impact; the reference component `DModRisk` draws the familiar 5×5 matrix. Which one is the screen? | **الشكل 43.** `Risk.Probability` and `Risk.Impact` are 1–3, and `Domain/RiskSeverity` scores them p×i: ≤2 منخفضة, ≤4 متوسطة, else عالية. `Bands()` returns all three every time, even at zero. | The plate is the client-validated document and it prints its own rule on the screen title — «الخطورة = الاحتمالية × التأثير» — with every one of its seven rows scored عالي/متوسط/منخفض on both. A 5×5 would need two levels the client never named, and every row of الشكل 43 would have to be re-scored to fill them. `Bands()` returning empty bands is the same rule as the «مرفوض 0» chip on الشكل 46: a band that disappears when it empties reads as a changed scale rather than a zero. | Resolved |
| P-118 | الشكل 46's document panel draws four tabs — المعاينة · المراجعات · التأشيرات · التفاصيل — and two of them have nothing behind them: no file bytes are stored and no flow records a stamp | **Two selectable tabs, and the other two NAMED in a notice inside التفاصيل.** `panelTab` is `'revisions' | 'details'`; the notice says what المعاينة and التأشيرات would show and why they are not enabled here. | `04 §9` wants an empty state to say which of the two empties it is. A tab that opens onto nothing says neither — it reads as a broken screen rather than an unbuilt one, and it is also the one of the four a reviewer would click first. Naming them keeps the plate's four features visible on the screen while being honest that this prototype stores no file contents. | Resolved |

| P-119 | الشكل 47's twelve rules each carry a «شرط الإطلاق» — «انزياح ≥ 5 أيام», «الصرف ≥ 90%». Is that an expression the system evaluates, or text? | **Text, and the screen says so.** `AlertRule.TriggerAr/En` is prose; no scheduler evaluates it, no channel dispatches, and the alerts are recorded rows that NAME the rule they would have come from. A notice under the table states this. | Storing a parsed expression would claim an engine that does not exist — `07 §2` lists delivery as POC work — and the first reader to switch a rule on and wait for an alert would find out the hard way. What the plate actually promises IS implemented: «إيقاف قاعدة يوقف التنبيهات التي أنتجتها فورًا» is `Domain/AlertInbox.Live`, and the footer count moves with the switch. Naming the gap keeps the twelve rules readable as the specification they are, which is what the client validated. | Resolved |

| P-120 | `07 §8` says «keep the tab, stub the viewer» for SCR-W10. Which half of الشكل 44 is the tab, and what does a stubbed viewer look like? | **Everything that carries data is built; the scene is a placeholder that names itself.** The version record, the discipline filters, the tree, the element panel, its links and the colour key are real. The floating toolbar (قياس · مقطع · لقطة · تدوير) is NOT drawn, and the version list is a RECORD rather than a selector. | A stub is honest or it is a lie with a spinner. Drawing measure and section tools over an empty viewport is P-118's defect — a control that opens onto nothing — and a version *selector* would be worse: elements belong to the project, not to a version, so picking «الإصدار 2» could only show today's elements under yesterday's label. What the plate is actually FOR survives intact: «روابط العنصر تربط النموذج ببنود حساب الكميات وبأنشطة الجدول الزمني», and those links are real joins that navigate. | Resolved |
| P-121 | الشكل 44 orders its disciplines إنشائي · ميكانيكي · كهربائي; الشكل 46's folders run معماري · إنشائي · كهربائي · ميكانيكي. Two plates, two orders, one lookup | **The `doc-discipline` lookup's `Sort` wins on both screens.** الشكل 46's order is the lookup's, and SCR-W10 follows it. | They are the same six disciplines and one list of them, so one order. Giving the model its own would mean two places the sequence is decided, and they drift the first time somebody adds a discipline to one of them. The plates' disagreement is almost certainly incidental — الشكل 44 shows only three of the six — and neither order carries meaning the other lacks. | Resolved |

| P-122 | SCR-W15 needs a project audit trail. `Data/Entities/AuditEvent.cs` was the documented starting point for one — wire it in, or read what already exists? | **Neither: delete it and UNION the three logs the system already keeps.** `ProjectActivityEvents` · `ContractActivityEvents` · `ChangeOrderAuditEntries` are written beside the records they describe; `EP-AUD-01` reads all three and stores nothing. | An audit table would have to be written by every endpoint that already writes one of the three, which is a second answer to «من غيّر هذا الحقل ومتى» — and the copy drifts the first time one of the three gains a column. Reading the originals also means this screen CANNOT disagree with the tab that owns the record: SCR-W3's سجل النشاط and this screen render the same rows. The starting point had no writer in any phase, so nothing was lost by removing it. RPT-11's `Reads` moved to the three real tables in the same change. | Resolved |
| P-123 | SCR-W14 is a project-level reports tab and SCR-E7 is the enterprise one. Two catalogues, or one? | **One `ReportCatalog`, two questions.** SCR-E7 asks whether a report can be produced at all (is the table registered); SCR-W14 asks whether it can be produced for THIS project (does the project have rows). Only `project`-scope rows appear in the tab. | A report definition is a capability of the system and there is exactly one list of them; a second list for the project tab would drift the first time a report was added to one. The two questions genuinely differ and the project one is sharper — RPT-09 «الأوامر التغييرية» is available ministry-wide the moment the table exists and still has nothing to print on a project with no change order. Counting rows per source also makes the unavailable message specific: the row names the empty source instead of greying out a button (04 §9). | Resolved |

| P-124 | The `/docs` page shows what the spec says a rule returns and what the function actually returned. Should it also show a pass/fail mark? | **No mark.** The two are rendered side by side and the reader compares them; a short note says why there is no tick. | `Expect` is prose from `02-BUSINESS-RULES.md` («61 days × 100,000 = 6,100,000 (below the 10,000,000 cap)») and `Result` is a JSON value. Nothing in the code can compare them, so any tick would be hard-coded — and it would be the single claim on a page whose whole purpose is that its claims are verified on every load. A green tick nobody checks is worse than no tick, on this page most of all. | Resolved |

| P-125 | SCR-W10 stored the model tree's root as one string, `Building = "مبنى A"`. The bilingual pass found it rendering Arabic at the top of an English page | **`BuildingAr` / `BuildingEn`, a pair like every other name in the system.** | One string cannot be two languages, and nothing about a building's name makes it the exception — `NameAr`/`NameEn`, `TitleAr`/`TitleEn`, `LabelAr`/`LabelEn` are the pattern everywhere else, including on the same entity. It slipped through because الشكل 44 is an Arabic plate and the screen was built and verified in Arabic; the defect was invisible until the language was switched, which is exactly what a bilingual pass is for. | Resolved |

| P-126 | «العرض بصفة» lived in the shell's account menu, one click from anywhere. Is it an app-wide identity control or a screen's control? | **A control on TWO screens.** The appendix names «مبدّل «العرض بصفة»» exactly three times and every one is a change-order screen — الشكل 29's filter bar, الشكل 30's header, and الشكل 37 describing the register behind the wizard. It now sits in both, as `<epm-persona-switcher />`; the account menu STATES the capacity in effect and says where it is changed. | `03 §7` says what the switcher is for: reviewing the change-order permission model, which is the one place a record shows a different face to a different صفة (BR-14). In the account menu it read as "who am I" — and it is not that. There is one user; every row is a صفة that one person acts in. On الشكل 29's filter bar it reads as what it is: a filter that changes which orders are «بانتظار إجرائي». **The consequence is real and accepted:** BR-15's workspace scope also follows the capacity, so switching on a change-order screen changes which projects are visible everywhere. That is one session identity behaving like one session identity, which is what `03 §7` says the POC will have. | Resolved |
| P-127 | `PersonaService` and the projects page's fixture button called a BARE relative `/api/…`, bypassing `environment.apiUrl` | **Both build the absolute URL from the same environment config `Api` reads.** `PersonaService` cannot go through `Api` — that service injects it for the X-Epm-User header and the two would be a cycle — so it reads the config directly. | The dev server's `proxy.conf.json` points `/api` at the DEPLOYED api, not at localhost:5080. So the persona list was answered by production and came back 500: `all()` stayed empty, `current()` returned null, and every consumer fell back to an empty string. The account menu rendered a blank name and nobody noticed, because a blank name looks like a layout choice. It surfaced the moment «العرض بصفة» needed the LIST rather than the name (P-126) and had nothing to offer. Two clients out of ~40 had this bug; the other 38 all go through `Api`, which is the argument for having `Api`. | Resolved |
| P-128 | الشكل 29 lists four filters — «بانتظار إجرائي، تجاوزت السقف الزمني، متأخرة، **المرحلة**» — plus «**مرشح نوع الأمر**»; الشكل 30 lists «**منتقي الأمر**». None of the three existed | **All three built.** Stage and type are client-side filters over data the register already holds (`currentStageNo`, `type`); the picker is a new `Siblings` list on `EP-CHG-02`. | They were named in the plates' «الوظائف والخصائص الظاهرة» and were missed when SCR-W8 was built — the screen was checked against its columns and its figures, not against its control list. Both filters offer only the stages and types actually PRESENT on the project's orders: a filter onto an empty result is not a filter. The picker navigates rather than swapping a signal, so the back button walks the orders a reader actually opened. | Resolved |

| P-129 | الشكل 47 names «زر ضبط قواعد التنبيه» and the editor behind it is not built — severity, channels, recurrence and the escalation ceiling are fixture data. Omit the button, or show one that does nothing? | **Show it, and let it say what it is.** The button is present and toasts «تجريبي», beside the notice that already states the conditions are recorded and not evaluated (P-119). | A control the plate names and the screen omits is a silent divergence: a reviewer walking الشكل 47 against the build finds it missing and cannot tell whether it was forgotten or refused. A control that names itself as a stub answers that question on the screen. It is the same call P-118 made for المعاينة and التأشيرات, and the opposite of the one P-120 made for the 3D toolbar — the difference is that this button has ONE obvious meaning and a viewer cannot mistake a toast for a saved rule, whereas measure and section tools over an empty viewport look like a broken viewer rather than an absent one. | Resolved |

| P-130 | SCR-W1 was built from the v1.1 reference `DModOverview` and not from **ملحق الشكل 4**. The plate names two panels; the screen had a contracts register and a beneficiaries list it does not name, and was missing five identity fields, four paired figures, both charts, the alerts button and actionable alert cards | **Rebuilt to the plate, and the two extra panels removed.** Contracts are الشكل 6's own screen; beneficiaries are on الشكل 5 and in the BOQ distribution. | A reader who walks الشكل 4 against this screen has to be able to tick every line. Two panels the plate never draws is not a bonus — on the FIRST screen of a project it is a claim about what matters, made by the build rather than by the client. And both were drawing the same records a dedicated screen already owns, which is where they disagree first. Nothing was lost: every figure the removed panels carried is still one click away, on the screen whose subject it is. |
| P-131 | «خط سير المراحل» reads each module's state from that module's own table (§79). Seven of the fifteen were still hard-coded `built: false, rows: 0` — every Phase 6 module | **Counted from their own tables**: risks, model elements, meetings and open actions, documents and drafts, alerts, and the audit trail's three logs. | The strip was reporting that this project had no risks, no documents and no alerts on a project with seven, fourteen and eight of them. It went stale the moment Phase 6 built those screens and nothing pointed at it, because the list is a literal in the endpoint rather than a query — the exact failure mode `§79`'s sentence exists to prevent. The counter moved from 6/8 to 13/14 as a result. |
| P-132 | الشكل 4 asks for «مخططان بمفاتيح سلاسل» and names neither. What are the two charts? | **Progress (actual vs planned, over time) and cost (approved vs revised vs spent).** They are the two figure groups the plate's own data list carries. | «الإنجاز المادي 31% مقابل مخطط 39%» and «المقررة 1,374 م والمعدلة 1,500 م … والمصروف 510 م» are the only two things الشكل 4 states twice — once as a figure and once as a comparison — and a comparison is what a chart is for. The progress chart plots only the dates a progress update was RECORDED and does not interpolate between them (`Domain/ProgressSeries`); its planned line is dashed because it is derived from the baselines while the actual line was observed. A smooth curve through dates nobody measured is the reference's own defect — its S-curve is `f*f*(3-2*f)` — and this screen already refuses four other fabricated figures for the same reason (P-09). |

| P-133 | SCR-W1 was rebuilt to الشكل 4's CONTENT (P-130) using generic components — a summary strip, a bar chart, a dual line. Then the client named **the live prototype** as the design authority, and its `DModOverview` uses `.d-verdict`, `DSCurve` and `.d-alertfocus` | **Rebuilt again, to the prototype's structure.** Two `.d-verdict` blocks each pairing an S-curve with a verdict panel; the next-action button INSIDE `.sb-head`; `.d-alertfocus` with severity share bars and a per-module action verb on every card. | Every one of those classes was already in `web/src/styles/desktop.css` — `.d-verdict` has 35 rules there and `.d-alertfocus` has 46 — and I reached for generic components anyway. That is exactly what CLAUDE.md §1 warns about, and exactly the PAGE-01 story it tells: *"the written spec gives you the rules; the reference component gives you the screen."* I had the plate and used it for content, and never opened the component. |
| P-134 | The prototype's S-curve is `f => f * f * (3 - 2 * f)` over twelve invented months, and its indices divide one made-up figure by another. Port the picture, or the data with it? | **The picture only.** `Domain/ProgressSeries.Monthly` fills it from month ends between the earliest baseline and the data date: the planned line is `PlannedProgress` (derived), the actual line is the progress updates somebody RECORDED in the contract log, and the cost curve's actual line is payments on or before each month end. | This screen already refuses four fabricated figures (P-09) and the curve is the fifth. The honest version reads differently and better: the actual line does not start until month 14, because that is when the first update was logged, and it goes FLAT across June and July because nothing was recorded then. A smoothstep would have drawn a confident rise through two months nobody measured. The shape is the prototype's; the claim is the log's. |
| P-135 | The overview's stage strip showed all fifteen modules and counted 13/14 | **Eight** — معلومات المشروع · العقود · جدول الكميات · الموقف المالي · الجدول الزمني · الإنجاز · الأوامر التغييرية · إدارة المخاطر. | الشكل 4 lists exactly those eight and the prototype's own `STAGE_IDS` is the same eight. The other seven modules still count themselves (P-131) — they are just not what this strip reports on. The counter now reads 8/8 over the eight, which is the plate's «4/8» read honestly. |
| P-136 | SCR-E1 opened with a panel titled «مؤشرات غير متوفرة بعد» carrying four tiles — physical %, financial %, SPI, CPI — each saying the figure could not be derived. **All four became derivable in Phase 4.4** and the screen went on saying otherwise for three phases | **The four are real now**, from the same `BoqEndpoints.Derive`, `PlannedProgress`, `EarnedValue` and payments the project screens read. `unavailable` survives, but it is now COMPUTED per figure and rendered inside the tile it belongs to, not in a panel at the bottom. | This is P-131 again at portfolio scale: a literal list of what is missing, written once and never re-derived, goes stale the moment somebody builds the missing thing. A figure that CAN be derived and is reported absent is worse than one that cannot — it teaches an executive to stop looking at the one screen they open. P-09 forbids rendering 0 for a missing input; it never asked for a figure to be withheld once its input arrived. |
| P-137 | SCR-E1 was built from the checked-in v1.1 `DDashboard` and reached for generic components. The live prototype's `DDashboard` is a two-row `.d-dash` — S-curve + three `DStat` tiles, cost curve + CPI tile + «المؤشر التنفيذي» — then a watchlist table, four paired panels and «معالم قادمة» | **Rebuilt to the prototype's structure**, class for class: `.d-toolbar` chips, `.d-dash` / `.d-dash-side`, `epm-scurve`, `.d-tl-band.col`, `.d-tablewrap` watchlist, `.d-grid.c2.eqrows`, `.d-mini` milestones. | Same story as P-133 one level up, and the same lesson: every class was already in `desktop.css`. The one substitution is the fourth breakdown panel — the prototype draws a five-row mini gantt with no CSS in this app, so it stays «القيمة النافذة حسب مساحة العمل», which is real portfolio data and needs no new chart. |
| P-138 | «المؤشر التنفيذي» needs a red/amber/green per project. The prototype has `EPM.execSignal`: `completed` → green; `stalled` or delay > 20% of duration → red; delay > 5% or SPI < 0.90 → amber | **Ported verbatim** into `Domain/ExecutiveSignal`, thresholds named as constants. | Unlike the prototype's curves and indices, every input this rule needs is one the system already derives — `Penalty.DelayDays` (BR-10), the contract's own duration, `EarnedValue.Spi` (BR-11). The thresholds themselves are numbers somebody chose and `02` defines none of them, so they are constants and say so, exactly as الشكل 4's «الحد المقبول 0.95» is (P-136 in SCR-W1). Two rules were written into the port that the prototype does not state: a MISSING delay cannot fire the delay term (no forecast is not "on time"), and a MISSING SPI cannot make a project amber — "unknown" and "behind" are different claims. |
| P-139 | The portfolio's two toolbar filters — status chips and entity type. The prototype filters its own array and recomputes every figure in the browser | **Query parameters on `EP-PRT-01`.** The server re-derives the whole band over what survives them. | Angular computes nothing (CLAUDE.md §3.1), and a headline derived over the whole portfolio sitting above a table filtered to part of it would be two different portfolios on one screen. They are in the URL rather than in a signal for a second reason: «متعثّرة · جامعات حكومية» is a view somebody sends to somebody else. |
| P-140 | With no activity baselines and no recorded progress in scope, `ProgressSeries.Monthly` still returns one row per month — all zeros — and the S-curve drew a flat line along the axis | **The endpoint returns an EMPTY curve** unless there is a baseline or a recorded update (and, for the cost curve, a baseline or a paid payment). The panel then renders its own empty state naming what the curve is drawn from. | A flat line on the axis reads as "nothing has happened". The truth is "nothing has been recorded", and those are different facts about a project. Found by filtering the live screen to «متأخر», which leaves one project with no BOQ and no schedule — the case the fixture never shows unfiltered. |
| P-141 | SCR-E8 said in its own header that it "shares arithmetic with SCR-E1 and nothing else", and built a four-tile band over a two-column row. The live prototype's `DWorkspaceOverview` says the opposite in ITS header — *"same model as the ministry board"* — and draws `DDashboard` scoped to one workspace | **Rebuilt to the prototype**, and the derivation extracted into `Domain/PortfolioBand` so both endpoints load rows and call ONE rule. | Two endpoints deriving the same band is not duplication that stays harmless: the ministry total would eventually stop being the sum of the workspaces underneath it, and the first person to notice would be a director whose university's figure does not appear in the minister's. It also puts the weighted planned roll-up where CLAUDE.md §3.1 requires — `PortfolioEndpoints` was computing several weights inline, which an endpoint may not do. 14 tests. |
| P-142 | SCR-E8's watchlist membership was the STORED status — `delayed` or `suspended` — with a note saying lateness could not be derived because BR-11 needed a baseline the system did not have | **`Domain/ExecutiveSignal`**, the same rule SCR-E1 uses: status, BR-10's delay as a share of the baseline duration, and BR-11's schedule index. | The note was true when written and stopped being true in Phase 4.4 — the third instance of the same staleness this week (P-131, P-136). Reading a watchlist off the stored status is the "indicator entered rather than derived" that `§9 «الاشتقاق لا الإدخال»` rules out, and it is precisely what the note said it was avoiding. A project marked `ongoing` that is 90 days past its forecast was invisible to it. |
| P-143 | SCR-E8's status chips carry counts. With a BRANCH selected, should a chip count the branch or the whole workspace? | **The branch.** The chip ignores only the STATUS filter. | A chip has to keep counting its own subject — selecting «مستمر» must not zero the «مستمر» chip — but that is the only reason to ignore a filter. «مستمر 2» above a branch that holds one project is a count of a scope nobody is looking at. The prototype ignores both filters here; this is the one place it was not followed, and the reason is that its own count would contradict the table underneath it. |
| P-144 | **CLIENT DECISION.** A chart or a card with no data was being rendered anyway — an empty-state panel for a chart, an "unavailable + reason" tile for a figure. The client: *"if a graph or card has no data no need to show it hide it"* | **Hidden.** The chart panel does not render; the tile does not render; a `.d-dash` / `.d-verdict` row whose chart is gone collapses to `.solo` and gives the row to what is left; a row with nothing left does not render at all. Applied to SCR-E1, SCR-E8 and SCR-W1 together, so one convention holds across portfolio → workspace → project. | This narrows P-09, which it does not repeal: P-09 forbids rendering **0** for a missing input, and that still holds everywhere — a hidden tile is not a tile reading zero. What changes is the fallback: "unavailable + reason" was the answer for three phases and the client finds it noise on a board they read at a glance.<br><br>**The cost, stated plainly:** two workspaces side by side can now carry different tile sets, and a director comparing them cannot tell *"we do not measure this"* from *"this is fine"*. The reasons are still on every response as `unavailable[]` — the audit trail moved to the API, it did not disappear — but nobody reading the screen sees them. If that bites, the fix is one quiet line under the board listing what was hidden and why, not a return to the tiles. |
| P-145 | Three things were deliberately NOT hidden | **SCREEN-level empty states**, the **watchlist** when nothing is off track, and **table CELLS** with no figure. | A screen-level empty state is not a hidden panel: there is nowhere else to look and there has to be a way out, which is what `04 §9` asks for and why «لا مشروع يطابق المرشّحات» keeps its «مسح» button. «لا مشاريع خارج المسار» is an **answer**, not an absence — hiding it would make a clean portfolio indistinguishable from one whose watchlist was never built. And a cell cannot be hidden without shifting every cell after it, so an absent figure in a row is a dash. |
| P-146 | P-140 dropped a curve when there was no baseline AND no updates. A project whose baseline starts in the same month as the data date passed that test and still rendered nothing: `Monthly` correctly returned ONE month end, and the panel drew a single dot at 0% under four empty gridlines | **`ProgressSeries.Drawable`** — two points at least, and something on them (any planned above zero, or any recorded actual). All three screens call it; an undrawable series comes back empty and the panel does not render. | Two separate mistakes were behind the same picture. One point is not a trend, and a curve is a claim ABOUT a trend — there is nothing to trace between a point and itself. And a series of zeros with no recorded actual draws a flat line along the axis, which reads as «no progress was made» when the truth is «nothing was recorded». The test lives in `ProgressSeries` because that is what builds the series and therefore what knows when it has built nothing; putting it in the three endpoints would have meant three chances to disagree. Found by the client on a live screen, not by a test — the fixture only exposes it on PRJ-0207 and PRJ-0159. 3 tests. |
| P-147 | **BUG.** `/projects/new` reached from the ministry-wide register carries no `?ws=`. The form accepted every field and then refused on submit with «اختر مساحة عمل قبل تعريف مشروع» — naming a fix it did not provide | **A workspace field**, shown only when creating AND nothing is in scope. The list is what this capacity may open (BR-15). The identity line changes with it: «يُحفظ المشروع ضمن مساحة العمل الحالية» is only true when there IS a current one. | الشكل 3 puts this screen at «مساحة العمل › المشاريع», so walking the documented path always carries a workspace — which is why the gap survived. But the ministry-wide register offers «مشروع جديد» too, and from there the screen was a dead end that only announced itself after twenty fields. Offering a workspace the server would refuse would move a 403 onto the user, so the list is the assigned ones and no others. |
| P-148 | **CLIENT DECISION.** «العرض بصفة» sat in the topbar as `<epm-role-switch />`. Its own header argued for that — capacity is global state, it rides on every request — and the client: *"the permission switcher should be on the page itself not across the app this needs to be resolved to avoid confusion"* | **Removed from the shell**; the component and its CSS deleted. The account menu still STATES the capacity — a reading, not a control. | The argument in that header was about where the STATE lives, and the state has not moved: it is still one value on every request. What moved is where it can be CHANGED, and the client is describing what the control looked like, not what it did — an app-wide dropdown beside the workspace crumb says *this is who you are*, when what it actually does is review the change-order permission model. The appendix names the switcher exactly three times and all three are change-order screens; `03 §7` gives the same reason. This is the second time the same control has been asked for per-screen (P-126), and the first pass left the global one in place beside it, which is precisely the confusion named. |
| P-149 | Links grew an underline on hover — `.epm-crumb-link`, `.epm-crumb-ws` | **Colour only**, as the prototype does. | Both rules were OURS, added on top of the copied sheet. Checked against the live prototype's stylesheets: `.d-crumbs a:hover` sets `color: var(--primary)` and nothing else. The four underline rules that ARE in `desktop.css` — the Z2 crumb, the footer, `.d-tile > .tt`, `.d-linkbtn` — are the prototype's own and stay. |
| P-150 | Table headers sat start-aligned above end-aligned figures. Under RTL that puts the label hard right and the number hard left with the column's whole width between them | **Every column's header takes its own column's alignment.** Fixed on the alerts, reports, portfolio, workspace and BOQ registers. | Root cause was one line of ours: `web/src/styles.css` — `.d-table td.num { text-align: end; }`. The prototype's `.d-table .num` sets tabular numerals and never touches alignment; it end-aligns explicitly with `.r` on BOTH `th` and `td`. Our rule moved the cells silently and left every header where it was, so the defect appeared wherever a column was marked `num` without `r`. **Found by measuring, not by reading**: the markup scan missed most of it because a class three stylesheets away decides the answer, so the check walks the live DOM and compares computed `text-align` of each header against its own first cell. That check is worth keeping. |
| P-151 | The curve x-axis read «ش1 … ش24» | **The period's own month** — «03-2025», then «04», «05» …, with the year repeated only where it turns. | An ordinal numbers the boxes and names none of them: a reader could not say WHEN the actual line went flat, or line a curve up against a payment date, without counting from the left. The server has always sent each period's real date and the client was discarding it. Digits rather than «تشرين الثاني» because a tick has about 28px on an eighteen-month curve and because an axis is a number line, not a sentence — it reads the same in both languages. The year is dropped where it has not changed: «03-2025» is 27px in a 28px slot, and the repeated year was the part carrying no information. Measured after: 10px minimum gap, no overlaps. |
| P-152 | Removing the global switcher (P-148) broke the two screens that refuse by capacity: «§23 يسند تعريف المشروع … بدّل «العرض بصفة» للمتابعة» pointed at a control that no longer existed anywhere | **`<epm-persona-switcher />` inside the refusal itself**, on the project form and the contract form. | A screen that refuses and then names a button somewhere else was already weak; after P-148 it named a button nowhere. The control now sits under the sentence that asks for it. This is not a retreat from P-148 — it is the same rule applied honestly: the switcher belongs on the screens that answer differently to different صفة, and a screen that REFUSES by صفة is exactly one of those. |
| P-153 | A `.d-verdict` row whose chart is hidden (P-144) still reserved two full grid rows — `.verdicts` splits the CHART's height between the big figure and the index row — leaving a tall empty band around one small card | **`.d-verdict.solo > .verdicts { grid-template-rows: auto; align-content: start; }`**, and its `.vdrow` auto-fits instead of pinning three columns. | With the chart gone there is no height to split, and reserving it anyway is the dead space the client pointed at. Measured after: the row is 89px and its content is 89px. Same class of mistake as the pinned three-column `.vdrow` — a layout sized against something that is no longer on the screen. |
| P-154 | The module rail underlined every item on hover, which the prototype never does | **`.d-nav-item{text-decoration:none}` in `styles.css`.** | The reference renders rail items as `<button>` (desktop-shell.jsx:248) because it navigates by calling `onNav()`; the port renders them as `<a routerLink>` so a module is a real, shareable, middle-clickable address. That swap silently inherited `tokens.css:397 a:hover{text-decoration:underline}`, and `.d-nav-item:hover` (desktop.css:90) never declares `text-decoration`, so nothing overrode it — hover ended up carrying three signals (background, colour, underline) where the design uses two. Declared in `styles.css` rather than in the copied sheet (P-07). The anchor stays: the URL is worth more than the class of element the reference happened to use. |
| P-155 | The workspace rail carried a seventh item, «العودة إلى المركز» (`.epm-nav-exit`), that the prototype does not have | **Removed**, with its CSS and its `ws_back_to_ministry` string. | It was ours, added on the reasoning that entering a scope must have a visible exit. Diffing both shells' live DOM at workspace scope shows the reference rail is exactly the six `wsNav` modules, and the way out is the switcher's first row «المساحة الرئيسية» — the same control that got you in. This port already renders that row, plus a «الوزارة» breadcrumb button and a command-palette entry, so the rail item was a fourth exit, not the only one. Verified after removal: switcher → المساحة الرئيسية lands on `/portfolio` and the rail swaps back to the seven enterprise items. Client asked for the match explicitly. |
| P-156 | P-86 left `.xlsx` unread — «تحليل Excel» is one of الشكل 13's five step names, and the wizard refused a workbook with "save it as CSV" | **SheetJS, lazily imported.** `await import('xlsx')` in `readWorkbook()`; CSV/TSV keeps the inline parser. First sheet only, `header: 1` (the column mapper is a USER step, المسار 3 step 3أ), `raw: false` + `defval: ''` so every cell arrives as the text Excel displays and the grid has the same shape the CSV path produces — a date arriving as serial `45292` would otherwise read as a quantity. **Installed from `cdn.sheetjs.com`, not npm**: npm's `xlsx` is frozen at 0.18.5 (2022) and carries CVE-2023-30533 (prototype pollution, fixed 0.19.3) and CVE-2024-22363 (ReDoS, fixed 0.20.2); 0.20.3 has neither. This is the app's first non-Angular runtime dependency, and P-86 said that call belonged to whoever owns deployment — the client made it. Lazy so the ~400KB parser never loads on the register's first paint. | Client decision on P-86's CONFIRM | Resolved — **P-86 superseded** |
| P-157 | Who may approve a BOQ import? المسار 3 puts submit (step 6) and approve (step 7) in **two different lanes** — «المستخدم المختص» and «إدارة المشاريع» — but `03 §7`'s persona list has no party called «إدارة المشاريع» | **`Personas.CanApproveBoqImport` = دائرة المهندس المقيم or مدير المشروع.** `CanDefineProjects` is deliberately REFUSED: the specialist who submitted the sheet may not approve it. The rate and change-order committees are also refused — their remit is a priced decision on a line, not the bill as a document. The version now records `Approver*` + `ApprovedAt` beside the submitter's `Actor*`, never overwriting them: «من قدّم» and «من اعتمد» are two facts about one document that moves contract value. The BOQ page carries `<epm-persona-switcher>` so both views are reachable, and the specialist's bar prints WHY there is no button rather than hiding it. | المسار 3 · 03 §7 · client asked to see both views | **CONFIRM** — which party «إدارة المشاريع» is |
| P-158 | The pending-version bar never appeared on a single-contract project | **`loadImportVersions()` now resolves `effectiveContractId()`, not the route's `contractId()`.** | A project with one contract is never asked to choose (P-46), so the URL carries no contract and the route signal is empty — the method returned early. It failed on exactly the projects most likely to have a pending import, and every other call on the page already resolved the contract this way. Found while wiring the approve button. | Resolved |
| P-159 | **The supply sub-type was built, tested and unreachable.** D-14 keys the bill's shape off the PROJECT's type, and D-13 typed all five fixture projects `construction` — so `BoqKind.ForProjectType` never returned `supply`, `SupplyItemDetails` was never read by a screen, and `countBySupplyStatus` came back `{}` on every register in the system | **`PRJ-0207` is retyped `equipment`** (and its `ExpenditureCategory` moved `construction` → `equipment` to match D-13's 1:1 map). Verified: `EP-BOQ-02` now returns `kind: "supply"` with the four supply states, and `EP-BOQ-12` writes a `SupplyItemDetail` whose `status`, `receivedPct` and `remainingQty` are derived by `Domain/SupplyStatus` — its first execution against screen data. | It is the only candidate that costs nothing. A bare retype needs a project that OWNS a contract, which rules out `PRJ-0159` and `PRJ-0277` (none), and the remaining three are `PRJ-0279` (the flagship works demo, two contracts) and `PRJ-0148` (the المسار 3 works test page). `CNT-0207` carries no BOQ rows, so retyping it re-weighs nothing and moves no total — every figure recorded in P-62 and elsewhere is untouched. **Three costs, recorded rather than papered over.** (a) **The name contradicts the type**: «صيانة شبكة المياه» is not a تجهيز project. Renaming a fixture row to fit a type would invent a project, and Fixture.cs's own comment on `PRJ-0207` warns that a row contradicting itself is what makes a reviewer stop trusting the dataset — so the contradiction is left visible instead of hidden. (b) **`ExecutionStage` stays `mep-first-fix`**, one of `06 §2`'s twelve CONSTRUCTION stages, because `06 §2` defines no supply stage list to move it to (P-12's question, one type further on). (c) **The default persona cannot see it.** `PRJ-0207` is in workspace `nu`; `Personas.DefaultId` is `user.univ-specialist`, assigned `["ub"]` — so a reviewer who opens the supply bill without switching persona is redirected by P-65's guard. Only `co-committee`, `co-rapporteur` and the two ministry-wide personas reach it, and none of them is the specialist who defines bill lines. | **CONFIRM** — whether the ministry wants a purpose-built supply project in the fixture (a named تجهيز project with device lines) rather than a retyped maintenance one; that is the only thing that clears all three costs at once |

| P-160 | **`BoqRateBands` was registered, read by three endpoints, and empty.** No fixture order had been applied against a line, so `banded` was false on every row in the system, the item card's «تفصيل الأسعار» had nothing to open, and `EP-BOQ-13`'s guard — the one that refuses to let a spreadsheet overwrite a rate لجنة تثبيت الأسعار fixed — had never once executed | **VO-06 is seeded APPLIED and CLOSED**, writing one band on `CNT-0279-EM|BQ-002` at 14,345,250, plus amendment no. 1 on `CNT-0279-EM` (+1,250,000, 0 days), six walked stages, nine apply steps and its audit trail. **And VO-07 is minted** to inherit P-59's young-pending role. Verified: the register returns `banded: true` with weights 56.67 / 43.33 still summing to exactly 100.00 (BR-01 holds), and approving an import version over BQ-002 now returns **409 naming BQ-002** — the guard's first execution. | VO-06 was the only `rate`-type order, so it was the only one that could seed the table. Two costs were weighed and taken deliberately. **(a) `02 §1`'s worked example leaves the screen.** BQ-002/BQ-004 were 56.13% / 43.87% of 100,000,000 — the figures `02 §1` and D-07 are written on — and become 56.67% / 43.33% of 101,250,000. `BoqWeightsTests` still asserts the original from its own inline inputs (P-04), so no test starts lying, but a reader looking for 56.13 on this screen will not find it. **(b) P-59's proof would have collapsed.** VO-06 was also the fixture's only YOUNG pending order; closing it leaves pending = {VO-02} and overdue = {VO-02}, identical sets, which is the exact defect P-59 was written to prevent and which was measured once already. VO-07 — a 1-of-6 quantity increase on BQ-004, five days old, sitting at stage 2 — restores it, and restores it better: VO-07 is pending-not-overdue while VO-03 is overdue-not-pending, so the sets now differ in both directions. **What this does NOT seed: the 20% tier split.** `02 §5`'s rule is about QUANTITY excess, and VO-06 changes no quantity — `ChangeOrderApply.BandsFor` returns a single band for a `rate` line. No fixture order applies a quantity increase past the ceiling, so no line in the database carries two bands; the tier rule still runs only in `TierSplitTests` and the wizard preview. | **CONFIRM** — whether the ministry wants a second applied order that DOES cross 20%, which is the only way the two-band case reaches a screen |

| P-161 | **`EP-BOQ-12` has no capacity check beyond workspace access**, so any persona assigned to the workspace may add a line to a bill of quantities — measured: `user.re-dept` (دائرة المهندس المقيم) posted a line to `CNT-0148` and got **200** | **Left as it is, and recorded rather than guessed.** `Load()` resolves the project and applies `WorkspaceScope.Deny` (BR-15); nothing else gates the write. No `Personas.Can…` predicate is consulted. | Adding one would mean INVENTING an authority rule. `EP-BOQ-13` could gate approval because المسار 3 names two different lanes for submit and approve, which is a spec sentence to read off (P-157); المسار 3 step 3ب names no capacity for manual entry at all, and `03 §7` does not say who may write a bill line. So a check here would be ours, not the ministry's — and on a screen that moves contract value, an invented permission is worse than a missing one, because it looks authoritative. **It is also defensible on its own terms**: a مهندس مقيم is the party standing on site, and المسار 3 step 3ب exists precisely for the case where there is no sheet to import. But nothing in either document supports OR refuses it. Recorded per CLAUDE.md §8. | **CONFIRM** — which capacities may add, edit and delete a BOQ line manually, and whether that differs from who may import one |
| P-162 | **Two screens print «قيمة المشروع» for one project and disagree** — the BOQ register's footer says 341,250,000 on `PRJ-0279` where SCR-W1's Overview says 351,250,000 | **Flagged, not resolved.** `BoqEndpoints.ContractAmounts` sums the BILLS (Σ line amounts across sibling contracts, band-adjusted); BR-00's `ProjectValue` sums the EFFECTIVE CONTRACT VALUES. Both are correct about their own subject and neither is a bug. | The 10,000,000 gap is VO-01's applied amendment: it moved `CNT-0279`'s contract value from 240,000,000 to 250,000,000 and its BOQ lines were never re-quantified, which is faithful — an engineering order can raise a contract's value without anyone having restated the bill yet. The defect is the LABEL, not either figure: one phrase over two different quantities is exactly the disagreement P-54 was written to prevent one screen earlier, and a reader cannot tell which they are looking at. **Not fixed here because the fix is a decision, not an edit**: either the footer is relabelled to say it is the bill's total, or the register adopts BR-00 and stops agreeing with its own column above it. **Pre-existing** — the same gap read 340,000,000 vs 350,000,000 before VO-06 was applied. | **CONFIRM** — what «قيمة المشروع» should mean on a bill screen |

| P-163 | Where do «العروض» saved views live? The reference keeps them in `usePersistedState('boq.views', [])` — localStorage | **A table, `BoqSavedViews`, owned by the `X-Epm-User` persona** (`EP-BOQ-14/15/16`). Client instruction: this system persists to the database. | A view in the browser is lost on another machine, invisible to its own author when they sign in elsewhere, and owned by nobody — the same objection P-29 raised against the reference's in-memory alert acknowledgement, and the same answer. Three consequences worth stating. **(a) It is the only table here holding no ministry data** — a search string, a coverage chip and a set of column keys — so it carries no workspace guard: filtering by `UserId` IS the rule (BR-15 has nothing to scope). **(b) Not scoped to a contract or project**, matching the reference's single unscoped key: nothing a view stores belongs to a bill, and a per-contract view would vanish the moment you switched contract. **(c) `sort` is not stored** though the reference's view carries it — this register has no sort control to capture. Columns are a CSV of the SHOWN set, the shape `Projects.BeneficiaryCodes` already uses, so adding a grid column is not a schema change and a view cannot start showing a column its author never chose. | Resolved |
| P-164 | «الجهات المستفيدة» was a `toast.demo` stub, and the previous note said no appendix plate defines the screen | **Built from the reference component, which does define it** — `contract-context.jsx:182 DBenDrawer`: the ministry's master list with a tick per row for this project, «الجهة الأم», an active/inactive pill, and the note that only ticked entries appear when distributing. `EP-PRJ-05/06`. | CLAUDE.md §1 settles the apparent gap — the plate gives the rules, the reference gives the screen, and the reference had it all along. **It is a PROJECT edit, so it lives in `ProjectsEndpoints`, not `BoqEndpoints`**, even though the drawer opens from the BOQ toolbar: the tick writes `Projects.BeneficiaryCodes` (01 §2.1), the same column `EP-PRJ-03` writes, and it must be audited by the same `ProjectActivityEvents` writer — verified, the save appears in الشكل 5's log with the specialist's four attribution facts. A copy in the BOQ feature would have been a second way to change one column with only one of them audited. **Nothing new is stored**: the master list and the CSV both already existed. `01 §2.1`'s inactive rule is enforced on both sides — an inactive beneficiary cannot be NEWLY ticked, but one already ticked stays editable, because it may hold quantity distributed before it was stood down and silently dropping it would erase a distribution the drawer never showed. The fixture has no inactive beneficiary, so **that branch is written and unexercised**. | Resolved |
| P-165 | **The API speaks two error shapes and the BOQ page read only one**, so every bilingual refusal reached the user as Angular's own «Http failure response for …: 409» | **`message()` prefers `messageAr`/`messageEn` by current language**, then `message`, then the transport error. | Found while wiring `EP-PRJ-06`, but it was never confined to it: the older refusals carry `{ message }` and every newer one carries the bilingual pair — `BoqKind.Unsupported`, `EP-PRJ-06`'s two checks, and **`EP-BOQ-13`'s banded-line guard**, the one P-160 had just made executable for the first time. That guard refuses to overwrite a لجنة تثبيت الأسعار rate with a sentence naming the offending line, and the user was shown a raw HTTP string instead. Measured before and after on the capacity refusal: «Http failure response for http://localhost:5080/api/projects/PRJ-0279/beneficiaries: 403 Forbidden» became «تعديل الجهات المستفيدة من صلاحيات المستخدم المختص في الجهة.». **Only `boq.page.ts` is fixed** — the same one-line read exists on other pages and should be audited. | Resolved · **CONFIRM** — audit the other feature pages for the same read |
| P-166 | **الشكل 12's one caveat printed TWICE** under the register — `boq_basis_note` and `boq_calc_note` are the same Arabic sentence, and the template rendered both back-to-back, each with its own `functions` icon | **The `<p class="boq-basis-note">` copy is gone**, with its string and its CSS rule; `.d-pager.calcnote` survives. | Found by diffing the running build against the live prototype, not by reading the template — on screen the two lines are identical and the eye slides over the repeat. The prototype carries **exactly one** node for it, a `<span>` inside `.d-pager.calcnote`, which is the block kept. The two strings had drifted only in ENGLISH («Weights are of the contract value…» vs «Weights from contract value…»), which is how one sentence survived as two keys long enough to be rendered twice. | Resolved |
| P-167 | **`01 §2.1`'s inactive-beneficiary rule was enforced twice and reachable NEITHER time** — the drawer disables the checkbox and `EP-PRJ-06` refuses with «لا يمكن ربط جهات موقوفة», and all five seeded beneficiaries were `Active = true` | **One inactive row seeded** — `BEN-UOB-RUS` «موقع الرصافة», a `site` under جامعة بغداد, referenced by no project. Measured after: the PUT refuses it with 400 and the drawer renders it disabled, carrying its «موقوفة» pill. | A guard no fixture can reach is a guard nobody reviews — the same argument P-59 makes for keeping a young pending change order beside an overdue one. **The live prototype seeds exactly this row** («موقع الرصافة (موقوف)», the only موقوفة entry among its eight), so it is the reference's shape and not an invented case. It also retires the seed's old rule that every beneficiary must be referenced by some project: that held while SCR-W1 read only a project's own codes, and `EP-PRJ-05` returns the WHOLE master list, where the unticked rows are half the point. **Still unreachable: the branch that leaves an ALREADY-ticked inactive beneficiary editable.** That needs a project whose `BeneficiaryCodes` already contains a stood-down code — a legitimate historical state, but one that ripples into the overview and the distribution drawer, so it is not seeded on the way past. | Resolved · **the already-ticked-inactive branch stays unreachable** |

| P-168 | **A warehouse receipt is capped against the CONTRACTED quantity, not against what the supplier actually delivered** — so an item may end up «مستلم بالكامل» with a received quantity ABOVE its `SuppliedQty`. Driving the live fixture: ITM-002 is contracted 111 · supplied 104 · received 95, الشكل 53 prints «المتبقي 16 جهاز» (= 111 − 95), and booking that 16 takes received to 111 — nine more than the supplier is recorded as having delivered | **Left as built: the ceiling is the contracted quantity.** `Domain/SupplyReceipts.Remaining` returns `contracted − Σ warehouse`, which is the number الشكل 53 itself prints. `SuppliedQty` is NOT a second ceiling | **الشكل 53's own worked figure settles the formula and nothing settles the coherence.** «المتبقي 16» on ITM-002 can only be 111 − 95; a supplied-based cap would make it 9 and the plate wrong, and CLAUDE.md §1 makes the plate binding over an inference. But no plate states «الكمية ≤ المجهّز» either, and physically a store cannot receive more devices than arrived. Two readings are open and only the client can choose: (a) `SuppliedQty` is the supplier's *cumulative delivery note* and should cap receipts, in which case الشكل 53's «المتبقي» is the lesser of the two and the plate's 16 was drawn on a row where supplied was still 111; (b) `SuppliedQty` is an *informational* figure from the supply order and receipts are measured only against the contract, which is what is built. **Nothing was invented either way** — the rule matches the only number the documents print. `SupplyReceiptsTests` covers the contracted ceiling; a supplied ceiling would be a second rule with its own tests, not an edit to this one | **OPEN — client** |
| P-169 | **الفقرات التجهيزية was built with الشكل 55's furniture on الشكل 50's screen.** Driving the running register against the client appendix: the items table sat inside a collapsible section titled «الفقرات التجهيزية» with a «7 من أصل 7» counter, its toolbar folded into that section header with the chips BEFORE the search and «الكل» drawn LAST; الشكل 55's register meanwhile had no heading at all. The receipts columns were headed «الرمز» and a merged «الجهة أو المخزن», and «لجنة الاستلام» appeared on both receipt drawers | **The frame moved onto the SHARED grid and both registers now use it.** `epm-data-table` gained `title`, `sub` and a projected `[epmTableToolbar]` slot, and composes `epm-field-group` — our `DFGroup` — with a new `flush` input. الشكل 50 renders as the prototype does: title row «الفقرات التجهيزية · 7 من أصل 7» with a chevron, a flush toolbar beneath it (search, then «الكل», then the four states, zero-count chips DISABLED, then «مسح الفلاتر» when filtered), then the grid. الشكل 55 gets the same frame, titled «سجل الاستلام المخزني» / «سجل الاستلام الأولي» by its own kind switch. The receipts columns became «الفقرة» and «المخزن»/«الجهة المستلمة». The committee — drawer field, table column and fixture value — became warehouse-only | **The appendix prose is not a layout, and reading it as one produced a wrong answer first.** الشكل 50's «الوظائف والخصائص» lists furniture in order and names no heading, so a first pass REMOVED the section header — and the client's own screenshot has one. `supply-items.jsx:545` settles it: the register is `<DFGroup flush title="الفقرات التجهيزية" sub="{shown} من {total}">` wrapping a `.d-toolbar` and then the table. The prose omits the frame because the frame is not a feature of that screen; it is the shell every register shares — which is exactly why it belongs on the shared grid and not in a page template. CLAUDE.md §1 says this in as many words: **the written spec gives you the rules, the reference component gives you the screen**, and the reference for this module is the LIVE prototype, which `docs/spec/reference/` does not contain. Both orders do put «الكل» first, and the prototype also DISABLES a zero chip rather than hiding it. **The committee is settled by الشكل 52**, which prints the two receipt cards together and they differ exactly there — «مخزن الوزارة المركزي · لجنة الاستلام المخزني · مطابق» against «جامعة البصرة · مطابق» — and الشكل 54's field list omits it too; a hand-over is witnessed by the receiving party. No CSS was added: `.d-toolbar`, `.grp` and `.d-field` are already canonical in `desktop.css` and SCR-W4 uses the identical shape | **CLOSED** — verified on a reset fixture against the live component: the frame renders «الفقرات التجهيزية · 7 من أصل 7» with its chevron, body order is `.d-toolbar` then `.d-tablewrap` inside `.gb.flush`, and the chips read «الكل 7 · مستلم بالكامل 0 · استلام جزئي 7 · محجوز 0 · لم تُجهَّز 0» with the three zeroes disabled — الشكل 50's own figures. **One CSS rule was added** (`web/src/styles.css`): `desktop.css:3086` already flattens `.d-toolbar` and `.d-vow-tw` inside `.gb.flush` but had none for `.d-tablewrap`, which is the shell this grid emits. Grep-verified absent first (§3.7), tokens only. **The rollout stopped here by the user's decision** — 42 hand-rolled tables across 26 files still emit their own `.d-tablewrap`, and moving them onto this grid is a separate pass |
| P-170 | **`POST /api/dev/reset` produced a database the app could not be used on.** `Fixture.cs:31` was the only writer of `LookupCatalog.Rows()`, so after a reset all 42 lookup categories were empty — «نوع التشكيل» rendered a blank select, and with it every status, type, stage and category control in the app. The first تشكيل could not be created, so nothing downstream of it could either | **`LookupCatalog.EnsureSeededAsync` is called wherever a schema is created** — `Program.cs` after `EnsureCreated()`, and at the end of `EP-DEV-01`. Idempotent: it writes only into an empty table, so an existing database is untouched. Reset now reports «Database is empty; lookup vocabulary seeded» | **This is not seeding demo data, and the doc comment says so where the next reader will look.** CLAUDE.md §4's «nothing is seeded automatically» governs the `06 §12` SCENARIO — the projects, contracts and figures the fixture itself flags as «illustrative, not ministry data». The lookups are neither: they are the system's own vocabulary, defined in code in `LookupCatalog.cs`, and every enum column resolves its label through them. A schema that exists but cannot be typed into is not a working empty state (`04 §9`) — it is an unusable one | **CLOSED** — verified: boot on an empty database yields 42 categories, the create drawer offers جامعة حكومية · جامعة تقنية · وحدة مركزية · مديرية تجهيز, and `reset` returns the vocabulary while ministry tables stay at zero rows |
| P-171 | **`executingParty` was required by the server and silent on the form.** It is in `ContractDefinition.RequiredFields`, but the field carried no star, no `epm-invalid` binding and no error slot — so a contract was filled in completely and refused at save with «الجهة المنفذة مطلوبة» and no indication which field meant it | **Wired exactly as `contractor` beside it** — star, invalid state, inline message | **The form avoids this everywhere else, which is why it was a defect rather than a style choice.** CLAUDE.md §6 asks for invalid input to be PREVENTED and explained rather than flagged after, and both create forms already gate their action buttons on capacity «rather than shown to everyone and refused on submit». An audit of the rest of the required set — `nameAr · start · finish · contractor · id · component · status · awardAmount` — found every other field correctly wired, so this was one omission, not a pattern | **CLOSED** |
| P-172 | **سنة الإدراج's range was ours, not the ministry's.** `ProjectDefinition.EarliestRegistrationYear` was 2000 and the ceiling `dataDateYear + 1`, carried under a comment that called it «an assumption, not a documented rule … one constant to change if the ministry states a real range» | **The client stated it: 1900 to the data-date year.** Floor 1900; ceiling `dataDateYear`, no longer `+ 1`. The refusal names the range — «سنة الإدراج يجب أن تقع بين 1900 و2026» | **The floor only widens what is accepted; the CEILING is a narrowing and is the half worth naming.** A project can no longer be registered against a plan year that has not begun, because the old `+ 1` was our inference about next year's plan rather than a rule anyone stated. `ProjectDefinitionTests` moved with it — the «one year ahead is allowed» case inverted, and two boundary cases added at 1899/1900. The form gained `min="1900"` (a real constant, mirroring the domain) but deliberately **no `max`**: the ceiling is the project DATA DATE's year, the create form has no source for it, and hardcoding 2026 would put a rotting copy of a domain rule in a template | **CLOSED** — 1899 refused · 1900 accepted · 2026 accepted · 2027 refused, verified against the running API. ⚠️ **Confirm with the client** that registering against an upcoming plan year is genuinely not a workflow; if it is, the ceiling returns to `+ 1` with 1900 kept as the floor |
| P-173 | **The empty database had no way IN.** Every empty state's only exit was «حمّل بيانات العرض» — a dev affordance, and the wrong instruction for someone entering real data. `/projects/new` and the create-workspace drawer both existed but lived in toolbars inside the DATA branch, so they were unreachable exactly when needed. The portfolio additionally rendered its filter toolbar above the empty message, collapsing to a bordered box around one dead «الكل» chip | **A first-run path.** The portfolio toolbar renders only when `projectCount > 0 \|\| filtered()`, which keeps the «a filter hid everything» case it was written for. Each empty state now offers the real next action and names the actual blocker: the projects register detects zero workspaces and routes to مساحات العمل rather than offering a «مشروع جديد» whose required `workspaceCode` has nothing to pick; the workspace register offers create to a ministry persona and tells everyone else which صفة can | **«There has to be a way out» (`04 §9`) was satisfied in prose and not in controls.** The states named the screen to go to without going there. Also: **«العرض بصفة» is now on three registers** — entities, projects and contracts — which is a deliberate departure from P-126, and the templates say why. P-126 moved the switcher out of the shell because on الشكل 29/30 it is a FILTER revealing BR-14's permission model, not a global identity control. The setup path is a different case: defining a تشكيل is ministry-level (`§24`) while defining a مشروع and a عقد belong to المستخدم المختص (`§23`), so **the capacities are inverse and no single صفة can walk the chain**. The switcher is the only way to reach the capacity each screen gates on. `EP-ENT-01` and `EP-CNT-01` gained the persona re-read effect `EP-PRJ-01` already had — BR-15 resolves scope server-side, so rows fetched under one capacity are the wrong rows under another | **CLOSED** — verified on an empty database in both directions on all three registers. ⚠️ **At three exceptions, P-126 is worth revisiting as a rule** rather than accumulating per-screen departures |
## REVERTED — visual fidelity chosen over the 05 §7 floors

**Client decision.** Where the reference prototype breaks the binding
accessibility contract, the prototype wins: the screens must be
indistinguishable from the one the client signed off. All four corrections
below were measured, applied, and then removed again.

They are recorded rather than deleted for two reasons. The measurements are the
expensive part and re-taking them later would be waste; and restoring any of
them is a few lines in `web/src/styles.css`, which is only true while the
values are written down.

| # | What v1.1 ships | Floor | Where it bites | Restoring it |
|---|---|---|---|---|
| P-21 | `--outline` / `--viz-base` `#A9B1BC` light, `#454C57` dark | 3:1 graphics (`05 §7.1`) | **2.16:1** light, **1.87:1** dark. Gantt data-date line (`04 §5`), chart baselines, every hairline that carries meaning | `--outline: #858E9C` light / `#6B7484` dark |
| P-30 | delay figure `--error` / `--status-completed-tx`, forecast date `--error` | no threshold colouring (`05 §7.9`) | SCR-E5. A magnitude coloured by threshold, and a date that reads as an error | drop `.epm-sc-*` from the delay and forecast cells |
| P-32 | `--fg-subtle` `#8B94A1` light, `#7D8794` dark, used as TEXT in ~100 rules | 4.5:1 text (`05 §7.1`) | **3.07:1** on white. `.d-stat-foot` and `.d-table td.d-cell-sub` — the KPI foot line and the secondary column on **every register** | `--fg-subtle: #666F7F` light / `#8D97A3` dark |
| P-33 | `--fs-100: 10px` | 11px (`05 §2`) | every filter-chip count in the app | `--fs-100: 11px` |

**`NFR-A11Y-01` is currently NOT met, by choice.** `ROADMAP.md` Phase 7's
accessibility pass is where this is settled before production — that pass
should start from this table.

Two things were NOT reverted, because neither is a fidelity question:

- **`.epm-sr-only`** on the Alerts row-action column header. The reference
  leaves that `<th>` empty; naming it for a screen reader changes nothing
  visually.
- **`<bdi>` isolation** everywhere (`05 §5.2`). Without it Arabic text
  reorders around numbers — that is a correctness bug, not a contrast one.

## Carried forward, still open

`07-POC-BUILD-PLAN.md §9` open questions D-01 … D-05 remain **CONFIRM** — rate bands after
several orders, the penalty rate, per-stage SLA, delegate cancellation authority, and who
revises a distribution when a decrease forces it. None are resolved by this port.

### Raised by الشكل 5, not resolved by it

| # | Question | Why it was not decided here |
|---|---|---|
| Q-F5-1 | **Do the enterprise lookup catalogues match the ministry's, or the prototype's?** `execution-stage` is a CONSTRUCTION-phase list here (design → handover) and an ADMINISTRATIVE one in the live prototype (دراسة · إعلان وإحالة · سحب عمل · تسوية حسابات); `project-status` is 5 vs 3; `expenditure-category` 5 vs 3. | **الشكل 5's own worked example uses the PROTOTYPE's values** — «النوع تنفيذ أمانة»، «مرحلة التنفيذ إعلان وإحالة» — neither of which exists in our catalogue. But these codes are STORED on every project and read by eight screens, the fixture and the domain tests, so changing them is a data migration, not a screen edit. `LookupCatalog.cs` already flags the divergence at `06 §2` and cites P-12. **Nothing on الشكل 5 depends on the answer**; the register, the portfolio and the schedule do. |
| Q-F5-2 | **What derives أولوية المشروع, رقم اعتماد الموازنة and المنطقة الجغرافية?** All three are tagged «مقترح» by the document and none has a derivation rule anywhere. | See P-72. `Suggest()` fills only the code and the expenditure category, and the expenditure map is itself inferred. `Workspaces` has no governorate column to derive a region from. |
| Q-F5-3 | **Does the activity log record WHICH fields changed?** The live prototype renders per-field «من → إلى» chips and mixes in project-wide events (progress updates). | `ProjectActivityEvent` records the four attribution facts `§7` asks for and no diff. The documents give a count («6 أحداث») and no schema, so a diff would be an invented one — and it needs new columns. The timeline renders the prototype's shape using its own text branch until then. | **UPDATE — answered for the CONTRACT log by الشكل 11 (P-82): the diff is recorded and rendered. `ProjectActivityEvent` still is not, and the same treatment now applies to it whenever الشكل 5 is revisited.**
| Q-F5-4 | **Do `plannedCost` and `designerParty` belong on a screen?** Neither appears in الشكل 5, and no other screen shows them. | They are written by the create form and then invisible. Dropping the columns would lose data المسار 1 requires (`plannedCost` is its «الكلفة أكبر من صفر» clause); showing them on الشكل 5 would add fields the document does not list. |
| Q-F5-5 | **Should clearing a «مقترح» field clear it?** `Apply()` treats an empty incoming `code` / `region` / `expenditureCategory` as "leave the suggestion alone", while `priority` and `budgetApprovalNumber` — which now carry the same badge — do clear. | Pre-dates الشكل 5 and is defensible per field; it is only inconsistent now that all five wear the same tag. |

| P-174 | **Two master lists described one set of real things.** `Workspaces` held «جامعة بغداد» as `ub`; `Beneficiaries` held it again as `BEN-UOB`, with its own type vocabulary, its own tree and its own seed block. P-24 built them separately and closed with «Confirm the department wants two screens». | **One list. The `Beneficiaries` table is deleted, and a «جهة مستفيدة» is a WORKSPACE playing a role on a project.** `Projects.BeneficiaryCodes` is now a CSV of `Workspaces.Code`; EP-PRJ-05/06 and every code→name resolution read `Workspaces`. **NO NEW WORKSPACES WERE INVENTED** — the fixture's beneficiaries were remapped onto the five that already exist (ub · nu · tu · cu · sp). | Answered by the client, closing P-24. The duplication was not cosmetic: the same university had two identifiers, two active flags and two names that could drift, and nothing reconciled them — a project could serve `BEN-UOB` while `ub` was stood down. **The `Beneficiary*` MEMBER NAMES ARE KEPT** on columns, DTOs and TypeScript interfaces (`BeneficiaryCode`, `BeneficiaryCodes`, `ProjectBeneficiaryRow`). They name the ROLE, which is genuinely separate from ownership — the workspace that OWNS a project (`WorkspaceCode`) is routinely not one it SERVES — and renaming 231 sites would have destroyed that distinction while changing no behaviour. **What was lost, and it is a real loss:** the 01 §2.1 parent tree. كلية الهندسة, كلية الطب and موقع الرصافة were beneficiaries below a university and are not workspaces, so sub-university bodies can no longer receive quantity; `ParentCode` and the drawer's «الجهة الأم» column are gone. The six universities that existed only as beneficiaries (نينوى · ذي قار · البصرة · الموصل · الكوفة · تلعفر) are gone with them — **the client's instruction was to stay inside the workspaces the system already has** rather than promote them, so PRJ-0279's two-way split is now `ub` + `nu`, PRJ-0439 serves `ub` + `nu` + `tu`, and الشكل 58's empty redistribution target is `cu`, which holds nothing and is a real workspace. Collapsing two old codes onto ONE workspace was avoided throughout: it would have produced two distribution rows for one entity and broken 02 §8's gate 4. **`beneficiary-type` (06 §6) is deleted with the table it described**; the drawer, the overview card and الشكل 5 all label from `workspace-kind` (P-68). | Resolved |

| P-175 | **The fixture no longer contains an inactive beneficiary**, because the only one was موقع الرصافة — a site under a university, which the merge deleted, and P-174 forbids inventing a workspace to replace it. | Left absent, and recorded here rather than papered over. | `01 §2.1`'s rule — an inactive beneficiary cannot receive new quantity — is still ENFORCED in two places (the drawer's disabled checkbox via `benLocked`, and EP-PRJ-06's «لا يمكن ربط جهات موقوفة» refusal, verified by direct API call). What is gone is the fixture row that made either one FIRE during a review, which is the condition the old fixture comment called out: «a guard no fixture can reach is a guard nobody reviews». The fix is one line — `Active = false` on a workspace — but every one of the five carries projects or a documented empty-state role, so choosing one is a business call, not a fixture tidy-up. | **CONFIRM** — should one of the five be stood down, or a sixth added purely to demonstrate the rule? |

| P-176 | The BOQ register's column ORDER did not match الشكل 12, and it carried a thirteenth column the figure does not list. القيمة المكتسبة sat immediately after القيمة, and التوزيع sat between التنفيذ and حالة التخصيص. | **الشكل 12's twelve, in its order:** الرمز · الوصف · الوحدة · الكمية · سعر الوحدة · القيمة · الوزن % · الأنشطة · الوزن المُخصَّص · التنفيذ · **القيمة المكتسبة** · حالة التخصيص. `distribution` becomes opt-in through «الأعمدة» (default off) rather than being deleted. | The addendum states the column list verbatim under «البيانات التي تعرضها», so this is a specification, not a preference. The position of القيمة المكتسبة is the substantive half: **العرض الفني's own chain is «نسبة تنفيذ البند ← الكمية المنجزة والمتبقية ← القيمة المكتسبة»**, so the figure belongs immediately after التنفيذ, where it reads as that column's RESULT. Beside القيمة it read as a second contract amount, which is the one thing it is not — it is derived from progress, never from the award. Two documents, one order. **التوزيع was not deleted, because nothing is lost by the default:** الشكل 12 names التوزيع as a TAB of the item card («عام · التخصيص · التوزيع · الإنجاز · الكلفة · السجل»), and this page already renders those six tabs, so the data has a documented home. Making it opt-in matches how القيمة الأصلية and الفرق are already handled — columns the figure omits, available to a reader who asks. **Already correct and left alone:** the tfoot الإجمالي row printing وزن 100.00 (BR-01), and the note «الأوزان من قيمة العقد؛ نسبة التنفيذ من الأنشطة المرتبطة» under the table, both of which الشكل 12 calls for explicitly. | Resolved |

| P-177 | الشكل 12's «الوظائف والخصائص الظاهرة» lists the contract switcher, the two tabs, the three buttons, the coverage filters, the search, «العروض» and «الأعمدة» — and **not** the «العرض بصفة» persona switcher this page carries in the same toolbar. | Persona switcher **kept**, and the divergence recorded here rather than silently resolved either way. | Removing it would break a documented workflow: المسار 3 has two sides on this one screen — the specialist submits an import (step 6) and إدارة المشاريع approves it (step 7) — and `03 §7` asks for exactly that capacity switch. The figure is a screenshot of one persona's view, so its absence is weak evidence of a prohibition. But it IS a visible control the binding figure does not show, and this codebase treats the figures as specifications elsewhere in this very pass (P-176). | **CONFIRM** — should «العرض بصفة» stay on the BOQ toolbar, or move to the account menu? |

| P-178 | Zone Z6 — the module bar — was written inline in **nineteen** page templates and **fifty-seven** times, because most modules draw it three times over: once for loading, once for error, once for the loaded screen. And its tabs sat inline beside the title, where الشكل 12 puts them on their own line. | **`shared/module-bar.component.ts`** — `<epm-module-bar [title] [backLabel] [tabs] (back)>` with `sub`, `actions` and `tabs` projection slots, drawing الشكل 12's TWO-line bar. Converted on the BOQ page only this pass; the other eighteen keep their inline markup until the shape is reviewed. | Three copies of one bar per page could drift without anything failing, and on several pages they already had — the loading and loaded bars carried different subtitles. The tab ROW is the visual change: الشكل 12 draws «السجل · الربط بالأنشطة» under the title bar, not beside the title. **The tabs are `.d-tabs`/`.d-tab`** (desktop.css:885) — v1.1 §9's "rest muted·500, active accent·600 + 2px underline", which is what الشكل 12 draws. They were FIRST built as `.d-fchip` on the reading that CLAUDE.md §1's correction (PAGE-01 reached for `.d-secnav` where `.d-fchip` belonged) applied here too. It does not: that correction is about FILTER CHIPS, a toggle over a list, and a tab switches which screen you are on. A filled pill does not say that, and the client rejected it on sight. Three CSS rules carry it, all in `styles.css` and all on tokens: `.d-tabs.z6-tabs` drops the control's `margin-top: 16px` and gives it the bar's surface and inline padding, since `.d-tabs` is written to sit in page content rather than as a band; `.d-pz6:has(+ .d-tabs.z6-tabs)` makes the PAIR close with one hairline instead of drawing two a row apart and reading as an empty box; and `.d-tab .n` styles the count badge «السجل ١٠», which had no rule because no tab in this build had carried one. **The contract selector moved too** — الشكل 12 puts «العقد CNT-0137 — عقد الأعمال المدنية» against the module name, not out with the actions, because it names WHAT is on screen while the actions are what you can do to it. That is the `scope` slot, projected before the spacer. With the name in the selector the subtitle is the ID alone; with one contract there is no selector (P-46) so the name stays in the subtitle. **`backLabel` doubles as the back control's switch** rather than a separate `showBack`: an icon-only control cannot exist without an accessible name (05 §7), so "has a label" and "has a control" are the same condition, and a caller whose back depends on state writes one expression — `[backLabel]="singleContract() ? '' : …"`. **`tabs` is an explicit boolean, not a `@ContentChild`**: the row must not render as an empty 34px band on the ten modules with no tabs, and a content query resolves after first paint. Verified in both states — with tabs, the title band drops its border and the tab band carries it; without, the single band keeps its own. | Resolved |

| P-179 | الشكل 19 logs four kinds of financial event and only three could exist. A payment is a `Payments` row and an applied amendment is a `ContractAmendments` row, but «تعديل كلفة أو تخصيص» is an EDIT to a column, and nothing anywhere kept the value that column held before. الشكل 18 calls itself «مدخل التحرير الوحيد للبيانات المالية للمشروع» and the tab was read-only, saying so. | **`FinancialEdits` — one append-only row per CHANGED field**, with its before value, its after value, the fiscal year where one applies, and §7's four attribution facts. Written by **`EP-FIN-04`**, which is الشكل 18's «تعديل». | `ProjectActivityEvent` was the obvious home and the wrong one: it records THAT the definition was edited, carries no before/after pair, and its own comment says it is not an audit framework. Widening it would make one table answer two questions. **Four of the eight figures الشكل 18 prints are editable and four are not** — spend is Σ payments by `PaidDate` (P-92), retention is paid-only (P-26), planned % is P-53's curve — and the form renders those read-only *inside edit mode too*, because a control the save would ignore is worse than no control. Two refusals are §15-2 read backwards: a revised cost below cumulative spend, and an allocation below its own year's spend. And الشكل 15's «السنوات السابقة سجل مقفل لا يُغيَّر إلا بإجراء مناقلة معتمد» is now enforced rather than printed — `ProjectAllocation.Closed`, a column nothing wrote, is read, and written on every save for years before the data date's. | Resolved |

| P-180 | **الشكل 14 runs its header equation on one basis and totals its own table on another, and this build had only the second.** The plate reads «المقررة 1,374,210,115 + تغييرات معتمدة 125,789,885 = المعدلة 1,500,000,000 − المصروف 510,305,195 = المتبقي 989,694,805» while the table beneath it totals 2,156,653,454 — and «أساسا القياس» exists to set the two against each other. Our strip bound Σ contract original and Σ contract effective, so it restated the footer and the budget figure never appeared. `1,500,000,000` is also STORED, not derived: الشكل 19 shows it edited 1,477,500,000 → 1,500,000,000, and its «تغييرات معتمدة» is not the contracts' applied-delta total. | **`Projects.RevisedCost` beside `PlannedCost`, and `Domain/BudgetBasis`.** The strip, Z10, نسبة الصرف, الإنجاز المالي and the EVM budget all read the RECORDED pair; the sheet's rows and footer keep the commitments. Where no revised cost is recorded the basis falls back to commitments and **the strip names the fallback** rather than implying a budget nobody entered. | **This settles P-44 at the project level from the client's own document.** العرض الفني §23-1: الإنجاز المالي is «المصروف التراكمي نسبةً إلى الكلفة المعدلة». `02 §4` said only that financial % «comes from payments», which is why SCR-W3 renders none — the denominator was the open half of the question. One derivation and three callers is not a preference: SCR-W1, SCR-W6 and SCR-W7 all print this percentage and the EVM board, and a second copy of the rule is how two screens come to disagree about one number (P-54). The fixture carries all three states on purpose — `PRJ-0279` where budget and commitments agree, `PRJ-0148` where the budget was cut under a signed contract and the note box demands a مناقلة, and the projects with no recorded budget at all. | Resolved |

| P-181 | **المسار 8 steps 5–9 did not exist.** `EP-FIN-02` wrote a `pending` certificate and opened its audit route, and nothing ever advanced it: `Payment.Status` could only ever be `pending` from the app, `certified` and `paid` came from the fixture alone, and `PaymentAuditStage.FinishedAt` was never written. `Personas.cs` said so in its own comment. | **`EP-FIN-03` — one endpoint, not three.** The desk holding the file releases it, and whether that ADVANCES the route, CERTIFIES the works or DISBURSES the money is `Domain/AuditRoute.Release`'s answer. | Three verbs would let a client certify a certificate sitting at the disbursement desk, and would put the lifecycle in the caller's hands. One release keeps `Payment.Status` a projection of the desks beneath it, written from one function so the two can never disagree. **Two gates, both of which must pass**: the CAPACITY gate is the endpoint's — `CanReleaseAuditDesk` matches the persona's party against the desk's, so the resident engineer cannot release the finance desk and the finance directorate cannot release the resident engineer's — and the ROUTE gate is the domain's, where only the desk actually holding the file may let it go. **Certification is the FIRST desk's release**, not the last audit desk's: `Payment.CertifiedDate` is «when the works were certified» and المسار 8 step 5 is the resident engineer's audit of exactly those works, which is also what makes a certificate «السلفة الجارية» — certified and not yet paid (P-99). مسار 8's step 6 escalation stays DERIVED from an overdue desk and is never recorded, so the banner and the dot cannot disagree. | Resolved |

| P-182 | العرض الفني §15-2 states two ceilings — «الصرف السنوي لا يتجاوز التخصيص السنوي، والمصروف التراكمي لا يتجاوز الكلفة المعدلة» — and مسار 8 makes checking them step 4. `EP-FIN-02` validated the gross, the net, the letter, the attachment and the portion split, and neither ceiling. | **`Domain/PaymentCertificate.Ceilings`, called TWICE**: at registration, and again at the disbursement desk. | The registration check is a PROJECTION — no money has moved — but the place to refuse an amount is before it enters a route three desks long. The disbursement check is the one that matters: months of route later the allocation may have been consumed by another certificate or the revised cost lowered by الشكل 18, and that release is the moment money actually moves. Both refuse with the ceiling NAMED and the excess printed, because «تجاوز» with no figure is a refusal the person cannot act on. **An absent ceiling is not a ceiling of zero** (P-09): a project the finance directorate has not reached records no allocation and no revised cost, and refusing every certificate on it would block the work rather than protect the budget. **The 90% ALERTS are a separate mechanism** — R2 and R9 warn, these refuse — and wiring the rules to fire from live figures rather than seeded `Alerts` rows is an alerts change, not a finance one. | Resolved — **the alert half is open** |

| P-183 | **Three sources gave the payment audit route three different shapes, and the disagreement was a live defect.** `Fixture.AuditStages` seeded three desks at 7/7/5; `EP-FIN-02` created FOUR at D-03's uniform five, inventing a «الرقابة المالية» desk; الشكل 17 draws two audits and مسار 8 adds the disbursement act after them. A certificate registered through the wizard therefore got a different route, and different caps, than every seeded one. | **`Domain/AuditRoute.Shape` — three desks as DATA**: تدقيق المهندس المقيم 7 · تدقيق الدائرة المالية 7 · الصرف (قسم الحسابات) 5. The fixture and `EP-FIN-02` both build from it, and the unused `audit-stage/audit` lookup code went with the desk it named. | The fixture's shape is the plate's two audits plus the disbursement act given a desk of its own, which is what مسار 8 steps 5–9 describe and what قسم الحسابات holds the file for — الشكل 16 names that section as the party that recorded the payment. D-03's five days is a DEFAULT and not this route's cap: P-97 recorded `CapDays` per stage precisely because the plate shows seven. A vocabulary lists what exists, so a fourth code no route builds is not a spare, it is a lie about the model. | Resolved |

| P-184 | Two capacities this pass needs had no actor. العرض الفني §7 gives الدائرة المالية «البيانات المالية والتخصيصات والدفعات» and «تدقيق وتسجيل الدفعات والمصروفات، وتحرير البيانات المالية المسجّلة», and الشكل 19's own event is attributed to «محللة موازنة · الدائرة المالية» — but `Personas.All` had no such party, so neither the second and third desks of المسار 8 nor الشكل 18's «تعديل» could belong to anybody. | **`user.finance-dept`** — Party «الدائرة المالية», role محلل موازنة, `MinistryWide`. It carries `CanReleaseAuditDesk` for the `finance` and `disbursement` desks and `CanEditFinancialRecords`. | `MinistryWide` because the directorate audits payments across the portfolio and is a مركز department, which is §7's «اطلاع شامل» — the same reason the two review committees carry it. **`CanEditFinancialRecords` is one party and is narrower than every other edit capacity in the file**: the card is badged «قيم معتمدة من الدائرة المالية», and a value another party could change would not be that. المستخدم المختص still records the planned cost at definition (المسار 1); revising the budget afterwards is a different act on the same column, and separating the two is what §7 asks for. **مدير المشروع is on no desk of the route**: it may raise a certificate against its own contract, and a party that could raise money and then audit its own claim would make the whole route one signature. | Resolved |

| P-185 | العرض الفني §16 puts the US dollar inside PHASE ONE scope beside the dinar — «تُحدَّد عملة العقد عند تعريفه، وتُعرض قيمه ومبالغ إحالته واحتياطه وإشرافه ودفعاته بها», with the symbol printed beside every figure, no cross-currency summation without an approved rate, and the rate «معتمدة رسميًا من الجهة المالية» declared beside any aggregate. Nothing in this build implements it: «د.ع» is hard-coded on every screen. | **Deferred, and recorded rather than quietly dropped.** SCR-W7 stays IQD-only this pass. | It is not a finance-module change. Currency belongs to the CONTRACT, so it reaches the contract tab, the BOQ, the change-order wizard, the schedule's cost impact, every portfolio and workspace roll-up, and every report — and the rule that forbids summing two currencies without a rate would change what a portfolio total MEANS, not just how it is printed. §26 already lists the exchange-rate source as an open dependency on the client («إقرار مصدر سعر الصرف ودوريته من الجهة المالية»), so the rule the aggregates would need is not decided yet either. Folding a cross-cutting change of that size into a finance pass would spread it far outside SCR-W7 and land it on an undecided rate. | **CONFIRM** — is USD in Phase 1 for this prototype, and what is the approved rate source? |

| P-186 | **الشكل 14's Z3 was built with the wrong copied class, and the right one was already in the stylesheet.** The plate frames the whole equation in ONE elevated card, pins it so the five figures stay on screen while the sheet scrolls, and leaves the terms as plain label-over-figure cells. This page reached for `.d-recon`, which does the exact inversion — no frame on the strip, a bordered box around every TERM, and `flex: 1 1 160px` on each, so five terms needed 928px in a 918px column and «المتبقي» wrapped onto a full-width row of its own. Above it sat a five-tile `epm-summary-strip` printing المعدلة, المصروف and المتبقي a second time, forty pixels from the first. | **`.d-fsheet-recon`** — the reference's own class, present in `desktop.css` verbatim and used by nothing — with the reference's own markup: `<em>` label, `.d-money.sm` figure, the percentage inside the label as `<i class="pc">`, the sign on the NUMBER (`.up`/`.down`) with a fixed operator, and «المتبقي» as `.tm.good`/`.tm.bad` rather than `.strong`. The summary strip is replaced by the plate's **Z10 status bar** (`.d-pz10` — also copied, also unused until now). | This is CLAUDE.md §1's own warning one class further on: *"PAGE-01 … reached for `.d-search` instead of `.d-field` and `.d-secnav` instead of `.d-fchip`"*. Nothing needed writing — both rules were copied from the reference at Phase 1.5 and the template simply never called them, which is why `grep` before writing a rule is the instruction and why an unused copied class is a signal rather than dead weight. **Z10 is not a smaller summary strip**, it is where the plate puts these figures: a 28px band of label-and-figure pairs on the FRAME, not a card band in the content. It also solves what the strip was for — مصادق ولم يُصرَف and الضمانات المحتجزة are P-26's two balances and have no place in the equation, and they sit on that line without becoming a second headline. A screen that prints one figure twice, close together, asks the reader to check whether the two copies agree; that is the failure P-54 names between screens, and it is no better within one. | Resolved |

| P-187 | **Z6 had the same problem as Z3, on the same screen.** `DModuleFrame` draws `title + unit · toolbar · spacer · actions`; this page passed «د.ع · حتى 2026-08-02» as the subtitle where the reference passes «د.ع» alone, built the year filter as a `.grp` of `.d-fchip` pills and put it in the ACTIONS slot so it sat at the far end, carried no «تصدير» at all, and hung a two-line capacity-denial sentence across the band on all six tabs. | **`.d-yearsel` in the `[scope]` slot** — the reference's own segmented control, copied into `desktop.css` at Phase 1.5 and called by nothing, with an `<em>` legend and the selected year raised on `--surface`. It renders on the four year-scoped tabs only, exactly as the reference gates it (`sheet · alloc · pays · hist`). **«تصدير»** is Z6's one permanent action. **«تعديل» and «تسجيل دفعة» both moved onto البيانات المسجّلة**, which is where الشكل 18 lists them — «أزرار: تعديل · تسجيل دفعة · تصدير». The as-of date left the subtitle for Z10, where P-186 had just put it. | `.d-fchip` is the FILTER control and reads as four loose toggles rather than one control with one answer; the year is a single-choice segmented group and the reference has a class that says exactly that. Putting it in `[actions]` was the deeper error — a toolbar states WHICH SLICE is on screen and belongs against the title, while actions are what you can do to it, which is the distinction الشكل 12 draws for the contract selector and which the `[scope]` slot already existed for (P-178). **The denial sentence was the visible symptom of the wrong home**: «تسجيل دفعة» is not a Z6 action in the reference at all — `desktop-workspace.jsx` raises it as a workspace module action and `DModFinancialNew` listens for the event — so gating it in the band meant five tabs that cannot register anything each explaining why. On its own tab it pairs with «تعديل», and since no persona holds both capacities the card always shows one button and one reason. **Three copied classes for this one screen were sitting unused** — `.d-fsheet-recon`, `.d-pz10`, `.d-yearsel` — which is not three oversights but one: the module was built from the written spec and the plate rather than from the stylesheet the plate had already been translated into. | Resolved |

| P-188 | **SCR-W7 carried an EVM board that the reference does not have.** A PV·EV·AC·CPI·SPI·EAC card set sat under جدول الكلف, added in Phase 4.4 on the reasoning that «الشكل 14's six tabs have no place for the EVM board, and it is financial-position content, so it sits under the sheet it is derived from rather than losing its screen». Grepping `DModFinancialNew` for those tokens returns exactly one hit, and it is a code comment about the per-contract EAC column. | **Removed.** The reference puts the tile set — and its EAC/VAC glossary — in `DModProgress`, which is الشكل 27 «الأثر والكلفة», a tab SCR-W6 already draws. `FinancialsEvm` and the response's `evm` field went with it, along with three orphaned i18n keys. | The premise was wrong rather than the placement: the board had not "lost its screen", it already had one, and this was a second copy of it. What الشكل 14 genuinely takes from BR-11 stays where the plate puts it — «عند الإنجاز» and «الفرق» are per-contract EAC and variance and are COLUMNS of the cost table, not a board beneath it. **Removing it exposed two call sites P-180 had missed**, both printing Σ commitments where the recorded budget belongs: `ProgressEndpoints`' `costImpact` (الشكل 27's «الكلفة المعدلة» and «نسبة الصرف» — the plate prints 1,500,000,000, its own contracts total 2,156,653,454) and `OverviewEndpoints`' `OverviewCost` (الشكل 4's «المقررة … والمعدلة … والمتبقي» and «نسبة الصرف 34% — 510 م من 1,500 م», whose own comment quotes the recorded pair). Both now read `Domain/BudgetBasis`; `OverviewTotals` keeps Σ contract values, because «القيمة النافذة» is the other question and that is where it is answered. All three screens now print 86,700,000 ÷ 350,000,000 = 24.77% from one function, with 351,250,000 visible separately as what the contracts commit. | Resolved |

| P-189 | **Three geometry defects, all found by MEASURING the running screen rather than reading it.** SCR-W7 was signed off on its content and never on its box, and the frame's content column is 968px at a 1440 viewport, not 1440. (1) الشكل 14's cost sheet carried `style="min-width: 1160px"` inline on a plain `.d-tablewrap`, which is `overflow: hidden` — so the floor did not make the sheet scroll, it made the last columns UNREACHABLE. Measured at 1440: «الفرق» spanned x −127…−10 and «الحالة» −213…−127, both entirely outside the wrapper, and «عند الإنجاز» was cut in half. Those are the two columns P-188 had just added. (2) Z10 shipped with FIVE stats, four of them money; `.d-pz10` is `flex-wrap: nowrap`, the row measured 1001px in 968, and «البيانات حتى» was crushed to 32px and clipped. (3) `.epm-f-err` was scoped `.epm-ws-f > small.epm-f-err`, i.e. the create drawer alone, while `shared/field-grid.component.ts` emits the same `<small>` inside `.d-form-i` — so every field-level refusal on SCR-W2, SCR-W3 and ملحق الشكل 18 rendered in body colour at the browser's own 10.83px. | (1) **`.d-vow-tw.wide-fin`** — the reference's own wide-table wrapper (`desktop.css:1903`, «scroll rather than crush»), composed with `.d-tablewrap` so it wins the x axis on source order and leaves the frame, the radius and the y axis alone; the floor moved to `styles.css` because a page template does not author geometry (§3.7). (2) **Z10 cut to the reference's own three** — `DModFinancialNew` project-modules.jsx:1115 passes الكلفة المعدلة · المصروف · نسبة الصرف, and no `DZ10` in the reference carries more than two MONEY stats. مصادق ولم يُصرَف and الضمانات المحتجزة moved to ملحق الشكل 16's register in a `.d-fig-row`, the class the allocation tab already uses. (3) **`small.epm-f-err`**, scoped to the element rather than to one host. | **A column that cannot be scrolled to is not a column**, and the failure was silent in every way a reviewer checks: the text is in the DOM, `get_page_text` prints it, the responsive pass's own test (`document.scrollWidth === clientWidth`) passes precisely BECAUSE `overflow: hidden` clips instead of scrolling. Only `element.scrollWidth > element.clientWidth` on a `hidden` box finds it. **Z10's five was an argument, not a measurement** — the template comment reasoned that «this bar is label-and-figure pairs on one hairline-divided line, so it holds five without becoming a band», which is true of the bar and false of the frame it sits in; the reference's three was the answer all along, and the two orphans landed on the tab whose payments they are drawn from. **The third is the same class of defect as P-41's `<bdi>`**: a correctness bug wearing a styling costume, since a refusal that renders as a hint is a refusal the user does not see. It was never SCR-W7's — the WIP was only the first screen to feed `f.error` — so fixing it fixed three screens. **Nothing smaller than 11px (§6) and status never colour-only (05 §7.6) both held**: the control keeps `.epm-invalid`. | Resolved |

| P-190 | **الشكل 14's cost sheet did not read like `DModFinancialNew`'s, in five ways at once**, and the client showed the prototype's own rendering to say so. (1) The three expense-item rows printed «—» in عند الإنجاز and الفرق, on P-90's reasoning that «apportioning the contract's forecast across three lines would be an allocation rule no document states». (2) The column headings were the long forms — «الكلفة المقررة · تغييرات معتمدة ومنفَّذة · الكلفة المعدلة» — under a group row that already says الموازنة. (3) تغييرات معتمدة printed «—» for zero and a «+» on positives. (4) الفرق was deliberately untoned. (5) مصروف السنة kept naming a year while «كل السنوات» was selected. | (1) **P-90 closed.** Each component now calls the SAME `Domain/EarnedValue.For` the contract row calls, given its OWN budget and its OWN recorded spend at the contract's actual percentage; `Vac` is «الفرق» and nothing is recomputed. (2) Four new short-form keys `fin_col_*`, with the long forms left where they are read without a group heading — Z10, the equation strip, الشكل 18. (3) `paren()` on every figure cell, which is the reference's `N()`: magnitude, negatives in parentheses, no sign prefix. (4) **`.fin-var.neg/.pos`**, the class form of the reference's `tone: true`. (5) `fin_col_spend` on the all-years branch, as `DModFinancialNew` switches it. | **P-90 asked the wrong question.** It read the plate as dividing ONE contract forecast into three lines, which would indeed need a ratio nobody states. The reference does not divide anything: EAC = budget ÷ CPI reduces to spend ÷ actual%, so a line that has its own budget and its own recorded spend has everything BR-11 needs the moment the percentage is known — and the percentage is the contract's because a component has no physical progress of its own, which is the true half of what P-90 observed. Verified on the plate's own figures: الإحالة 1,373,774,835 + الاحتياط 62,867,647 + الإشراف 39,619,294 = 1,476,261,776, the contract row exactly, and the three variances sum to (144,145,977) likewise. **This build now sums the same way** — 126,057,631 + 6,355,847 + 3,001,372 = 135,414,850 on CNT-0279 — so the column adds up on screen, which is the test P-90's «—» could never pass. **The tone is the one deliberate §6 tension** and it is recorded in the class comment rather than buried: «never colour a magnitude by threshold» holds, because الفرق is a SIGNED quantity whose colour restates the sign the parentheses already carry, no cut-off is asserted, nothing is colour-only, and the magnitudes beside it stay `--on-surface`. **Not ported: the reference's `.freeze` sticky first column.** It exists only under `.d-boqgrid` and giving `.d-table` its own would be a parallel set of header/footer/hover/selected background rules; the sheet scrolls (P-189) and the plate the client showed never reaches the width where a frozen column is exercised. | Resolved |

| P-191 | **The column groups were a flat band, not groups**, and the client pointed at the join to say so. الشكل 14's header runs over TWO tiers — الموازنة (3) · الفعلي (2) · التنبؤ (2) over nine columns — and this build drew the second tier with `colspan` on a plain `.d-table`, so the three headings sat on one uninterrupted strip with nothing marking where one group ended and the next began. P-189 had also given the table a `.d-vow-tw.wide-fin` floor of our own so `.d-tablewrap`'s `overflow: hidden` would stop clipping it. | **`.d-line-table.d-sheet` inside `.d-vow-tw.wide-sheet`**, which is what `DModFinancialNew` project-modules.jsx:1145 writes, wrapped in the `DFGroup` the reference gives it («جدول الكلف حسب العقود ومكوّناتها · د.ع · 2026»). `th.grp` takes `--surface-container-high` and a `border-inline-start` hairline; `.freeze` on the identity column of every row, header and footer. `wide-fin` deleted — `wide-sheet` already carries `min-width: 1250px` — and `.fin-var` retargeted. The one rule left is the freeze column's 230px, which the reference passes inline and §3.7 keeps out of a template. | **A fourth copied-but-uncalled class on this one screen**, after `.d-fsheet-recon`, `.d-pz10` and `.d-yearsel` (P-186 · P-187), and the same root cause each time: the module was built from the plate rather than from the stylesheet the plate had already been translated into. `.d-sheet` is 20 lines of `desktop.css` that had never been referenced, and it is not a skin — the group plane and its hairline ARE the grouping, which is the defect the client saw. **It also settles two things P-189 and P-190 had each left open.** P-189 invented `wide-fin` to make the sheet scroll, when `wide-sheet` existed for exactly this table; a local fix for a symptom the reference had already answered is the same mistake in miniature. P-190 declined to port the `.freeze` sticky column on the grounds that it lived only under `.d-boqgrid` and `.d-table` would need a parallel rule set — true of `.d-table`, and irrelevant once the table is the `.d-sheet` it should always have been, which brings `th.freeze`, `td.freeze`, both header tiers pinned and a sticky totals row with it. **The lesson is P-186's, restated: grep the stylesheet before writing a rule** — CLAUDE.md §3.7 says so in as many words, and three phases of this screen have now been corrected by obeying it. | Resolved |

| P-192 | **ملحق الشكل 21 is NAMED for a panel this screen refused to draw** — «الجدول الزمني — جانت مع لوحة تحديث إنجاز النشاط» — and lists «تحديث نسبة إنجاز النشاط بشريط التمرير وحفظ التحديث» among its actions, on المسار 6. SCR-W5 printed a sentence in its place redirecting to SCR-W6, which is P-55's «SCR-W5 refuses the same edit». | **The panel is built.** A slider over a LOCAL draft, «المتبقي» recomputed live from `PlannedProgress.RemainingDuration`'s own rule, cancel/save, and الشكل 21's own rollup sentence beneath it. Save calls **`EP-PRG-02`** — SCR-W6's existing route and its existing client, injected rather than reimplemented — then re-reads the schedule. Beside it: «أثر التسليم على إنجاز المشروع» and «الأثر المالي» on a late critical activity, and كلفة النشاط · القيمة المكتسبة · المتبقي المالي, all three computed server-side because §3.1 gives Angular no arithmetic. A MILESTONE gets no slider: `EP-PRG-02` accepts only 0 or 100 on one, and offering 47 to have it refused inverts §6's «prevent invalid input». | **P-55 was decided against the reference and is now decided against the client's own two documents.** Its reasoning — an editor here «would move a number whose consequence is off screen» — is exactly what the annex answers: «تجعل تحديث الإنجاز فعلًا واعيًا بأثره: يرى المستخدم قبل الحفظ أثر التسليم على تاريخ المشروع وعلى القيمة المكتسبة، لا مجرد رقم يُدخله». The consequence is not off screen; the panel IS the consequence, which is why the draft is local and only the button writes. **Verified against the running API**: A5 60% → 72% moved المتبقي 48 → 34, which is the figure the panel had already previewed, القيمة المكتسبة 8,352,000 → 10,022,400, and the project roll-up 55.06% → 55.76% in the headline strip above. **One write path, two entry points** — `grep EP-PRG-02` still returns a single client method, so the two screens cannot compute progress differently. | Resolved |

| P-193 | **Four smaller divergences in Z6 and in الشكل 23's figure row.** The toolbar's selects were `.boq-ctxsel` where `DModSchedule` writes `.d-ctxsel`; the weight basis was a segmented `.boq-basis` shown ONLY when the file carried man-hours; the WBS level filter appeared on جانت alone; and الشكل 23's four figures read أنشطة متأثرة · **أكبر انزياح** · **منها على المسار الحرج** · أثر الكلفة where the plate reads أنشطة متأثرة · **مضافة** · **أصبحت حرجة** · أثر الكلفة. | `.d-ctxsel` on all four controls, so Z6 speaks one vocabulary. The basis is a select like its neighbours and is **always present, disabled with its reason** when the import carried no man-hours. The level filter follows the reference's own gate (`view !== 'compare'`), so الشكل 22's table collapses with the chart. «مضافة» replaces «أكبر انزياح» and reads `impactSummary.addedCount`; «أصبحت حرجة» replaces «منها على المسار الحرج». الشكل 22's inline `min-width: 1180px` moved to `.d-vow-tw.wide-sched`. | Hiding the basis was the substantive one: **§13-1 requires the basis in force to be DECLARED wherever weights are shown** — «ويُعلَن الأساس المستخدم في كل عرض» — and hiding the control made it invisible on exactly the imports that had no choice to make, which is the case where a reader most needs telling. Disabled-with-a-reason states the fact and refuses the change in one control. **«أكبر انزياح» was ours and the worst slip already opens the sorted list beneath it**, so the tile spent the plate's slot restating its own first row while «مضافة» — how many activities the baseline does not contain — was stated nowhere. The 1180 floor is P-191's rule a second time: the reference authors geometry inside the component and CLAUDE.md §3.7 does not. | Resolved |

| P-194 | **«أصبحت حرجة» cannot be derived from what this build stores, and the figure that replaced «منها على المسار الحرج» inherits the gap.** الشكل 23 asks which activities the slip PUT on the critical path. `ScheduleImpact.FloatBefore` is the plate's own arithmetic — ملحق الشكل 23 works A2 at «الأساس … عوم 7 · الحالي … عوم 2» over a 5-day slip, i.e. before = after + slip — but under it an activity that was ALREADY critical also reads as newly critical, because zero float plus a slip is still a positive "before". | **Counted as `IsCritical && FloatBefore > 0` and the limit STATED in the endpoint**, not papered over: the figure means «critical and slipped» until the baseline's own float is recorded. Recorded here and in `TODO.md` for the client rather than silently approximated. | The honest fix is a column, and it belongs to an act this build already has a name for: المسار 4 step 8 is «تثبيت خط الأساس وحفظ الإصدار», and float is exactly what fixing a baseline should freeze. `EP-SCD-06` deliberately does **not** write float — its own comment says criticality belongs to the schedule in force and a re-baseline is not a reset — so adding `Activities.BaselineFloat` is a change to what baselining MEANS, which is the client's call and not a detail to slip into a fidelity pass. **The reference does not derive this either**: `sd.comparison.nowCritical` is fixture data, so the prototype can print the number without being able to compute it. Noting that is the difference between matching a screen and matching a system. | **CONFIRM** |

| P-195 | **The pinned activity-name column had nothing behind it, so the Gantt bars scrolled visibly through the activity names.** Reported from a screenshot: green bars drawn across «التهيئة وتسوية الموقع» and «أعمال الحفر والردم». `.d-gantt-cellname` is `position: sticky` with `background: inherit` (desktop.css:615), and `.d-gantt-row` sets a background only for `:hover`, `.on`, `.wbs` and `.head` — so on a plain ACTIVITY row `inherit` resolves to TRANSPARENT and the track slides under it in plain sight. | **`.d-gantt-row` given the opaque base `background: inherit` was written to find**, and `.on` moved from mixing its tint over `transparent` to mixing it over `--surface-container-lowest`, which is what the reference already does for `.d-gantt-cellinfo` one declaration away. | **The bug is the reference's, and it arrived with one of its own fixes.** desktop.css:1604's «20b» comment says it: «the info columns join the scrolling region; only the activity name stays pinned. Previously both were sticky, which pushed the chart track outside the pane entirely below 1024px». Until then `.d-gantt-cellinfo` sat pinned beside the name on an opaque `--surface-container-lowest` and hid everything passing underneath; making it static fixed the layout and left the name column as the only pinned thing, with nothing painted behind it. **Fixing the ROW rather than the cell is what keeps the four states intact** — a hard-set colour on `.d-gantt-cellname` would have out-specified `.wbs` and `.head` and stopped the name column tinting on hover with the row it belongs to. Only visible while the chart is scrolled far enough for a bar to reach the pinned column, which is why it survived the responsive pass: nothing measures a scrolled state. | Resolved |

| P-196 | **الشكل 21 lists «تصدير واستيراد P6» as one pair and this page carried only the second half, under a name that did not say what it was.** The button read «استيراد» — no mention of Primavera, though the wizard behind it reads XER, P6 XML and Excel — and there was no export at all, on either reading view. The first view tab read «المخطط الزمني», which restates the module title directly above it. | «تصدير» added beside it, writing `DModSchedule.exportSchedule`'s own ten columns for the ACTIVITY rows; the import button is **«استيراد P6»**; the tab is **«جانت»** with the reference's `calendar_view_week` icon. The CSV writer both exports now share was extracted rather than copied. | The import was BUILT and invisible: ملحق الشكل 24's five steps, its three formats and its two weight bases were all there behind a button whose label named none of them, which is why it read as missing. Naming the tool on the control is the whole fix. **«جانت» is the reference's VTABS label and the plate's own breadcrumb** — «مساحة المشروع › الجدول الزمني › جانت» — and «المخطط الزمني» spent the tab restating the module. The export exports ACTIVITIES only: a WBS node is a roll-up of the rows beneath it, so a file carrying both would double every figure in it. | Resolved |

| P-197 | **SCR-W5's six dropdowns were native `<select>`s, and two of them wore `class="epm-select"` while still opening the operating system's list.** `shared/select.component.ts` calls itself «THE APP'S ONE DROPDOWN» and states the reason: a native list is drawn by the OS, so «our font, our spacing, our RTL and our dark theme all stop at the popup's edge, and on Windows it arrives as a grey system list in the middle of an Arabic form». Z6's four (خط الأساس · المستوى · أساس الوزن · العقد) and ملحق الشكل 24's two (الصيغة · أساس الوزن) were all native. | **All six are `<epm-select>`.** Z6's four take a new **`bare`** input that drops the trigger's border, height and plane so it sits inside `.d-ctxsel`, the toolbar pill that already draws them — the popup, the roving focus and the type-ahead are untouched, which is the whole reason to reach for the component here. The wizard's two take the default `.d-form-input` trigger, since they sit in a `.d-form-grid` where that IS the control vocabulary. | **A `.d-form-input` inside `.d-ctxsel` would have been a box in a box** — §6's «sections are label + space, never nested boxes» — so the honest options were to drop the reference's toolbar pill or to give the trigger a frameless variant. `bare` keeps the reference's Z6 geometry exactly (a 20px trigger inside a 31px pill, against the 32px the framed trigger forced) while the list stops being the OS's, and it is opt-in, so every existing caller is untouched. The focus ring moves to the PILL: a ring drawn tight around bare text inside a bordered pill reads as a second control. **The two in the wizard were the worse case** — they carried the shared control's class without its behaviour, so a grey system popup in an RTL modal looked like a deliberate design rather than a native fallback. `<option>` elements were the last place in this feature where a label lived in the template; the lists are now `{code,label}` projections beside the signals they feed. | Resolved |

| P-198 | **«مرجع المقارنة» was absent from SCR-W6, and the drawer explained the absence with a rule about the wrong control.** All four of الأشكال 25–28 name «مرشح مرجع المقارنة» in «الوظائف والخصائص الظاهرة» and again in «الإجراءات المتاحة للمستخدم» as «تغيير مرجع المقارنة». `prg_how_base_b` said there was none because this build has exactly one baseline, so «لن يكون خلفه ما يُقارَن». | **Built, as what it actually is.** `DModProgress` :1416 states it in its own comment: the control «picks which earlier READING each tile compares against». So it is a PERIOD selector over the recorded progress history — القراءة السابقة · الربع الماضي · بداية المشروع — and this build already had that history. `Domain/ComparisonPeriod` resolves all three spans; `EP-PRG-01` sends them resolved; the Z6 `.d-ctxsel` picks one and every tile's delta follows it. | **The omission was of a control nobody had described.** The baseline reasoning was true and is still in the drawer under its own heading — it was attached to the wrong question. **Both ends of every subtraction now come from ONE series**, which the reference cannot do: its two series are generated independently, so `DModProgress` :1432 SCALES the financial delta onto the figure the tile shows, and that hack is visible in الشكل 25 itself — its own history says 38 − 29 = 9 and the plate prints +13. Here physical reads `ProgressSeries.ActualAt` (the function SCR-W1's curve is drawn from) and financial reads Σ paid ÷ revised at the same two dates (the identical derivation the headline uses), so no rescaling is possible. **A span the record cannot support is OFFERED AND REFUSED** — disabled, carrying its reason — per §6, and `SelectOption` gained `disabled`/`why` for it. The DEFAULT falls back to a span that resolves (`DefaultFor`), because opening on «القراءة السابقة» with one reading on file put «لا قراءة سابقة تصلح للمقارنة» on every tile while بداية المشروع sat one click away. | Resolved |

| P-199 | **SCR-W6 was not built on the archetype its own plates draw.** الأشكال 25–28 are `.d-tile` KPI cards over a `.d-l04` twelve-column grid — four, three, six and four of them — and this screen used `<epm-summary-strip>`, `.d-recon` and an eight-item `.d-form-grid` instead. The whole L04 vocabulary had been in `styles/desktop.css:3395–3480` since Phase 1 and **no screen in the build had ever used it.** | `shared/tile.component.ts` (`<epm-tile>`), and all four tabs lead with a `.d-l04` grid of them. `Domain/TileThreshold` resolves the band on each. **No CSS was added** beyond one host rule. | **The drill-through is the part that was FUNCTIONALLY missing**, not the styling: all four plates list «الانتقال إلى الوحدة المصدر لكل مؤشر» among their user actions, and the screen offered no way off itself. A strip also has nowhere to put a comparison, a delta or a governing note — so الشكل 27's «الكلفة المقررة» under «الكلفة المعدلة» had no home and was not sent at all. Same substitution P-186 found on SCR-W7. **The bands went to `Domain/`, not the template**: «more than five points behind plan is bad» is a judgement about ministry projects and §3.1 leaves Angular only display formatting — `ExecutiveSignal` set that precedent for the same kind of figure (P-136), and the sixteen thresholds `DModProgress` computes inline are now named constants with 21 worked examples read off the plates. A tile holding a MAGNITUDE takes no band at all. **One host rule was needed**: `<epm-tile>` sat between `.d-l04` and `.d-tile`, so each card took one of twelve columns and its own `span 3/6/12` — every reflow breakpoint included — addressed an element that was no longer a grid item. `epm-tile { display: contents }`, the same trap already documented for `epm-app-footer`. | Resolved |

| P-200 | **The reference and ملحق الشكل 25 disagree about the progress curve, and only about that.** `DModProgress` :1590 draws a span-6 `DSCurve` on الملخص titled «منحنى الإنجاز — المخطط مقابل الفعلي (تراكمي)». The plate's screenshot shows the four tiles and then the updates section, with no curve between them. | **Drawn.** The checked-in `project-modules.jsx` was diffed against the live prototype before this pass and `DModProgress` is byte-identical, so the live app draws it today and the plate's screenshot predates the tile. | The two sources are read the way CLAUDE.md §1 sets out — the written spec gives the rules, the reference gives the screen — and the annex plate is a screenshot of a build, not a rule. It costs **no new derivation**: `ProgressSeries.Monthly` is the function SCR-W1's own curve comes from, over the series the comparison periods above already build, so the two screens cannot draw different lines for one project. The reference builds its curve from `f => f * f * (3 - 2 * f)` over eight invented periods; nothing here is a shape. **An undrawable series comes back EMPTY and the tile does not render** — `Drawable`'s two conditions, and the client's own answer to a chart with no data (P-144). Recorded here rather than decided silently, because it is the one place the two sources of truth for this screen do not agree. | Resolved |

| P-201 | **The first L04 pass matched the archetype but not the plate.** Driving the live prototype's own الإنجاز beside this build — `PRJ-0170`, the project ملحق الشكل 25 was captured from — turned up nine differences, none of them cosmetic in aggregate: the sections sat OUTSIDE the tile grid as `.d-sec`; «تحديثات الإنجاز» printed one row per contract EVENT (التاريخ · العقد · من ← إلى · المُسجِّل · القسم المصدر) where the plate prints one row per project READING (التاريخ · المادي · المالي · المصدر · المستخدم); the labels beside figures were the long forms; «حسب هيكل التجزئة» carried a count badge; the spend note dropped its currency; and الأثر والكلفة carried a message bar and a table restating its own six cards. | All nine matched. The three sections are `<epm-field-group>` INSIDE `.d-l04` with `.gf` footers, the updates table is the reading series, the short labels are `prg_*_short`, only «مخاطر الجدول» keeps a badge, the note carries د.ع on both amounts, and الأثر والكلفة is six cards and nothing else. | **The archetype was right and the content was not**, which is the harder half to see from a screenshot: `.d-tile` and `.d-l04` were in place, so the page LOOKED like the plate while its most-read table answered a different question. The updates table is the substantive one — it is now the same series `ComparisonPeriod` measures deltas against and `ProgressSeries` draws the curve from, so one source is read three ways on one screen instead of two sources disagreeing. **المصدر is the recorded `ActorParty`**, not the reference's `i % 2` alternation between «الجدول الزمني» and «الموقف المالي», which is a label chosen by row position. **A badge on a tab means "this many things need you"** — six WBS levels is the shape of the programme, so that badge was making a structural fact look like a queue. **Two additions are kept and stated**: the EVM working (PV · EV · AC · budget), because `05 §7.9` wants an index checkable and the four ratios are on tiles; and «الإنجاز حسب العقود», because the reference's project has ONE contract and this build's has several, so the headline would otherwise be untakeable-apart. Both sit AFTER the plate's own content, in the plate's own card. | Resolved |

| P-202 | **`display: contents` fixed the tiles and did not fix the sections.** `epm-tile { display: contents }` let `.d-tile`'s own `span 3/6/12` reach the grid. The same rule on `<epm-field-group>` left every section one column wide and 1178px tall. | `.d-l04 > epm-field-group > .d-fgroup { grid-column: span 12 }`, with the host spelled out. | `display: contents` removes an element's **box**, not the element from the **selector tree**. `.d-tile` carries its span as an UNSCOPED base rule, so it matched wherever the card ended up; `.d-l04 > .d-fgroup` is child-scoped and still saw `epm-field-group` as the child, so it never matched. The two behaved differently for a reason that is invisible in either stylesheet on its own, and it is worth knowing before the next component is dropped into a grid: **`display: contents` is a layout fix, never a selector fix.** | Resolved |

| P-203 | **الشكل 26 and الشكل 28 were checked against the running prototype and five strings were wrong** — not layout this time, wording. «الإنجاز التجميعي» was missing «للجدول», the word that separates the SCHEDULE rollup from the BOQ-derived headline one tab away; «الأنشطة المعرّضة للخطر» had its ordering baked into the title and its SUB was the threshold, which the card above already states; and the empty state read «لا نشاط تجاوز الحد» where the plate says «لا أنشطة معرّضة للخطر» and names the critical path in its body. | All five matched to the prototype's own strings, including its orthography («صعوداً», «يُدخَل»). The section subtitle is now «مرتّبة بحسب الانزياح», and the threshold appears once, on the tile that filtered the list. | The tiles and the grid were already right, so these read as trivia until you notice what each one costs. **«للجدول» is the whole distinction** between two figures a reader sees within one click of each other and would otherwise take for the same number reported twice. **A threshold stated in two places can drift**, and the one on the card is the one the list was actually filtered by. And an empty state that says «لا نشاط تجاوز الحد» answers a narrower question than the list asks — the plate's own wording covers both halves of the rule, the threshold AND the critical path. | Resolved |

| P-204 | **A UI/UX pass over SCR-W6 against WCAG 2.2 AA found two real defects and one that is not this screen's to fix.** Run with the `ui-ux-pro-max` rule set the client named, applied as a checklist over the web-relevant CRITICAL categories — its own scope notice sends native/mobile rules elsewhere, and CLAUDE.md §6 and `05 §7` govern wherever the two disagree (type scale, no shadows, no letter-spacing). | **Fixed:** the `bare` select trigger was a 94×20 pointer target — WCAG 2.2 AA 2.5.8 puts a web target at 24×24 CSS px — and `min-height: 24px` grows the hit box inside the 34px pill without moving anything. `.d-fgroup > .gh` is a `role="button"` with `tabindex="0"` and Enter/Space bound and had **no focus indicator at all**, on every screen that uses `<epm-field-group>`. **Recorded, not fixed:** `--fg-subtle` measures **3.07:1** in light and 4.44:1 in dark against 12px text. | **Checked and already sound**, so no change: icons emit `aria-hidden` from `icon.component.ts`; `prefers-reduced-motion` is handled in the stylesheet; every tile state carries an `.sr` phrase, so nothing is colour-only; and Z10 is `position: static` in its own flex row rather than an overlay, so a focused control at the foot of the scroll box cannot be hidden behind it (WCAG 2.2 `focus-not-obscured`). **The contrast finding is a one-token, 114-reference change** to a palette copied verbatim from the reference — it repaints the caption layer of every screen, which is very likely right and is still the client's call, so it is in `TODO.md` with the measurement and two candidate values rather than applied here. | Resolved |
