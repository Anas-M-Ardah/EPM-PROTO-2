# DECISIONS — business-rule questions resolved in the prototype

One entry per decision so the final build inherits the reasoning, not just the code.
Mirrors the handoff's open questions (`07-POC-BUILD-PLAN.md` §9). Items marked **CONFIRM** still need client sign-off before production.

| # | Question | Prototype decision | Source | Status |
|---|---|---|---|---|
| D-01 | Rate bands after several orders — does the second order's 20% threshold use the *original* or *current effective* quantity? | Uses the **original** contract quantity (`line.contractedQty`). | 07 §9.1 | **CONFIRM** |
| D-02 | Delay penalty rate | **0.1%/day, capped at 10%** of the effective contract value (`configuration.rules.penaltyPerDayPct/penaltyCapPct`). | 02 §10 | **CONFIRM** against contract template |
| D-03 | Per-stage SLA | Uniform **5 days** (`configuration.rules.slaDaysPerStage`); a stage past SLA is `overdue`, raises needs-action, and escalates. | 02 §12 | **CONFIRM** per committee |
| D-04 | Delegate authority to cancel | The rapporteur may record a cancellation only for external parties flagged `canCancel: true` (Endorsement Review Cmte, Admin & Financial Dept). | 03 §4 | **CONFIRM** |
| D-05 | Distribution vs. decrease | When a decrease would push distributed total above the revised quantity, the order **cannot be applied** until the distribution is revised. Revising party not yet fixed. | 02 §8 | **CONFIRM** (RE dept vs beneficiary) |
| D-06 | "Now" reference | Derived from the **project data date** (`configuration.time.dataDate = 2026-08-02`) in demo mode; real clock in production. Never `Date.now()`. | CLAUDE.md, 06 §12 | Resolved |
| D-07 | BOQ weight rounding | **Largest-remainder** to exactly 100.00%; `toFixed(2)` is a bug. Denominator is the **contract's** BOQ rows. | 02 §1 | Resolved |
| D-08 | Approved value authority | The **approved value** comes only from the pricing/rate-fixing committee, entered at financial review. The RE-dept proposal governs display; the contractor proposal is shown labelled until then. | 02 §6 | Resolved |
| D-09 | Approved ≠ Applied ≠ Closed | Approving changes nothing. Applying creates a contract amendment and moves quantities/dates/penalty baseline. Closing verifies the application. Approved-unapplied orders shown as a **projection**, never folded into effective figures. | 03 §6 | Resolved |
| D-10 | External parties | Statuses inside the owning stage (recorded by a delegate against an official letter number+date), **not** workflow stages. Attribution: deciding party, delegate as recorder. | 03 §3–§4 | Resolved |
| D-11 | Money type | Displayed as integer IQD; production must use `decimal`/integer minor units — never `float`. Tier splits and largest-remainder must be exact. | 07 §2 | Resolved (prototype), enforce in prod |
| D-12 | Contract scoping | A BOQ item / activity belongs to exactly one contract; the project is derived from the contract. One order may not span two contracts (validation gate `cross-contract`). | 01, 02 §7 | Resolved |

## Assumptions made to fill gaps

- **Seed figures.** Concrete BOQ/activity amounts were chosen to be internally consistent (`amount = qty × rate`) and to exercise the rules; they are illustrative, not real ministry data. The documented worked examples (56.13%/43.87%, the qty-100-add-30 tier split) run in the **Rules Reference** route from their own example inputs, independent of the seed.
- **Man-hours basis** is stubbed to fall back to `budgetedCost` where P6 man-hours are absent (the cost/man-hours toggle is present in the BOQ activity-assignment view).
- **3D/BIM viewer, standalone Claims, Quality (NCR/Lab), HSE, GIS** are out of Phase 1 (07 §8) — tabs are kept and stubbed.

---

# Full-stack port decisions (Angular + .NET + SQL Server)

Decisions taken while porting the React prototype. One entry per decision, with the reasoning,
so the next person inherits the *why* and not just the code.

| # | Question | Decision | Rationale | Status |
|---|---|---|---|---|
| P-01 | Storage shape | **Flat tables, plain ID columns.** No navigation properties, no foreign keys, no cascade rules, no indexes. | A normalised EF model needed ten `OnDelete(Restrict)` overrides purely to satisfy SQL Server's multiple-cascade-path rule — pure ceremony, first thing a reader hits. Relationships now live in the endpoint's `Where()` clause where they can be read. | Resolved |
| P-02 | Schema evolution | **No migrations.** `EnsureCreated()` on boot; `POST /api/dev/reset` to apply a change. | Migration files are the top merge-conflict source for parallel agents, and there is no production data to preserve. Cost: reset wipes anything typed in. | Resolved |
| P-03 | Seeding | **Nothing on boot.** The `06 §12` fixture loads only via `POST /api/dev/load-fixture`. | The reference figures are illustrative, not ministry data — the original `DECISIONS.md` says so itself. Making them opt-in stops invented numbers being mistaken for real ones. | Resolved |
| P-04 | Test isolation | Domain rule tests use **inline worked examples** from `02-BUSINESS-RULES.md` and never read the database. | If a fixture figure turns out wrong, no test starts lying. The spec, not the data, is the oracle. | Resolved |
| P-05 | Identity | **Persona header (`X-Epm-User`), no authentication.** | The permission *model* from `03 §7` is real and resolved server-side; only the identity is fake. `03 §7` itself asks for the persona switcher as the fastest way to review the model. | Resolved |
| P-06 | Build unit | **One page at a time, end to end** — table, endpoint, Angular trio, UML, TRACE row. | Building 30 tables up front from a spec was speculative: columns existed for screens nobody had built. Page-at-a-time means no column exists on a guess. | Resolved |
| P-07 | Design fidelity | **Copy the stylesheets verbatim** (2,947 lines) rather than re-derive tokens. | The reference JSX uses `className` strings, so porting a screen is mechanical and the design *matches* rather than approximates. | Resolved |
| P-08 | Canonical status keys vs reference CSS | API speaks `06 §1` canonical keys (`delayed`, `cancelled`); a `statusClass()` map translates to the stylesheet's older `stalled` / `withdrawn` classes. | The spec keys are correct and must not be bent to the CSS; editing the verbatim stylesheet would break the copied-not-re-derived guarantee. | Resolved |
| P-09 | Derived values not yet derivable | Return **null and render an em dash**, never a zero. | Physical % is weight-rolled BOQ progress (BR-04). Until the BOQ page exists it cannot be derived, and a 0% bar asserts something false. | Resolved |
| P-10 | Reference component vs written spec | **Open the reference component before writing a page.** | PAGE-01 was built from the written spec alone and had to be redone: it invented a column set, used the wrong CSS classes, and missed that the Workspace column is hidden when workspace-scoped. The spec gives the rules; the component gives the screen. | Resolved |
| P-11 | Where the value lists live | **`Features/Lookups/LookupCatalog.cs`**, called by `Fixture.Load()` — not inline in `Fixture.cs`. | `Fixture.cs` is illustrative demo data and says so at the top. The `06 §1–§11` lists are the opposite: they are the specification's own enumerations, every stored code comes from one of them, and a wrong row is a wrong label on every screen. Putting them beside their endpoint keeps one grep for `boq-change-type` finding the list, the endpoint and the Angular caller. They still load only via `POST /api/dev/load-fixture`, because nothing is seeded on boot (P-03). | Resolved |
| P-12 | Which execution-stage list | The **12 construction stages of `06 §2`** (`design` … `handover`), which are the codes the fixture already stores. | `06 §2` names 12 construction-progress stages *and* says to carry the client's Arabic wording from `reference/app/data.jsx`. But `data.jsx`'s `PROJECT_STAGES` is a different list — an administrative lifecycle (دراسة · إعلان وإحالة · سحب عمل · تسوية حسابات) with no correspondence to the construction stages. The two cannot both be `execution-stage`. `06 §2`'s own enumeration wins because `Projects.ExecutionStage` already stores its codes; the Arabic wording is ours. | **CONFIRM** — which list the client means, and whether the administrative one is a second field |
| P-13 | `co-lifecycle` is missing two states | Added **`approved`** and **`cancelled`** to the `06 §7` list of six. | `03 §6`'s lifecycle diagram requires `approved` — the state whose entire purpose is *the values were agreed and **the contract has not changed*** (non-negotiable #2, D-09). Without it, approved-≠-applied cannot be represented at all. `03 §5`'s fourth decision (`cancel`, used when an external party rejects) terminates an order, which needs a terminal state distinct from `rejected`. `06 §7` lists neither; this is a gap in the data dictionary, not a design change. | **CONFIRM** the two Arabic labels |
| P-20 | v1.1 declares Inter but never loads it | **We load Inter.** | `tokens.css` sets `--font-en: 'Inter', system-ui, sans-serif`, but the branch's `index.html` loads **Roboto** and no Inter at all — so its Latin text silently falls back to `system-ui` and the token is decorative. Loading the font the tokens actually name is the fix; the alternative (change the token to Roboto) would be re-deriving the design. A defect in the branch, worth reporting upstream. | Resolved |
| P-21 | v1.1 breaks the binding accessibility contract on two tokens | **Corrected on adoption** — `--outline` and `--viz-base` to `#858E9C` (3.31:1) light and `#6B7484` (3.43:1) dark, overridden in `web/src/styles.css`. | v1.1 ships both at `#A9B1BC` = **2.16:1** on white; `05 §7.1` requires ≥3:1 for borders, icons and graphics, and the palette they replaced sat at 3.03:1 — the old values were chosen to pass. Dark theme was worse: `#454C57` = 1.87:1. These are not decorative: `05 §1` defines `--outline` as "borders and graphics only" and `04 §5` uses `--viz-base` for the Gantt data-date line and bar edges, so WCAG 1.4.11 applies. The chosen values are the **lightest** on v1.1's own neutral ramp that clear the floor, so the intended look survives as far as the contract allows. Overridden rather than edited into `tokens.css` so the copied sheets stay verbatim (P-07). `--outline-variant` / `--stroke-default` left alone — decorative separators are exempt. | **REVERTED by client decision — known defect** |
| P-22 | `--tertiary` is now the same blue as `--status-ongoing-tx` | **Flagged, not resolved.** | v1.1 sets `--tertiary: #1748B0`, identical to the `ongoing` status text colour; the Redwood red it used to carry is gone from the palette. `05 §7.5` forbids one colour carrying two meanings, so using `--tertiary` as a decorative accent anywhere would read as *ongoing*. Nothing in the one screen built today uses it that way, so nothing is broken yet — but every Phase 2 screen could introduce it. | **CONFIRM** — audit `--tertiary` usages before Phase 2 fans out |
| P-17 | The copied stylesheet pins the summary strip, `05 §8` forbids it | **`.d-grid.stats.fit` in `src/styles.css`** overrides only the column count to `repeat(auto-fit, minmax(120px,1fr))`. | `desktop.css` pins `.d-grid.stats` to `repeat(4, 1fr)` and `.d-grid.stats5` to `repeat(5, 1fr)`, with media queries pinning 3 and 2 below — while `05 §8` and CLAUDE.md §6 both require auto-fit. Measured in the browser: 5 stats under the pinned rule lay out **2 + 2 + 1**, the ballooned-orphan defect §8 was written about; under `.fit` they lay out as 5 equal tracks. Overriding one property in the additions file keeps every other `.d-grid.stats .d-stat` rule — hover, watermark, staggered reveal, reduced-motion — and leaves the verbatim copy intact (P-07). | Resolved |
| P-18 | Field grid: reference is 2 pinned columns, ROADMAP asked for auto-fill | **Kept the reference's `.d-form-grid`** (2 columns, collapsing to 1 at ≤900px). | ROADMAP 1.3 specified `repeat(auto-fill, minmax(240px,1fr))`, but nothing in `05` requires it — §8's grid rule is about *figure strips*, not field grids. The reference's cell borders depend on the 2-column count (`.d-form-i:nth-child(2n)` drops the inline-end border, `:nth-last-child(2):nth-child(2n+1)` the last row's bottom), so auto-fill breaks them, and the reference never renders more than two columns. CLAUDE.md §1 is explicit that the reference component gives the screen. | **CONFIRM** — if wide workspace panes should show 3–4 field columns, the border rules need rewriting too |
| P-19 | A CSS syntax error inside the "verbatim" stylesheet | **Fixed in `web/src/styles/desktop.css:739`**; the reference copies are left untouched. | The base prototype's own `styles/desktop.css:739` ends `.d-modnav` with `scrollbar-width: thin; }; }` — a stray `; }`. Browser error recovery skips it, so the reference prototype renders correctly and nobody noticed; Angular's production CSS parser rejects it and **`ng build` failed outright**. Removing the stray tokens changes no declaration and no rendering, so this is a syntax fix, not a re-derivation of the design. Marked in place with a comment. **The same typo is still in `../epm/styles/desktop.css` and in `docs/spec/reference/styles/desktop.css`.** | Resolved |
| P-15 | `02 §3`'s assigned amounts do not match its own rule | **The rule is binding; the prose figures are an illustration.** `Allocation.Shares([5.8, 5.2], 26,730,000)` gives **14,094,000 / 12,636,000**; `02 §3` states 14,092,710 / 12,637,290. | The spec quotes the weights rounded to 1dp (5.8 / 5.2) but computed the amounts from the UNROUNDED underlying weights — `02 §4` says exactly this about its own example ("client's figure 14,057,931 — same rule, unrounded shares"). The delta is 1,290 IQD on a 26.7M line, 0.005%. Bending the arithmetic to match the prose would break the rule for every other input. `AllocationTests` asserts both the rule's answer and the divergence, so nobody "fixes" it later. **ROADMAP 4.2's exit criterion quotes the prose figures and has been annotated.** | Resolved |
| P-16 | Is amendment no. 0 `original` or `superseded` once amendment 1 is applied? | **Always `original`.** `superseded` means an earlier applied AMENDMENT (no ≥ 1). | `02 §9` words the list as "original (no. 0) · superseded (an earlier applied one) · effective (the last applied)" — `original` is keyed to the number, so it is the row's identity, and the chain shows it as العقد الأصلي whether or not it is still in force. The "which version is in force" question is answered by `Amendments.Effective()`, not by this label. Noticed because a first pass wrote the two cases inconsistently and the tests caught it. | Resolved |
| P-26 | `06` defines no value lists for alerts, but `Alerts.Severity` and `Alerts.Kind` are stored codes that need AR/EN labels | **Added three lists to `LookupCatalog.cs` in a clearly-marked ADDENDUM section** (`alert-severity`, `alert-kind`, `alert-status`), with a comment stating they are ours and not the data dictionary's. Codes come from the `Alert` entity's own documented vocabularies. | P-11's argument is that stored codes belong in the catalog: one mechanism (`EP-LKP-01` → `core/lookups.ts`) then labels every enum in the app, the AR↔EN toggle works without a per-screen map, and the client can correct wording without a code change. The alternative — a label map inside the Alerts page — would have been the second mechanism, reachable by no grep. Marking the section rather than silently merging it keeps "`06 §1–§11` verbatim" true of everything above the marker. **`06` should gain these three sections.** | **CONFIRM** — the nine Arabic labels, and whether `06` adopts the lists |
| P-27 | The reference uses **two Arabic forms of each severity** — `حرِج` in the table cell, `حرِجة` in the cards and chips — and two English ones for the top severity (`High` on the chip, `High severity` on the card) | **Both carried.** The singular is the lookup label (one alert's severity, agreeing with «تنبيه»); the plural is a group heading and lives in `core/lang.ts` with the rest of the chrome. | Not an inconsistency in the reference — each form is correct where it stands, and collapsing them to one would put a grammatical error on one of the two. It does mean severity is the one enum whose label does not come from exactly one place, so both sites are cross-referenced in comments. | Resolved |
| P-28 | The reference Alerts feed carries dates **after its own stated data date** (`2026-09-08` under a header reading *بيانات حتى 2026-08-10*) | **Every fixture alert sits on or before the project data date `2026-08-02`.** | D-06 already forbids the wall clock; this is the same rule applied to seeded rows. The reference's dates come from an offset against `Date.now()`, which is why they drift ahead. A demo where alerts are raised in the future undermines the one screen whose entire job is *what needs attention now*. | Resolved |
| P-29 | Is acknowledgement client state or a write? | **A write.** `EP-ALR-02` persists `Acknowledged` and stamps the persona into `AcknowledgedByUserId`. | The reference keeps an `ackMap` in component state — correct for a clickable prototype, wrong for a record. An acknowledgement is somebody accepting that they have seen an alert; one that evaporates on refresh, or that records no one, is indistinguishable from the alert never having been read. Makes `Alerts` the first table a screen writes to. | Resolved |
| P-30 | The reference colours the delay figure `--error` when late and `--success` when not | ~~Not ported.~~ **REVERTED — the reference's colouring is reproduced** (`.epm-sc-late` / `.epm-sc-ontime` / `.epm-sc-fc-late`, tokens not literals). | `CLAUDE.md §6` and `05 §7.9` forbid colouring a magnitude by threshold in as many words — "the neutral branch is `--on-surface`" — and `05 §7.5` reserves the status hues for status. A delay is a magnitude, not a state; the reference also paints the forecast DATE red, which makes a date look like an error condition. **The client chose visual fidelity over the §7 floor** (see the REVERTED block below). | **REVERTED by client decision — known defect** |
| P-31 | Import status has no source — the Activities table is not registered | **Returns `pending` for every project**, from a `ScheduleImported` bool that is currently constant, and the endpoint says why. | This is *true*, not a placeholder: with no Activities table, no P6 schedule can have been imported for anyone. It reads as a real finding — nothing in the portfolio has a published baseline — rather than as a fabricated column. Becomes a genuine `db.Activities.Any(...)` in Phase 4.3 with no change to the DTO or the screen. Labels added as `schedule-import-status` in the LookupCatalog addendum (P-26's section). | Resolved |
| P-34 | The shell was missing twelve of the reference's chrome regions, all of them already styled in the copied sheets | **Built all twelve with real behaviour**: `.d-fill` · `.d-appfoot` · sidebar collapse (`data-side`, persisted) · workspace switcher · account block · `.d-side-sep` · global search + ⌘K palette · `.d-nav-count`. Page-head action clusters restored on all five screens, each firing the reference's own "— تجريبي / — demo" toast. | Diffing the two rendered DOMs turned up twelve `d-*` classes the reference renders and we never did. **Every one already had CSS** — 11 rules for `.d-appfoot`, 14 for `.d-cmdk`, 11 for `.d-side-acct` — so this was unbuilt markup, not missing design. Nothing here is a dead prop: collapse persists, the switcher navigates with `?ws=` (which every endpoint already accepts), and the palette is built from the route table so it can only offer a page that exists. The persona switcher moved from the topbar into `.d-side-acct`, which is the same semantic slot and lets the topbar match the reference exactly. | Resolved |
| P-35 | v1.1 asks for a `support_agent` icon its own `icons.js` does not define | **Used `help`.** | Identical to P-25: the reference renders a fallback glyph in its own footer. Worth reporting upstream. | Resolved |
| P-36 | Angular wraps every component in a custom element, so a component whose ROOT must be a grid child gets a wrapper in between | **`epm-app-footer { display: contents; }`** in `styles.css`. | `.d-appfoot` carries `grid-column: 1 / -1`. With `<epm-app-footer>` in between, the wrapper became the grid item and the footer inherited the 256px sidebar track instead of spanning — measured at 256px wide before the fix, 1440px after. Any future component in the same position needs the same treatment. | Resolved |
| P-32 | **`--fg-subtle` breaches the 4.5:1 text floor in both themes** — measured **3.07:1** on white, 2.86 on `--surface`, 2.76 on `--background`; dark 4.44 on `--surface-container` | **Corrected on discovery** — `#666F7F` light (5.07 on white, 4.55 on `--background`) and `#8D97A3` dark (6.01 on `--surface`, 4.81 on `--surface-container-high`), overridden in `web/src/styles.css`. | `05 §7.1` is binding and 12px/400 is not large text. v1.1 uses this token as TEXT colour in about a hundred `desktop.css` rules, two of which every register already built depends on: `.d-stat-foot` (the KPI foot line) and `.d-table tbody td.d-cell-sub` (the secondary column on Projects, Contracts, Entities and Alerts). It escaped the Phase 1.5 audit because a `.d-cell-sub` nested INSIDE a `td` picks up a different, compliant rule at 6.56:1 — only a `td` carrying the class itself is affected. Chosen by the same method as P-21: the lightest value on v1.1's own cool-grey ramp that clears the floor on every surface the token actually lands on. `--fg-disabled` deliberately untouched — `05 §7` exempts disabled state. | **REVERTED by client decision — known defect** |
| P-33 | **`--fs-100` is 10px**, below the type scale's floor | **Raised to 11px** in `web/src/styles.css`. | `05 §2` is binding and `CLAUDE.md §6` repeats it: the scale is exactly 11/11.5/12/13/15/18/21/24 and nothing is smaller than 11px. `desktop.css:387` sets the filter-chip count to a literal 11px and `desktop.css:3079` then overrides it with `var(--fs-100)`, so every chip count in the app rendered at 10px — measured on Schedule Control, Projects, Contracts and Alerts. 11px is the scale's own floor, so this snaps to the nearest legal step rather than inventing a size. | **REVERTED by client decision — known defect** |
| P-37 | The v1.1 `DReports` is not the same screen as the pre-v1.1 one, and `04 §2` describes the old one | **Ported the v1.1 catalog.** SCR-E7 is a register of the twelve reports a user can run — search, project scope, five category chips, pager — not the four-chart board of `docs/spec/reference/app/desktop-reports.jsx:39`. | Identical to the Alerts Center at Phase 2.4 (card feed → register): the roadmap's own warning says the `docs/spec/reference/` copy is pre-v1.1 and the sibling repo has the screen. Two further reasons this is the right way round. First, the old board's four charts already exist on SCR-E1 — trend, status split, entity ranking — so porting it would have built a second dashboard, and `04 §2` gives SCR-E1 that job. Second, the charts were driven by `window.EPM.buildProjects()`, a generator; against real data three of the four would have had nothing to draw. **`04 §2`'s one-line description of SCR-E7 is stale and should be updated to the catalog.** | **CONFIRM** — that `04 §2` adopts the catalog wording |
| P-38 | A report is a promise about data, and nine of the twelve read a table this build has not registered | **Every catalog row declares the tables it `Reads`; the endpoint compares that against the tables actually registered in `EpmDb` — read off the EF model, not a second list — and returns `available` plus a reason naming the missing source and the phase that builds it.** A blocked row gets no Run button, an explicit in-row note, and «غير متاح» in the action cell. | The reference makes every row runnable because in a clickable prototype every row toasts. Here that asserts twelve capabilities the system does not have, on the one screen whose whole subject is what the system can tell you about itself — and the register would be twelve identical demo toasts, which is the "button that silently does nothing" ToastService.demo exists to prevent. This is P-09's rule ("never render a figure for a missing input — show unavailable + reason") applied to a row rather than a KPI tile, and CLAUDE.md's rule for a gated action ("an explicit locked note, never a bare disabled button") applied outside the persona gating it was written for. Reading the EF model rather than keeping a hand-maintained list means Phase 4.1 registering `Payments` flips RPT-01/02/03/04 with **no edit to this feature** — the alternative goes stale the first time somebody forgets. Today: 3 available (RPT-06 · RPT-08 · RPT-10), 9 blocked. | Resolved |
| P-39 | Angular's `[value]` on a `<select>` whose `<option>`s come from `@for` binds before the options exist | **Carry the selection on each `<option [selected]>` instead.** | Measured on SCR-E7: with the page scoped to `PRJ-0148` — scope bar shown, catalog narrowed 12 → 9 — the dropdown read «كل المشاريع» and `select.value` was `""`. The control was contradicting the screen. The options render after the select's own bindings are applied, and re-fetching the project list re-runs `@for`, so the value lands on an empty list every time. `<epm-pager>` has the same shape and is not affected only because its first option *is* its default. **Any future `<select>` fed by `@for` needs the same treatment**, or `[ngModel]`, which this project does not use. | Resolved |
| P-40 | The v1.1 workspace is not three panes, and both `04 §3` and ROADMAP Phase 3 say it is | **Ported the v1.1 shell**: topbar project picker · one detail pane · grouped module rail · Z2 identity bar. **And the rail shows all fifteen modules, the thirteen unbuilt ones disabled with the phase that builds them** — ROADMAP's "only tabs that exist appear" would have left two entries and four empty group headings. | Same situation as P-37, one level larger. v1.1's `DWorkspace` renders `<div className="d-three" data-ctx="off">` containing a single `.d-pane.d-detail`, with the comment *"DETAIL — actions now live in the page header, no side pane"*; `DProjectContext` is exported and rendered by nothing. The per-module actions it held moved into Z6, beside the view they act on. On the rail: the grouping IS how the workspace states what a project consists of, and a reviewer needs that whole before any of it is built — so it renders, and each unbuilt entry says which phase. Nothing is a dead link: `built: false` is not routable and has no route entry either, which is the half of the guarantee `app.routes.ts` owns. **`04 §3` should be updated to the v1.1 anatomy.** Both halves were put to the client and chosen deliberately. | **CONFIRM** — that `04 §3` adopts the v1.1 workspace |
| P-41 | `desktop.css:865` puts `letter-spacing: .6px` on the module rail's group labels | **Overridden to `normal` in `web/src/styles.css`.** `text-transform: uppercase` left alone. | `CLAUDE.md §6` and `05 §2` forbid letter-spacing in as many words, and the reason is not aesthetic: Arabic is cursive and spacing the glyphs severs the joins. That makes it the same class of defect as the missing `<bdi>` isolation, which Phase 2.9 explicitly did **not** revert because it is a correctness bug in Arabic rather than a contrast one. `text-transform` is a no-op on Arabic (no case) and keeps the reference's look on the English labels, so it stays. **Separately noted, not acted on:** `.d-stat-val` is **28px** and its suffix **16px** — neither is on the 11/11.5/12/13/15/18/21/24 scale, and it affects every KPI band already merged. Flagged like P-22 rather than changed, because changing it moves six screens. | Resolved (letter-spacing) · **CONFIRM** (the 28px/16px scale breach) |
| P-42 | A module page reads `:id` off `route.parent`, and the parent route OUTLIVES the module | **`takeUntilDestroyed()` on the parent-route subscription** in both module pages. | Measured: on Information, switching project fired `GET …/PRJ-0148/overview` *and* `…/information` — the destroyed Overview component's subscription was still live and re-fetched for a component that was no longer on screen. Every enterprise page subscribes to its own `route.queryParamMap` without unsubscribing and is fine, because there the ActivatedRoute dies with the component; a CHILD reading its parent is the case where that stops being true. **Every workspace module added from Phase 4 on needs the same treatment** — it is load-bearing, not hygiene. | Resolved |
| P-43 | A .NET feature namespace `Epm.Api.Features.Contract` shadows the `Contract` ENTITY for every file under `Epm.Api.Features.*` | **The feature is `ContractTab` / `contract-tab` on both sides.** The route segment stays `contract` — it is the module id the rail uses. | `Fixture.cs` stopped compiling with *"'Contract' is a namespace but is used like a type"*: from inside `Epm.Api.Features.Dev`, the sibling namespace wins over `Epm.Api.Data.Entities.Contract`. CLAUDE.md §2's one-slug rule bends here rather than breaks: both stacks say `contract-tab`, so one grep still crosses them, and the alternative — qualifying `Entities.Contract` at every use — would have to be repeated in every future feature file that touches a contract. **Any feature named after an entity has the same problem** (`Payment`, `Project`, `Alert`): name it `<Entity>Tab` or after the screen. | Resolved |
| P-44 | `02 §4` says financial % "comes from payments" but never fixes the denominator | **No financial % is rendered on SCR-W3.** The money is shown — disbursed, certified, retention, advance recovery, remaining — and the part-to-whole bar is unlabelled geometry beside figures already printed in words. | The candidates are the EFFECTIVE value and the TOTAL contract cost (award + reserve + supervision), and they differ by ~7.5% on `CNT-0279`. Picking one would put a number on a guess, on a screen whose whole subject is which figure is which. `01 §2.2` calls financial % derived and points at `02 §6`, which is about proposals, not disbursement. Recorded here rather than guessed, per CLAUDE.md §8. | **CONFIRM** — which denominator, and whether reserve and supervision are in it |
| P-45 | **The v1.1 reference computes the delay penalty by a different rule than `02 §10`** | **`Domain/Penalty.cs` is unchanged — 0.1%/day capped at 10% (D-02) — and the screen shows that.** The reference's formula is recorded here, not implemented. | v1.1's `penaltyOf()` is `perDay = value × rate / duration`, `cap = value × rate`, with `rate` a per-contract band of 10–25% defaulting to 10%, and a comment citing *"Regs 2/2014"*. That is a total ceiling reached when the delay equals the full contract duration — a different rule, not a different rounding. On `CNT-0279` (250,000,000 effective, 531 days, 16 days late) the spec's rule gives 4,000,000 and the reference's gives ~120,000; on `CNT-0207` the spec's rule caps at 3,120,000 where the reference's would too, but for a different reason. CLAUDE.md §1 settles which wins — *"the written spec gives you the rules, the reference component gives you the screen"* — and `PenaltyTests` asserts `02 §10`'s worked example, so changing it silently would break the spec's own oracle. **But the reference is newer than `02` and cites a regulation, which is evidence about what the ministry actually applies.** D-02 was already marked CONFIRM; this is the concrete alternative to confirm it against. | **CONFIRM** — which penalty rule is contractual, and whether the 10–25% band is per contract |
| P-14 | Rounding scope | **BOQ weights round (largest remainder, BR-01); schedule weights do not.** | `02 §1` states BOQ weights must sum to exactly 100.00% and gives 56.13/43.87 as the worked example, so rounding is part of that rule. Activity absolute weights are different: `02 §3` feeds them straight into the allocation share and `02 §11` into earned value, so rounding them at source would push error into every derived figure. `Domain/ScheduleWeights` returns exact decimals; a screen that shows a weight column rounds for display with `Rounding.LargestRemainder`. | Resolved |

| P-46 | **`04 §4` gates the BOQ tab behind a contract selector; the v1.1 component deliberately does not** | **Gated, with one exception: a project with exactly ONE contract is not asked.** The gate renders «اختر عقداً للبدء» with one button per contract, each carrying its item count. | The reference's own comment argues the other way in as many words — *"open on the first contract rather than gating the page behind a picker: the selector in Z6 is right there, and an empty page teaches nothing"* — and it is a fair point about a clickable prototype where every contract has data. It is the wrong call here. `01 §1` makes the contract the scope of a BOQ item, so "the project's BOQ" is not a thing that exists; opening on `contracts[0]` silently picks one of two bills of quantities and shows 240,000,000 of civil works under a heading that says nothing about which contract it belongs to. `04 §4` and ROADMAP 4.2 both ask for the gate, and CLAUDE.md §1 gives the written spec the rules. The single-contract exception is not a compromise: there is no choice to make, and it is the same call SCR-W3's register already makes (P-16's neighbour). The selector stays in Z6 either way, so switching contract is still one control. | Resolved |
| P-47 | Is the allocation override per LINK or per BOQ ITEM? `BoqActivityLink.IsManual` is a per-row column, but `02 §3` says "manually overridable **per BOQ item** and persisted" | **Per ITEM.** One manual link puts the whole line into override mode: every share on it is then the stored value, and a reset clears `IsManual` on all of them. | The per-row column suggests mixing, and mixing is incoherent. BR-03's computed share is `absWeight / Σ(absWeights of the LINKED activities) × 100` — a share **of the set**. Freeze one row at 60 and recompute the others against the same denominator and the total is 60 + 100 = 160%, a figure nobody chose and no rule produced. `02 §3`'s wording is therefore not loose, it is the answer. It also has a consequence worth stating: **a computed set always sums to exactly 100**, so `partial` and `over` — two of `06 §11`'s four states — are reachable *only* through an override. That is why the fixture's partial and over lines are manual ones, and it is what the assignment screen exists to let someone fix. `IsManual` stays per row because that is where EF can store it; the endpoint reads it as `mine.Any(l => l.IsManual)`. | Resolved |
| P-48 | `02 §2` says the weight basis is chosen at schedule import, and nothing stores that choice | **The register always computes on COST. The man-hours toggle on the assignment screen is a what-if that must be SAVED to become binding**, and saving it is — correctly — an override (P-47). | Leaving the basis free would let the register and the assignment screen show two different coverages for one line, which is the disagreement the single `Derive()` function exists to prevent: on `BQ-003` the cost basis gives 52.7 / 47.3 and man-hours gives 57.9 / 42.1, and both are right on their own terms. Cost is the honest default because it is the basis every activity has — `BudgetedManHours` is nullable precisely because a P6 file may not carry it, and the toggle is hidden when any activity lacks it. Making the what-if explicit also matches what the toggle really is: a person asking whether the schedule should have been imported on the other basis. **The stored choice belongs on the schedule import (Phase 4.3), and when it exists this becomes reading a column instead of assuming one.** | **CONFIRM** — whether the ministry imports on cost or on man-hours |
| P-49 | Derived decimals arrive over JSON with 26-digit tails — a share of 5.8/11 is `52.727272727272727272727272730`, and an assigned amount of exactly 14,094,000 arrives as `14094000.000000000000000000001` | **Rounded at the transport boundary: money to 2dp, quantities and percentages to 4dp** — the precisions `EpmDb` stores those columns at. Totals are summed at full precision and rounded once, at the end. | It is a projection, not arithmetic: nothing downstream re-derives from these numbers, and rounding a column *after* summing it is what stops a rounded column failing to add up. The alternative — sending the raw decimal — pushes the problem onto every consumer, and the browser is the worst place for it: JavaScript has no decimal type, so the tail becomes a float artefact rather than staying exact. **The related trap is on the client and was measured**: the assignment table originally recomputed the assigned amount from the share it was displaying and printed **14,094,007** where the rule says 14,094,000 — `Domain/Allocation` divides before it multiplies precisely to avoid that. The page now shows the server's figure until the user starts typing, and only then previews. | Resolved |
| P-50 | **What is an activity's ABSOLUTE weight the share OF?** `02 §2` says "÷ ALL activities" without saying all of what, and the client's lite prototype divides by the PROJECT | **÷ the CONTRACT.** `Domain/ScheduleWeights.For(value, allTotal, parentTotal)` is passed the contract's total, and SCR-W5's «الوزن المطلق» column is that figure. | `02 §3` settles it by arithmetic rather than by wording: A5 is 13,920,000 of `CNT-0279`'s 240,000,000 and the spec prints **5.8%**. Against the project's 340,000,000 the same activity is 4.09%, a number that appears nowhere in the specification. It is also the only reading consistent with `01 §1` — an activity belongs to exactly one contract, so "its share of the programme" is a question about that contract's programme. **The divergence changes no money**, which is why it can be recorded rather than escalated: BR-03 divides one absolute weight by a sum of absolute weights over the same set, so a common scale factor cancels exactly. It would only bite if a figure were ever compared across contracts — and `01 §2.4` forbids the row that would do it. | Resolved |
| P-51 | **The reference rolls a WBS node's progress up by original DURATION; `02 §4` says by WEIGHT** | **By weight.** `ProgressReflection.Rollup(Σ basis, Σ basis × progress)` — the same function SCR-W4 already uses for a contract total, applied to a subtree. | `02 §4`'s sentence is "project physical % rolls up by weight", and the rule has a reason a duration roll-up does not: weight is what the contract is *paid* on, so a weighted roll-up is the only one that agrees with earned value (BR-11) and with the BOQ progress BR-04 derives from the same activities. Rolling up by duration lets a 210-day supply activity of 40,000,000 outrank a 30-day commissioning activity of 15,869,000 by seven to one on time alone, and on `CNT-0279`'s node 3 the two methods differ by several points. The reference is not wrong for a clickable prototype — duration is the field a Gantt already has on screen — but this system reports to a ministry that pays against progress. **The visible consequence**: node 2 reads 67% by weight where duration gives a different figure, and nothing on screen explains the difference, so the roll-up basis belongs in the eventual user documentation. | Resolved |
| P-52 | **`desktop.css:637` paints a critical Gantt bar `--error`, contradicting `04 §5`, `CLAUDE.md §6` and the reference's own legend** | **The bar keeps its STATUS fill and wears a 2px `--on-surface` ring**, overridden in `web/src/styles.css`. The fill travels as a `--bar-status` custom property that the rule itself reads. | This one is not a judgement call: `04 §5` says "critical path is a 2px `--on-surface` ring, not a colour — the legend shows the ring", `CLAUDE.md §6` repeats it ("criticality is a ring… the colour channel belongs to status"), and `desktop.css:605` already renders `.d-gantt-legend i.crit` as exactly that ring. So the reference's chart disagrees with the reference's own key, and a delayed critical activity and an on-time critical activity are drawn identically — which is the one distinction the status channel exists to make. **Why a custom property rather than an inline `background`:** an inline style would merely out-specify the red rule, leaving a stylesheet that is still wrong and a later edit able to remove the only thing hiding it. Reading `var(--bar-status)` inside the rule makes the STYLESHEET correct on its own. | Resolved |
| P-53 | **BR-11 takes `planned` as an input and `02` never says where it comes from.** Without it there is no SPI | **`Domain/PlannedProgress.PlannedPct` — an activity earns its weight LINEARLY across its own BASELINE span**, rolled up on the same cost weights physical % uses. Recorded as an ASSUMPTION, not a rule. | The question has to be answered somewhere, and the honest place is a rule file that says what it assumed. The two alternatives are worse: a **constant offset** (`planned = actual + 8`, which the reference's own comment says was tried and abandoned) makes every project identically behind, which measures nothing; an **S-curve over the project span** is unfalsifiable in detail and would disagree with the delay days BR-10 computes from the same baseline dates, with no second source to settle which is right. Linear-per-activity has the property both lack: it is computed from the SAME baseline dates and the SAME cost weights physical % uses, so SPI compares like with like and a reader who disbelieves it can re-derive it from two columns already on SCR-W5. **The guard order matters and a test caught it**: a milestone has `start == finish`, so on the day it falls due both guards match — testing `now <= start` first reported a milestone as demanding nothing on the very day it was due. The finish test comes first. If the ministry's own progress curve exists, this is one function to replace. | **CONFIRM** — whether the ministry has a defined planned-progress curve |
| P-54 | SCR-W6, SCR-W7 and SCR-W1 all need a BOQ line's progress and achieved amount, which `BoqEndpoints.Derive` already computes for SCR-W4 — and CLAUDE.md §3.2 forbids a service class | **`Derive` became `internal` rather than being duplicated.** Three features now call it. | Its own comment states the invariant: *"The register and the assignment screen are two views of ONE model, so they are derived by one function. Two of them would be two chances for the coverage on the grid to disagree with the coverage in the editor."* Re-deriving in `ProgressEndpoints` would be exactly that trap one screen further on — and the figure at stake is the one the client's own worked example turns on. **This is not the service class §3.2 rules out**: it holds no state, injects nothing, is not registered in DI, and every piece of arithmetic inside it is still `Domain/`'s. It is one feature reading another feature's projection, and the anchor comment at the call site says so. The alternative was ~90 lines copied three times. **The visible payoff**: SCR-W1's physical %, SCR-W6's and SCR-W7's are the same number by construction, not by coincidence. | Resolved |
| P-55 | **`04 §3` and the v1.1 reference make Progress a READ-ONLY dashboard** — its header comment says so in as many words — while `07 §M3` and ROADMAP 4.4 ask for *"change an activity's progress, watch BOQ progress, achieved quantity and achieved amount update"* | **Both.** The dashboard is the reference's; the editor sits beneath it in the same view, with the BOQ lines it moves visible. SCR-W5 refuses the same edit. **And SCR-W6 is NOT gated on a contract**, unlike SCR-W4 and SCR-W5. | The two are not in conflict once you ask *where* the edit belongs. `02 §4` makes progress reflect straight onto the BOQ, so the screen that edits it must be the screen that shows the consequence — on SCR-W5 the consequence is off-screen entirely, which is why the Gantt's activity record carries a read-only note pointing here instead. Putting the editor under the dashboard also gives the reference's tiles something to be about: they stop being a report and start being the thing that just moved. **The gate is a separate question with a different answer**: a bill of quantities and a programme each belong to one contract (`01 §1`), but `02 §4` ends "project physical % rolls up by weight" — across every contract. Gating this page would put the project's own headline figure behind a picker. The reflection tables carry their contract per row, and the column is dropped when there is only one. | Resolved |
| P-56 | The reference's financial module has six tabs; two of them — **annual allocation** and **advance audit SLA** — have no source in this data model | **Rendered as "unavailable + reason", not invented.** No table records a yearly ministry allocation, and no column records when a certificate entered each audit stage. | The same call SCR-E1 made about physical % and SCR-E5 made about the critical path (P-09), and it is the one this system has to keep making: `01` defines neither, so building them means choosing figures. The SLA is the sharper case — the reference derives it from `DATA_DATE − 9 days`, i.e. it assumes a submission date rather than reading one, and the output is a **legal pay-by date**. A fabricated legal deadline on a ministry screen is not a placeholder, it is a liability. Both reasons live on the SERVER beside the rule that owns them, so the screen only picks a language. | **CLOSED — the allocation half by P-92, the SLA half by P-97. `EP-FIN-01` has no `unavailable` entries left.** |
| P-57 | `01 §2.3` calls `award_amount`, `reserve_amount` and `supervision_amount` "the three expense items", and the reference renders them as an indented sub-tree under each contract — but they do not sum to it | **They are NOT a partition and are no longer drawn as one.** The cost sheet keeps the contract row; the three travel beside it under their own heading. | `01 §2.3` defines `original_value` as *"the awarded value"*, and `award_amount` is that same money seen again — on every fixture contract the two are equal. So award + reserve + supervision exceeds the contract by exactly the two allowances: `CNT-0279` reads 240,000,000 as a contract and 258,000,000 as three components. A tree whose children out-total their parent is worse than no tree, and the reference knows it — it prints a message bar underneath explaining why its own numbers disagree ("two bases: approved budget vs contracted commitments"). Making the parent Σ components instead would have been the other way out, and worse: the contract row would then read 258,000,000 where SCR-W3, SCR-W1, SCR-E3 and every total in the system say 250,000,000. **Measured before the change**: the sheet's own footer did not equal its rows. | **SUPERSEDED by P-89 — the plates show the three items partitioning the contract; the fixture was wrong, not the model.** |
| P-58 | ROADMAP names SCR-W8's reference as `DModVO` `project-modules.jsx:1142`, and there is a component of that name there — but v1.1 has a SECOND one | **`vo-record.jsx:454` is the real component.** ROADMAP and TRACE are corrected. | `vo-record.jsx:4` says it outright: *"Loaded after project-modules.jsx so this DModVO replaces the earlier one."* The pre-v1.1 one is dead code that still parses, which is the worst kind of stale reference — building from it would have produced a screen that looks plausible and matches nothing the client signed off. The ROADMAP already warns that `docs/spec/reference/` is the pre-v1.1 copy and that line numbers are stale for every screen v1.1 rewrote; this row is the case where the FILE moved too, not just the line. Same class of error as P-37 (SCR-E7's two different `DReports`). | Resolved |
| P-59 | `03 §10` lists «متأخر» and «يحتاج إجراء» as exception chips, and BR-12 defines a PER-STAGE SLA. Are they the same measurement? | **No — two clocks.** «متأخر» is the WHOLE ORDER, 14 days since the incoming letter. «تجاوزت السقف» is the CURRENT STAGE against its own `SlaDays` (BR-12). An order can breach either without the other. | `06 §12` seeds the proof and says why: VO-02 at 22 days and VO-06 at 5 exist *"to prove pending and overdue are different sets"*. If the two chips shared one clock, «متأخر» would be a synonym for «قيد الاعتماد» on any order older than the ceiling, and the register's whole attention model would collapse into the lifecycle column it sits beside. The stage clock also survives a stage changing hands — a fresh stage starts a fresh SLA while the order's own age keeps running, which is exactly the distinction a committee chair needs to see. **A related split**: the «قيد الاعتماد» INDICATOR counts the `pending` lifecycle only, so it agrees with the group of the same name, while needs-action and overdue count pending **and** returned, because a returned order is back with its originator and that is somebody's work. Measured before the change: the indicator said 3 while its own group tab said 2. | Resolved |
| P-60 | `03 §7` gives the delegate relation `recorder` — "the viewer is the delegate and an external party is pending" — but `ChangeOrderExternalParty` is a Phase 5.4 table | **`externalPartyPending: false` is passed EXPLICITLY, so `recorder` is unreachable in 5.1 rather than guessed at.** | The alternative — inferring "an external party is probably pending" from a stage owner or a lifecycle — would fabricate an authorisation. `recorder` is one of only two relations that ENABLE actions (`03 §7`'s gating rule), so a false positive there is a permission granted to someone the specification did not grant it to. Passing the `false` at the call site with a comment is better than omitting the parameter: it records that the input is missing rather than that the case does not exist. The same P-09 instinct applied to a boolean instead of a figure — and it becomes a real query in 5.4 with no change to `Domain/ViewerRelation`, which already takes it as an argument. | Resolved |
| P-61 | The audit found no link at all between a user and a workspace, so `EP-ENT-01` returned every workspace to everyone and `?workspace=` was a filter the CALLER chose | **A persona carries `Workspaces` + `MinistryWide` (BR-15).** `Domain/WorkspaceAccess` holds the rule, `Features/Workspaces/WorkspaceScope.Deny` is the one-line guard, and every scoped endpoint calls it. **No group, role, permission, policy or scope table.** | `§7` states the rule as a ضابط أساسي — «نطاق رؤية المستخدم هو اتحاد تكليفاته … ولا يرى بيانات خارج تشكيله» — and `§24` defines assignment as user → role → scope. The prototype has to demonstrate the VISIBILITY half, and the role half already exists as `Persona.Party` (BR-14). Modelling the other four levels would have added five tables and a matrix screen to prove a rule that two fields prove. The identity is still the spoofable `X-Epm-User` header (P-05) and deliberately so: what is enforced is the business rule, not the session. | Resolved |
| P-62 | Should the enterprise view — no `?workspace=` at all — show the whole ministry or only the viewer’s workspaces? | **Only theirs.** `WorkspaceScope.Effective` resolves an absent workspace to the union of assignments, so "all projects" means all of MINE. Ministry-level personas are assigned to everything and therefore still see everything. | `§7`’s rule is about the USER, not about a filter they happen to have set. If the unscoped view showed the whole portfolio, «لا يرى بيانات خارج تشكيله» would hold only while a workspace was selected — which is the opposite of a control. Measured: `user.re-dept` unscoped now returns 3 projects / 420,000,000, identical to `?workspace=ub`, while `user.senior-mgmt` returns 5 / 451,200,000. | Resolved |
| P-63 | Selecting a workspace in the switcher merged `?ws=` onto whatever page you were standing on | **Selecting a workspace ENTERS it and lands on its overview; «المساحة الرئيسية» returns to the ministry portfolio.** New route `/workspace?ws=`, new screen SCR-E8. | The reference does exactly this — its switcher rows call `openWorkspace(w)`, which sets `view=’overview’` (desktop-shell.jsx:302, :422) — and الشكل 1 → الشكل 2 documents the transition. The merge behaviour produced the state the audit flagged: a user inside a project of workspace A with a breadcrumb reading workspace B, and a project picker that did not contain the open project. Scope moved and nothing landed. **Cost:** switching from Contracts no longer keeps you on Contracts. Accepted — the workspace nav is one click away and coherence beats place-keeping. | Resolved |
| P-64 | `?ws=` is the app’s only scope mechanism, so where does the workspace overview live? | **`/workspace?ws=ub`, not `/workspaces/ub`.** No `?ws` redirects to the register. | A path segment would be a SECOND way to say which workspace is in scope, and the switcher, every sidebar link, six endpoints and every module rail link already speak `?ws=`. Two mechanisms means two things to keep in sync and a class of bug where they disagree. The Angular folder is `features/workspaces/` (plural, the enterprise workspace) beside the existing `features/workspace/` (singular, the project workspace shell) — the same distinction the documents draw between «مساحات العمل» and «مساحة المشروع». | Resolved |
| P-65 | A `?ws=` the persona may not open — pasted, bookmarked, or left over after a persona switch | **The shell corrects it once: toast + redirect to the register.** The API still 403s independently. | Six pages each rendering their own error is not an answer to "you are not assigned to this workspace", and the register is the screen that lists what you MAY open. The guard waits for `EP-ENT-01` before judging, so it never redirects on a list it has not seen. It is convenience only — `WorkspaceScope.Deny` does not care what the client decided, which is why the direct-API tests still return 403 with no browser involved. | Resolved |
| P-66 | `.d-stat` is `overflow: hidden`, `.d-stat-val` is 28px, and the strip floor is `minmax(120px, 1fr)` | **A strip carrying a ≥ 9-character figure raises its own floor to 180px** (`SummaryStripComponent.needsWide` + `.epm-stat-wide`). | Measured on SCR-E8 at a 900px viewport: the tile was 147px, the number 148px, and "420,000,000" rendered as **"20,000,000"** — not truncated with an ellipsis, silently beheaded into a different and entirely plausible number. Widening the track rather than shrinking the type keeps the 11/11.5/12/13/15/18/21/24 scale intact; one tile in a band set smaller than its neighbours is its own defect. Fixes SCR-E1 at narrow widths too, since both use the same component. | Resolved |
| P-67 | `.d-ctx-emblem` sets `color: #fff` and no background — the reference supplies one inline per workspace from a palette this port does not have | **Each plane gets a neutral chip:** translucent white on the charcoal sidebar, `--surface-container-high` in the popover, and `.ent` keeps its white chip with a `--primary` mark. | Measured: the workspace code in the switcher popover was `rgb(255,255,255)` on `rgba(0,0,0,0)` — white on white, present in the DOM and readable by nothing — and the ministry emblem was a white icon on `.ent`’s white chip. Inventing a colour per workspace was rejected: `05 §1` has every colour channel already assigned and identity is not one of them. The neutral chip also makes the register, the sidebar and the popover show one object three times instead of three treatments. | Resolved |
| P-68 | The workspace register labelled its kind column from `beneficiary-type` | **New `workspace-kind` lookup with الشكل 1’s own four values**; `Workspace.Kind` migrated to them. | A beneficiary RECEIVES distributed quantity (`01 §2.1`); a workspace OWNS projects (P-24). The two lists share the word "university" and nothing else, so a directorate rendered as the raw string `directorate` — latent only because the fixture had three universities and nothing else. الشكل 1 names the four the register filters by: جامعة حكومية · جامعة تقنية · وحدة مركزية · مديرية تجهيز. The fixture now carries four of them, which is what makes the filter chips real. | Resolved |

| P-69 | `docs/spec/reference/` is a **pre-v1.1 snapshot**, and الشكل 5's screen had moved on inside it | **The LIVE prototype at `infinite-azaiton.github.io/epm` is the visual authority for الشكل 5**; the checked-in copy is kept for everything not yet re-audited. | The checked-in `DModInformation` groups fields with a regex over ENGLISH labels and renders `.d-sec`. The live one (v338) renders `.d-fgroup` cards, puts its tabs in a `.d-pz5` zone, adds a `.d-z9` action bar and edits INLINE — and the appendix's own الشكل 5 screenshot matches the LIVE one, not the checked-in one. Same class of staleness as P-58, one level worse: there the component moved file, here the whole screen was redrawn. **The rule this sets:** before building a screen from `docs/spec/reference/`, diff it against the live prototype — the ROADMAP already warns line numbers are stale, but the SHAPE can be stale too. | Resolved |
| P-70 | Editing a project lived at `/projects/:id/edit` — a full-page form outside the project workspace, and **nothing in the UI linked to it** | **الشكل 5 edits IN PLACE.** «تعديل» flips each cell to a control on the same screen; `/projects/:id/edit` is deleted and `project-form.page` is create-only. | The document puts «تحرير البيانات» inside «مساحة المشروع › معلومات المشروع», and the route put it outside — dropping the module rail and the identity bar, which are what the documented breadcrumb is made of. It was also unreachable: `grep -rn "/edit" web/src` returned the route, a shell guard and comments, and no navigation. So the app had a complete, working, invisible editor and a read-only screen where the editor belonged. **What did NOT change:** the write path. `EP-PRJ-04` loads the definition and `EP-PRJ-03` saves it, exactly as before — الشكل 5 is a second CALLER of المسار 1's endpoint, never a second copy. The create form still posts the same `ProjectDefinitionInput` to `EP-PRJ-02`. | Resolved |
| P-71 | الشكل 5 stars TEN fields; المسار 1's one-line gate («اكتمال الحقول الإلزامية وصحة سنة الإدراج والكلفة») names fewer, and omits four the screen marks | **The RULE was extended to meet the document**, not the stars trimmed to meet the rule. `status`, `formation`, `orgStructure` and `consultantParty` are now refused when empty. | A star the save does not enforce is a false statement on a screen the documents call «المصدر الوحيد» for this data, and this screen is where a specialist learns what the system requires. The reverse fix — unmarking four fields — would have made the card disagree with its own reference screenshot. `RequiredFields` (what الشكل 5 stars) and `EnforcedFields` (what `Validate` refuses) are kept as two sets precisely because they are not identical: **`plannedCost` and `workspaceCode` are enforced and NOT starred**, because المسار 1 requires them and الشكل 5 does not show them at all. A test asserts each of the ten is refused when empty, so the star and the 422 cannot drift apart. The fixture gained a consultant on four projects that had none — otherwise editing them would 422 on a field that was blank before anyone touched it. | Resolved |
| P-72 | The «مقترح» prose names TWO fields (رمز المشروع · المنطقة الجغرافية); the screen marks FIVE, adding أولوية المشروع · الفئة الإنفاقية · رقم اعتماد الموازنة | **Five** — the screen is what renders the badge. The test behind it stays `has a value`. | Prose and screenshot are the same document and the screenshot is the more specific statement. What is NOT resolved is the badge's MEANING: nothing records who put a value there, so a code the specialist typed is tagged «مقترح» too, permanently. A provenance column was deliberately not added, because the documents describe no confirm step that would ever clear it — inventing an acceptance workflow to justify a badge is a business rule invented to complete a UI. Only two of the five are actually derived (`Suggest()` fills the code and the expenditure category on create); the region has no source at all, since `Workspaces` carries no governorate column. **The visible consequence:** the card cannot yet keep its promise to «تمنع اعتماد قيم غير مؤكدة دون قصد». | **CONFIRM** — is there a confirm step, and what derives the other three? |
| P-73 | `.d-pz7` scrolled on **none** of the seven built workspace modules | **`display: contents` on the module host elements** (`web/src/styles.css`), the same fix that file already documents for `<epm-app-footer>`. | `.d-pframe` is written as `flex: 1; min-height: 0` and expects to be a direct flex child of `.d-detail-main`, so `.d-pz7` is the one scrolling box. Angular puts TWO component hosts on that chain — `<epm-workspace-page>` and the module's own — and a plain block host takes `height: auto`, so the frame grew to its content and the overflow travelled up to `.d-fill { overflow: hidden }`, which clipped it. Measured at 910px with the fixture loaded: overview · progress · financial · changeorders · information overflowed `.d-main` by up to 780px, and the bottom of each was unreachable. **It predates الشكل 5** and was only found because that screen's six cards plus its edit bar are the tallest content in the workspace. Every module added later needs its selector in that rule, or its page will not scroll. | Resolved |
| P-74 | Native `<select>` draws its option list with the OPERATING SYSTEM | **`shared/select.component.ts`** — a combobox on the existing `<epm-popover>`, with `.d-pop-row` options. | Everything above the popup was themed and the popup was not: our font, our RTL, our spacing and our dark theme all stopped at its edge. The trigger stays `.d-form-input` so a select and a text box sit on the same line, and the panel keeps the surface the switcher and account menu already use. Three details were measured rather than guessed: `.d-pop`'s 7px padding put option text **17px** from the panel edge against the field's **10px**, `min-width: 260px` made the panel wider than any narrower field, and its `overflow-y: auto` nested a second scroller inside the list's. All three are neutralised for select panels only, via `.d-pop:has(> .epm-select-list)`. **A popover bug fell out of it:** `<epm-popover>` closes on scroll to stay anchored, but its listener CAPTURES, so it also caught scrolls originating inside itself — the option list closed the panel on the first pixel and snapped back to 0. It now ignores scrolls from within; page scroll still closes it. | Resolved |
| P-75 | الشكل 7's header prints **«مبلغ الإحالة 520,200,000»**, and the SAME screen's تفصيل الكلفة prints **«الإحالة 479,400,000»** — one label, two amounts, one card apart | **The plate's NUMBER, الشكل 6's LABEL.** The header field renders `originalValue` under «القيمة الأصلية»; «الإحالة» stays the cost item it is in تفصيل الكلفة and in الشكل 8. | 520,200,000 is 479,400,000 + 25,500,000 + 15,300,000 — the contract's original value, which is what الشكل 6 calls «الأصلية» and what the header pairs against «القيمة النافذة» on the line below it. العرض الفني §11 is the tiebreak: «القيمة الأصلية (مبلغ الإحالة عند التعاقد)» — the documents themselves treat the two names as one figure, and only one of the two names is free on this screen. Rendering the plate's label verbatim would have put «الإحالة» against two different amounts three rows apart, which is the one thing a contract card may not do. | Resolved |
| P-76 | `02 §4` never fixed the denominator of **الإنجاز المالي**, so EP-CON-02 shipped a `financial-pct` "unavailable + reason" and the overview drew its own bar over the EFFECTIVE value | **الشكل 7 fixes it: كلفة العقد الكلية.** One figure — `money.spentPct` — is the card's headline «22 % من كلفة العقد الكلية» AND «الإنجاز المالي», and المتبقي is `totalCost − disbursed`. | The plate states the denominator in words and then proves it in figures: 112,841,143 ÷ 520,200,000 = 21.69% → the 22% it prints twice, and 520,200,000 − 112,841,143 = 407,358,857, the متبقي it prints. Against the effective value (587,673,564) the same spend reads 19% and 474,832,421 — neither number appears anywhere on the plate. This closes the CONTRACT half of P-44, matching what the الشكل 6 pass already did to the card's المصروف %; the PROJECT bar keeps the effective value, because الشكل 6 labels that one «الصرف من القيمة النافذة». Two bars, two questions, each naming its own denominator on screen. | Resolved |
| P-77 | Local dev could not reach the API **at all**: `environment.ts` points the client at `http://localhost:5080` directly, and the API's CORS list allowed only `:4200` — the one port CLAUDE.md §7 says this project does NOT use | `:4300` and `:4301` added to the dev origins in `Program.cs`; `.claude/launch.json` serves web on **4300**, the port §7 documents, with `autoPort` off. | Every `/api` call from a locally served page failed the preflight, so the whole app rendered «تعذّر تحميل البيانات» — found while verifying الشكل 7 in the browser. `autoPort` made it worse rather than better: when 4300 was taken it silently moved to a random port, which CORS could never allow. The `proxy.conf.json` path is untouched and still points at the deployed API for anyone who wants it. | Resolved |
| P-78 | الشكل 9's payments table has **no status column** — five columns, and a total row that sums every one of them. But only a PAID certificate is spend (P-26), and on the fixture's own contract الإجمالي is 117,925,000 against a المصروف of 76,700,000 | **The plate's five columns, plus the status pill as a sub-line under the payment number** — and the footer strip prints المصروف beside الإجمالي whenever the two differ. | The plate's own contract has three paid payments, so its total and its spend coincide and the question never arises on the screenshot. Ours does not, and a table whose total contradicts نظرة عامة's المصروف by 41 million with nothing on screen to explain it is the failure this codebase is most careful about. Neither figure was bent: الإجمالي is the register's total exactly as الشكل 9 defines it, المصروف is البند الشكل 7's, and the pill is what makes the gap legible. **The pill mapping was broken and this is what found it:** `payment-status` had no entry in `StatusPillComponent`, so مصروف and مصادق عليه both fell through to the neutral pill and rendered IDENTICALLY — on this screen and on the financials certificate panel. Now paid = settled, certified = in motion, pending = held. | Resolved |
| P-79 | الشكل 9 prints payment codes — `PAY-100`, `PAY-101` — and `Payment` carries a sequential `No` and no code column | **«دفعة 1».** No `PAY-` code is invented, and no column is added to carry one. | The plate's codes are illustrative fixture data, not a documented identifier scheme (`06` defines no payment code, and المسار 8 does not ask the user for one). Minting `PAY-100` from `No + 99` would put a fabricated ministry identifier on a legal record to match a screenshot. The number is real, the label names it, and if the ministry does have a code scheme it becomes a column and a fixture change, not a format string. | **CONFIRM** — is there an official payment code? |

| P-80 | الشكل 10 draws **one** «سجل التعديلات التعاقدية» containing the approved-but-unapplied ملحق as a row, under a column headed «قيمة العقد» — the last row of that column reads 597,949,017, which is a value the contract does not have | **One table, as the plate draws it**, with a state pill per row and the pending rows' قيمة/تغيير rendered subdued. The two lists stay two lists in the API (`versions` · `pending`) and in every total. | The alternative — keeping the two separate tables this tab used to have — is safer in isolation and was wrong here: the plate is the binding visual for this screen (the same client instruction that reshaped الشكل 6), a reader comparing screen to document would flag the split, and «الحالة» is a real column that answers exactly the question the split was protecting. What the rule actually forbids is FOLDING the projection into the effective figures, and nothing here does: no column on this table is summed, `ContractRegisterTotals` and `ContractMoney` both exclude the pending deltas, and the section above the table carries the explicit «أوامر معتمدة لم تُطبَّق بعد» warning with the projected value and date. | Resolved |
| P-81 | **The penalty formula in the client documents is not the one we implement.** الشكل 10 prints a daily penalty of 161,449 on a 587,673,564 contract of 364 days, and العرض الفني §11 states the rule in words: «غرامة اليوم = (قيمة العقد ± تغيّر المبلغ) ÷ (مدة العقد ± تغيّر المدة) × نسبة الغرامة» — i.e. **value ÷ duration × 10%**. `02 §10` (BR-10, D-02) says **value × 0.1% per day, capped at 10%**, which on the same contract gives 587,674 — 3.6× larger | **BR-10 is unchanged and the screen states the rule it actually applies.** الشكل 10's layout, fields and قبل/بعد/الفرق table are built exactly as drawn; only the «كيف تُحتسب الغرامة» text is ours rather than the plate's. | CLAUDE.md §1 settles which document wins where: *the written spec gives you the rules, the reference gives you the screen*. Silently swapping a penalty formula changes money on a legal record, and this one is not a rounding difference — it is a different rule with a different shape (under the client formula the cap is reached after exactly one contract duration of delay; under ours after 100 days regardless). Printing the plate's formula over our figures was the one option ruled out: a screen whose stated equation its own numbers do not satisfy is worse than either rule. **D-02 was already CONFIRM; it now has a concrete alternative and a worked example on both sides.** If the client formula is the real one, `Domain/Penalty.For` changes in one place, `PenaltyTests` gains the 161,449 example, and every screen follows — the screens are already correct. | **CONFIRM — which formula is binding?** |
| P-82 | **Q-F5-3 answered, for contracts.** The project activity log deliberately records no per-field diff — «a diff would be an invented one, and it needs new columns». But الشكل 11 asks for one in as many words: «عرض التغيير بصيغة القيمة السابقة مشطوبة ← القيمة الجديدة» | **`ContractActivityEvent` gains `Field` · `Before` · `After` · `Source` · `RefId` · `Note`, and `EP-CON-04` writes ONE ROW PER CHANGED FIELD.** | Q-F5-3's reasoning was that the DOCUMENTS gave a count and no schema, so a diff would be invented — that reasoning does not survive a document that specifies the diff, names the strike-through, and prints a worked example («مبلغ الإحالة 1,349,706,163 ← 1,391,449,653 د.ع بواسطة مهندس مشروع»). Values are stored as the strings the field held rather than as typed columns: a log row is a statement about what a screen said, and one nullable decimal per field type would be null four times out of five. **A save that changes nothing writes nothing** — a row saying an edit happened with no edit in it is noise in the one place that must be auditable. The diff itself is in `ContractEndpoints`, not Domain: it is neither a rule nor arithmetic, and Domain may not see a Features DTO. **الشكل 5's project log can now have the same treatment** and does not yet — `ProjectActivityEvent` is untouched. | Resolved for contracts |
| P-83 | الشكل 11 shows event kinds NOTHING in this build writes: applied change orders («أمر تغييري VO-01 — زيادة كميات أعمال الكهرباء») and progress updates («تحديث الإنجاز المادي إلى 31%»). Both come from other modules | **Seeded in the fixture and marked `system`**, with the `change-order` and `progress` actions rendering as the plate draws them. | The alternative was a timeline with two of the plate's four event kinds and no way to see the other two, on a screen whose whole subject is that everything lands in one place. The fixture already carries amendments, payments and change orders that no UI writes either — this is the same illustrative scenario, and `Fixture.cs` says so at the top. The rows are honest about what they are: `Source = "system"`, attributed to «النظام · حدث آلي», which is exactly how الشكل 11 attributes them. When 5.2 and the progress module write their own events, they write these actions and the screen does not change. | Resolved |
| P-84 | الشكل 12 opens a **بطاقة البند** with six tabs (عام · التخصيص · التوزيع · الإنجاز · الكلفة · السجل). SCR-W4 could EDIT an item, distribute it and assign it — and could not show it: every figure was a column, and nothing gathered them into the item | **The card reads the register row it opens from.** No second request: `BoqRow` already carries all six tabs' figures, and the two tabs with a deeper view of their own (التوزيع · التخصيص) OPEN it rather than restate it. | A `GET …/items/{code}` would have re-derived weights, coverage and progress for a record already on screen and given the card its own chance to disagree with the row beside it — the P-54 failure, one screen down. The card also closes when «تعديل البند» is pressed: the edit happens in the row, and a panel left open beside a row that has become a set of inputs shows one item twice, in two states. **The card is opened by the CODE cell as a button**, not by a row click — the row already carries an inline editor, a delete confirmation and an overflow menu, and a whole-row handler would fire from inside all three (`05 §7`). | Resolved |
| P-85 | الشكل 12 lists three Z6 actions — «إدخال يدوي · استيراد · الجهات المستفيدة» — and none of the three has a code path behind it | **Rendered as demo stubs**, the same treatment تصدير already gets, rather than left off the screen. | Each is blocked on something real, and the honest thing is to say which: **إدخال يدوي** needs a create endpoint (`EP-BOQ-03` updates by code and 404s on an unknown one, so there is no way to add a line); **استيراد** IS الشكل 13, the next screen in the appendix; **الجهات المستفيدة** at project level has no figure of its own anywhere in the document, and the per-item drawer (BR-08) is the only beneficiary UI specified. Leaving them off would have made the toolbar disagree with the document; wiring them to nothing would have been worse. | **Open — إدخال يدوي needs EP-BOQ-09** |
| P-86 | الشكل 13 is an **Excel** import — «تحليل Excel» is one of its five step names — and reading a real `.xlsx` needs a spreadsheet parser this build does not have (an .xlsx is a zip of XML) | **CSV / TSV is parsed natively; `.xlsx` is refused with the fix.** The upload step says: save the sheet as CSV from Excel and upload again. | Three options, and only this one is honest. Adding SheetJS is a dependency decision with a bundle and supply-chain cost that belongs to whoever owns deployment, not to a screen task — and this system may end up on a ministry network where that matters. Hand-rolling a zip+XML reader is ~120 lines of fiddly code (stored vs deflated entries, shared strings, inline strings, date serials) that nothing in the repo could verify without a real workbook to test against. Faking the parse was never an option. **Everything downstream of the parse is real**: mapping, validation, comparison, submission and the version record all work today, so wiring a real .xlsx reader later changes ONE function and no rule. The CSV parser is ~40 readable lines with quoted-field and Arabic-numeral handling, which is why it is inline rather than a dependency. | **CONFIRM — add a spreadsheet library, or is CSV acceptable?** |
| P-87 | المسار 3 step 6 is «تقديم النسخة للاعتماد — لا استبدال للإصدار السابق», and الشكل 13 prints it inside the dialog. Where does a submitted-but-unapproved bill LIVE, given that `BoqItems` is what every weight, link and earned-value figure derives from? | **Two new tables — `BoqImportVersions` · `BoqImportVersionItems` — and `BoqItems` is not written at all.** `EP-BOQ-10` records a version; nothing moves into the register. | A version column on `BoqItems` would have been less schema and one forgotten `Where` away from putting an unapproved bill into BR-01's denominator, into the activity links, and into every earned-value figure on four screens. Here an unapproved import is unreachable BY CONSTRUCTION: no screen that reads the bill reads these tables. **`Amount` IS stored on a version item**, against the never-store-derived rule (§3.5) and deliberately — a version is a record of what someone submitted, and re-deriving qty × rate later would silently restate a signed document. **Steps 7 and 8 («اعتماد الإصدار الجديد» · «تحديث أوزان البنود وقيمة العقد المرجعية») have no screen in the appendix**, so nothing approves a version yet and the table only grows — which is exactly «يحمي البيانات التاريخية: لا يُمحى إصدار سابق». | **Open — approval has no figure yet** |
| P-88 | The comparison needs the bill «القائم» to compare against, and `BoqItems` carries contracted quantities while the register SHOWS effective ones (a re-priced line has a blended rate under BR-05) | **Compared against `BoqEndpoints.Derive`, the same model the register renders.** | Comparing a file against the raw columns would have reported a change on every banded item that nobody made — P-54's failure one screen down, and on the screen whose entire job is to say truthfully what an import would do. | Resolved |
| P-89 | **P-57 is superseded.** It found that award + reserve + supervision out-totalled the contract they belong to, concluded they are not a partition, and split them out of the cost sheet — so الشكل 14's tree could not be drawn | **The three items ARE a partition, and the fixture was the defect.** `Apply()` now derives a new contract's value as award + reserve + supervision, and the fixture's awards were reduced so each contract's three items add to its value. | Three plates say it in figures and none of them was available when P-57 was decided: الشكل 7 prints «مبلغ الإحالة 479,400,000 + الاحتياط 25,500,000 + الإشراف 15,300,000» against «القيمة الأصلية 520,200,000»; الشكل 8 shows the same three; الشكل 14 draws them as children that add to their parent row. `01 §2.3`'s «the awarded value» is the value AWARDED BY the contract, not the award LINE inside it. **P-57 considered and rejected changing the PARENT** — the contract row would have moved to 258,000,000 where four screens say 250,000,000 — and that rejection still stands; what changed is the CHILD. Every contract value on every screen is untouched: only `AwardAmount` moved, from 240,000,000 to 222,000,000 on `CNT-0279`. Verified after the change: Σ components = the contract row on both fixture contracts. | Resolved |
| P-90 | الشكل 14 prints «عند الإنجاز» and «الفرق» on the component rows as well as the contract rows, and its component forecasts sum to the contract's — but by no ratio any document states (award and supervision share 1.0968 while reserve reads 0.914) | **Forecast at CONTRACT level only; components print «—».** | BR-11 forecasts from a CPI, and a CPI needs an earned value: a contract has one (its BOQ progress against its own disbursement), an expense item does not. Reverse-engineering the plate's apportionment would have meant inventing an allocation rule for money on a legal record from three ratios in a screenshot. The contract forecast is real — `EarnedValue.For(revised, planned, physical, disbursed)`, the same rule SCR-W6 and SCR-W1 read — and null until the contract has spent, because a CPI with a zero denominator is not a forecast of zero (P-09). | **CONFIRM — how is a component's forecast apportioned?** |
| P-91 | الشكل 14's note box compares «الكلفة المعدلة المسجّلة» with «إجمالي العقود», and the two are different numbers with different meanings | **Both are sent and both are named**: `projectBudget` is `Projects.PlannedCost` (what المسار 1 recorded), `contractCommitments` is BR-00 over BR-09, and `budgetGap` is signed. | This is the same class of thing as الشكل 6's two denominators: one screen, two questions, and a reader who takes the wrong one is wrong about whether the project can pay for itself. The gap is what the box exists to raise — commitments above budget need a cost revision or a transfer BEFORE the spending stalls. `projectBudget` is nullable and the box says so when المسار 1 recorded no cost: no budget is a state, not a gap of zero (P-09). | Resolved |
| P-92 | **P-56 is closed.** The financials screen carried an `allocation` "unavailable + reason" saying «يتطلب جدول التخصيصات السنوية للوزارة — لا يسجّله هذا النموذج بعد», and الشكل 15 is that tab | **`ProjectAllocations` exists** — one row per project per fiscal year — and only the ALLOCATION is stored. | Three different figures live on this module and none is the others: **الكلفة المعدلة** (the approved cost, `Projects.PlannedCost`), **إجمالي العقود** (what has been committed, BR-00 over BR-09) and **التخصيص السنوي** (what the ministry actually released this year). الشكل 14's note box compares the first two; الشكل 15 is the third, and it is the only one of them that can stop a payment in October. **The spend is NOT stored on the allocation**: a year's المصروف is Σ of the payments whose money moved in it (`Payment.PaidDate`), derived at projection time like every other spend figure — so this tab and جدول الكلف cannot report different figures for one year. Verified on the fixture: 2025 closed at 96% of a 90,000,000 release, and both screens agree its spend was 86,700,000. | Resolved |
| P-93 | الشكل 15's total row needs a consumption % over several years, and the obvious reading — average the per-year percentages — is wrong | **Σ spent ÷ Σ allocated**, never the mean of the rates. | A 100%-consumed year of 12,000,000 and a 10%-consumed year of 300,000,000 do not average to 55% of anything; the mean would report a project as comfortable when it has exhausted a small release and barely touched a large one, or the reverse. Same shape as BR-01's denominator rule and الشكل 6's weighted progress: a ratio of sums, not a sum of ratios. On the fixture the honest figure is 41% (86,700,000 ÷ 210,000,000) where the mean of 0% and 96% would have read 48%. | Resolved |
| P-94 | الشكل 16 shows a payment covering **two contracts** («عقدان») with a declared split, and `Payment` carries exactly one `ContractId` — one row cannot span two | **No schema change: the register groups by the FUNDING LETTER.** A payment already records the official letter it was released against (`Payment.FinanceLetterNo`), and two contracts sharing one letter is one row with «عقدان». | The letter is what the ministry actually files by — `Payment.FinanceLetterNo`'s own note says so, and it is why the column exists rather than an internal certificate number. So the grouping is a reading of data already recorded, not a new parent table with the drift that invites: no id to keep in sync, nothing to orphan, and the per-contract view (الشكل 9) keeps reading exactly the same rows. **The split shown is the apportionment الشكل 9 records on each payment** — one apportionment, two screens, and neither can disagree with the other about what a contract received. The fixture gained one certified certificate on the EM contract sharing `0931/2026` with CNT-0279's third, so the «عقدان» case is visible; certified and not paid, so it moves nothing in المصروف, in الشكل 15's consumed allocation, or in الشكل 14's actual columns (P-26). | Resolved |
| P-95 | الشكل 16's register leads with «الرمز» (`PAY-102`), and a letter that groups several certificates has no single number of its own | **The letter number IS the row's identity**; the الرمز column is dropped. | The same reasoning as P-79, applied one level up: `Payment` has a per-contract sequence and no code, `06` defines no payment-code scheme, and a group spanning two contracts could not carry one of their numbers even if it did. Minting `PAY-102` at project level would be inventing an identifier for a record the system does not issue. The letter number is real, official, and already how the row would be found in a filing cabinet. | Resolved |
| P-96 | الشكل 16's panel names who registered the payment («سجّلتها محللة موازنة في قسم الحسابات»), and `Payment` records no actor | **Named as a gap in the panel, not left blank.** | Nothing in this build creates a payment — الشكل 20's «تسجيل دفعة» wizard is the screen that would, and it is not built. Adding four actor columns that only a fixture could fill would put an unsourced name on a financial record; the panel says instead which screen will capture it. Same treatment as الشكل 12's السجل tab. | **Open — resolved by الشكل 20** |
| P-97 | **P-56 is fully closed.** Its second gap — «يتطلب تواريخ مراحل تدقيق المستخلص — لا تسجّلها جداول الدفعات بعد» — was الشكل 17 | **`PaymentAuditStages`: one row per DESK a certificate sits at**, recording four facts (which desk, its cap, when it received the file, when it let it go) and deriving nothing. | A column pair per desk would have fixed the route in the schema and needed a migration the first time a ministry adds a stage; a row per stage makes the route DATA, which is what it is — the appendix names two desks «by way of example» and never claims the list is closed. **The arithmetic is BR-12's** (`Domain/SlaLeadTime`), measured against the project's data date and never a clock (D-06): a finished stage is measured to its own finish, the one holding the file to today-as-the-project-knows-it. **`CapDays` is per stage, not `SlaLeadTime.SlaDaysPerStage`** — the plate shows 7 where D-03 assumes 5, and a cap that varies by desk is what an instruction changes without changing code. With both gaps closed, `EP-FIN-01`'s `unavailable` list is now EMPTY. | Resolved |
| P-98 | الشكل 17 prints «الموعد القانوني للصرف», and the obvious derivation — certified date + Σ stage caps — does not reproduce the plate's own date | **Recorded on the payment (`Payment.LegalDueDate`), not derived.** | The plate's certificate is certified 2026-07-09 with caps of 7 and 7, which would give 2026-07-23, and it prints 2026-08-12. So the date comes from the instruction governing the certificate, not from the desks it passes. Deriving it would have invented a rule AND made a date the ministry can be held to move whenever someone edited a cap. Null when none is set, and the box simply does not render. | Resolved |
| P-99 | «السلفة الجارية» — which certificate does this screen watch? | **The one certified and not yet paid**, most recently certified first. | A paid certificate has no lead time left to watch and a `pending` one has not entered the route; the certified-not-paid state IS the transaction in flight, and it is the one P-26 already keeps separate everywhere else. When there is none, the tab says so rather than drawing empty stage cards (04 §9). | Resolved |
| P-100 | **The fixture's stage chains were not `03 §2`'s.** They ran five stages named «دراسة دائرة المهندس المقيم · لجنة أوامر الغيار · لجنة تثبيت الأسعار · لجنة المراجعة المصادقة · المستوى الإداري الأعلى» — two of which are not stages at all | **The six documented stages, taken from `Domain/WorkflowMachine.Stages` rather than typed into the fixture.** لجنة المراجعة المصادقة becomes what `03 §3` says it is: an EXTERNAL PARTY inside stage 4. المستوى الإداري الأعلى keeps its persona and owns nothing. | `03 §2` and الشكل 33 agree on the six names, their owners and their order — «دراسة الطلب · لجنة أوامر الغيار · تثبيت الأسعار · المصادقة والتخصيص · الأمر الوزاري وملحق العقد · التنفيذ» — and الشكل 29 prints «الأمر الوزاري وملحق العقد» in the register's own stage column. The invented chain put the wrong party in «الجهة المسؤولة» on a screen whose whole job is naming who holds the file, and it had no execution stage, so «معتمد ≠ مطبَّق» had no row to live in. Seeding from the domain table means a stage cannot be named one thing and rendered another. **Two consequences the register shows:** stage 6 التنفيذ is owned by دائرة المهندس المقيم, so the RE department — not the committee — is now `awaiting` on VO-04 and VO-05; and the per-stage ceilings become الشكل 33's 3 · 5 · 7 · 10 · 14 · 7 instead of a flat 5, which is why D-03's flat figure is a DEFAULT and not the rule. | Resolved |
| P-101 | `03 §6` lists **seven** application steps; الشكل 30 prints **nine** and names them | **Nine, each carrying its `03 §6` number where it has one.** `WorkflowMachine.ApplyStep` gained `SpecStep`, null on the two the plate adds. | The two extra steps are not decoration: **إصدار ملحق العقد** is BR-09's amendment row, which is the thing that makes an approved figure effective — without it "approved ≠ applied" has no step that separates them — and **إعادة احتساب الغرامات التأخيرية** is BR-10's baseline moving with an approved extension. Both are operations this system genuinely performs. Rendering seven would have made the screen disagree with a plate that enumerates nine; renumbering the spec's seven would have broken every `03 §6` citation. Two numbers on one step costs nothing and keeps both readings greppable. | Resolved |
| P-102 | الشكل 32 prints THREE day counts — 30 requested, **21 from the analysis**, 21 approved — and the analysis figure is neither the sum of the affected activities' days (9 + 6) nor their maximum | **`ChangeOrder.AnalysisDays` — stored on the ORDER, not derived from its activities.** | An activity extension absorbed by float moves the project by less than itself, and two extensions on one path move it by more than either: the project-level effect is what the schedule analysis CONCLUDES, and no arithmetic over the activity rows reproduces it. Deriving it by `Max()` (which the first cut did) would have printed a number the schedule analysis never produced, on the screen whose standing note exists to say exactly that — «مدة النشاط ليست مدة المشروع». | Resolved |
| P-103 | `03 §1` records two letters that PRECEDE entry — the contractor's request and the consultant's opinion, «each with an official letter number and date» — and `ChangeOrder` stored neither | **Four columns: `ContractorLetterNo/Date` and `ConsultantLetterNo/Date`**, rendered in الشكل 30's «مدخلات سابقة لإدخال الأمر». | The first cut derived them from the order's own `IncomingNo` (`IN-3312/2025`, `IN-3312/2025/1`), which put ONE letter's number on THREE different documents in a legal record — الشكل 30 prints three distinct numbers precisely because they are three distinct letters. **VO-06 has no consultant letter and the section shows one input**: a supply order's technical opinion comes from the inspection side, and inventing a second row to keep the layout symmetrical would be inventing a document. | Resolved |
| P-104 | Which amendment did an order produce? `EP-CHG-02` first matched on the pair (delta value, delta days) | **`ContractAmendment.SourceChangeOrderId`** — the column already existed and nothing was setting it; the fixture now stamps it. | Two orders on one contract moving the same figures is not unusual, and the pair-match would then have paired an order with somebody else's amendment — on the section that states whether the contract has actually changed. The fixture stamps amendment 1 ← VO-01 (applied) and amendment 2 ← VO-05 (approved, waiting), which is the pair «معتمد ≠ مطبَّق» is demonstrated with; VO-05's approved days moved 0 → 12 and its requested value 3,400,000 → 3,375,000 so that the order and its pending amendment carry ONE set of figures, not two. | Resolved |
| P-105 | VO-04's seeded weight-recalculation failure said «مجموع الأوزان 99.94% — لا يبلغ 100.00%», and the record's own weight panel recomputes the same figures and prints **100.00% · مطابق** | **The failure message says what failed to RUN, not a sum the screen contradicts two tabs away.** | VO-04 moves 500 م² between two lines at the same rate, so the contract value does not move and BR-01 still totals 100.00% — the seeded sum was fiction the moment the panel started recomputing instead of reading a stored number. `03 §6` asks for a failed step, not a wrong total: the step now fails because the recalculation could not complete against a re-numbered activity link, which keeps فشل التطبيق real on the register and the weight panel honest. | Resolved |

| P-106 | The reference wizard computes the 20% split, the revised quantities and the impacts **in the browser** (`vo-wizard.jsx` `bOne` / `bCalc`), and الشكل 39 recalculates as the two proposals are typed | **`EP-WIZ-02`, a preview endpoint called on a 300 ms debounce.** The wizard holds the draft; every figure on it is computed by `Domain/ChangeOrderRecord` — the same function the RECORD page reads. | CLAUDE.md §3.1 admits no exception for "it is only a preview": a number a user approves on the strength of is a number the rule must have produced. The alternative — porting `bOne` to TypeScript — would have created a SECOND implementation of BR-05 that agrees with the first only as long as both are maintained, and the divergence would surface as a wizard that promised one impact and a record that showed another. **What the round trip buys:** what a user saw when they pressed «إرسال للمراجعة» is what the record shows afterwards, by construction. The cost is one request per pause in typing, against a local database. | Resolved |
| P-107 | `ChangeOrder.No` is documented as unique **within the contract**, and the record's URL is project-scoped (`…/projects/{id}/changeorders/VO-06`). The first wizard build minted VO-06 on CNT-0279 while VO-06 already existed on CNT-0279-EM | **The next free number is taken across the PROJECT.** Still unique per contract — that rule is a weaker claim — and now addressable. | Caught by submitting through the wizard and finding two rows in one register both labelled VO-06, one of which the record page could not open. The alternative was a contract segment in the URL, which the plates do not show and which makes a change-order reference unpasteable into a letter. `06 §12`'s own fixture already numbers VO-01…VO-06 across two contracts of one project, so this is what the documented dataset assumes. | Resolved |
| P-108 | `03 §8` collects no TITLE, and the register's «الأمر التغييري» column shows one. الشكل 42 prints the justification where a title would be | **The title is the justification's first line**, capped at 120 characters, falling back to the type when it is empty. | Inventing a title field the document does not ask for would put text on a legal record that nobody was asked to write; leaving the column blank would break the register a phase earlier. The first line of «الأسباب الموجبة» is what a reader writes there anyway, and it is a value the user actually authored. | Resolved |
| P-109 | Does a DRAFT enter the approval chain? | **No.** `kind=draft` writes the order and its lines and NO `ChangeOrderStage` rows; `kind=submit` plans all six through BR-13 and opens the first applicable one. | A draft has not been referred to anybody, so a chain would put it in somebody's «بانتظار إجرائي» with a clock already running (BR-12 measures from the incoming letter, and the register's «متأخر» would start counting against a document nobody has seen). It also keeps `03 §6`'s lifecycle honest: `draft` is before `pending`, not a quiet version of it. **The gates are not skipped for a draft either** — they are simply not blocking: `02 §7` blocks SUBMISSION, and a draft is where an incomplete order is supposed to live. | Resolved |
| P-110 | الشكل 58 draws the supply variant of step 2 — redistributing a تجهيز item **between beneficiaries** (ITM-002), with a from→to→quantity table per transfer | **The type card is there and the supply flow is scoped to it; the beneficiary-level redistribution table is NOT built.** A supply order can still move quantity BOQ-line to BOQ-line. | الفقرات التجهيزية is its own module (الأشكال 50–56) and this build has no supply-item register, no beneficiary allocations per supply item and no receipt records — so the from→to picker would have nothing real to pick from and the balance check nothing to check against. What IS honoured is the rule that comes with it: a supply order has no 20% tier and no rate-fixing stage (`02 §5`), and step 2 says so in its own explainer instead of showing a limit that does not apply. | **Open — needs الفقرات التجهيزية (الأشكال 50–56)** |

| P-111 | Where does the PENDING amendment come from? `02 §9`'s projection is rendered from `ContractAmendments` rows with a null `AppliedAt`, and until 5.4 nothing wrote one — the fixture seeded VO-05's by hand | **Approval creates it (`EP-WFL-01`), applying FLIPS it (`EP-WFL-03`).** One row per order, never two. | The first cut had apply INSERT an amendment, which on VO-05 — whose pending row the fixture already carried — produced two rows numbered 2 on one contract, and the record page then read the wrong one. Creating it at approval is also what makes the projection true the moment it becomes true: SCR-E3, SCR-W1 and SCR-W3 show «أوامر معتمدة لم تُطبَّق بعد» from these rows, and before this an approved order was invisible to all three until somebody seeded a row for it. | Resolved |
| P-112 | `03 §6` calls the weight recalculation failable. What does a failure DO — mark the step and carry on, or stop? | **Nothing is written at all.** `Domain/ChangeOrderApply.Plan` computes every change as a value first; if BR-01 does not total 100.00% the endpoint records the nine steps with step 5 `fail`, leaves the order in `applied_partial`, and returns 422 having touched no contract, no BOQ line and no date. | A half-applied contract is the worst outcome available here: the value moved and the quantities did not, or the other way round, with no single row to say which. Computing the plan as a value before writing anything is what makes "all or nothing" true rather than intended — and it is why the steps after 5 stay `todo` instead of `na`, because they did not run rather than not applying. | Resolved |
| P-113 | Who may CANCEL an order (`03 §5`'s fourth decision)? D-04 says the two stage-4 external parties, and they are not system users | **Only the delegate**, and only as the recorded outcome of a party that carries `CanCancel`. `EP-WFL-01` refuses `cancel` from anyone who is not a delegate; `EP-WFL-02` ends the order when such a party's outcome is recorded as «أُعيد». | The decision belongs to a party that cannot log in, so the only honest path is the one `03 §4` already defines for every other external outcome: the delegate records it against an official letter, and the attribution keeps both names. Offering «إلغاء الموضوع» to a stage owner would have handed a committee an authority the document gives to the finance directorate and the endorsement committee. | Resolved |
| P-114 | A RETURN moves the order back — but back to what state, and what happens to the stage that returned it? | **The returning stage keeps its `returned` status and its decision; the stage it goes back to reopens with a fresh clock.** `resubmit` then reopens the first applicable stage and clears the return. | `03 §5` requires full history and prior versions preserved, and the natural implementation — resetting the returning stage to `pending` — erases the fact that a return happened at all. Keeping it is what lets الشكل 33 draw VO-03's المصادقة والتخصيص as `returned` with its own comment while stage 1 is active again, which is exactly what the plate shows. | Resolved |
| P-115 | The record page decides which actions to render, and the endpoints decide whether to accept them. Two lists of the same rule | **Both read `Domain/WorkflowMachine.Available`** — the endpoint calls it directly, the page mirrors it in one `computed`. The page hiding a control is COURTESY; the endpoint refusing the call is the rule (`03 §7`). | The alternative — shipping the available set in the record payload — was rejected for one reason: it makes the browser's list authoritative-looking, and a client that edited it would get a 403 rather than a screen that quietly disagrees. Mirroring costs eight lines in one `computed` and is checked by the same tests that check the endpoint. **The gate is verified live**: `user.univ-specialist` posting a decision on VO-02 gets 403 with no control ever rendered. | Resolved |

## REVERTED — visual fidelity chosen over the 05 §7 floors

**Client decision.** Where the reference prototype breaks the binding
accessibility contract, the prototype wins: the screens must be
indistinguishable from the one the client signed off. All four corrections
below were measured, applied, and then removed again.

They are recorded rather than deleted for two reasons. The measurements are the
expensive part and re-taking them later would be waste; and restoring any of
them is a few lines in `web/src/styles.css`, which is only true while the
values are written down.

| # | What v1.1 ships | Floor | Where it bites | Restoring it |
|---|---|---|---|---|
| P-21 | `--outline` / `--viz-base` `#A9B1BC` light, `#454C57` dark | 3:1 graphics (`05 §7.1`) | **2.16:1** light, **1.87:1** dark. Gantt data-date line (`04 §5`), chart baselines, every hairline that carries meaning | `--outline: #858E9C` light / `#6B7484` dark |
| P-30 | delay figure `--error` / `--status-completed-tx`, forecast date `--error` | no threshold colouring (`05 §7.9`) | SCR-E5. A magnitude coloured by threshold, and a date that reads as an error | drop `.epm-sc-*` from the delay and forecast cells |
| P-32 | `--fg-subtle` `#8B94A1` light, `#7D8794` dark, used as TEXT in ~100 rules | 4.5:1 text (`05 §7.1`) | **3.07:1** on white. `.d-stat-foot` and `.d-table td.d-cell-sub` — the KPI foot line and the secondary column on **every register** | `--fg-subtle: #666F7F` light / `#8D97A3` dark |
| P-33 | `--fs-100: 10px` | 11px (`05 §2`) | every filter-chip count in the app | `--fs-100: 11px` |

**`NFR-A11Y-01` is currently NOT met, by choice.** `ROADMAP.md` Phase 7's
accessibility pass is where this is settled before production — that pass
should start from this table.

Two things were NOT reverted, because neither is a fidelity question:

- **`.epm-sr-only`** on the Alerts row-action column header. The reference
  leaves that `<th>` empty; naming it for a screen reader changes nothing
  visually.
- **`<bdi>` isolation** everywhere (`05 §5.2`). Without it Arabic text
  reorders around numbers — that is a correctness bug, not a contrast one.

## Carried forward, still open

`07-POC-BUILD-PLAN.md §9` open questions D-01 … D-05 remain **CONFIRM** — rate bands after
several orders, the penalty rate, per-stage SLA, delegate cancellation authority, and who
revises a distribution when a decrease forces it. None are resolved by this port.

### Raised by الشكل 5, not resolved by it

| # | Question | Why it was not decided here |
|---|---|---|
| Q-F5-1 | **Do the enterprise lookup catalogues match the ministry's, or the prototype's?** `execution-stage` is a CONSTRUCTION-phase list here (design → handover) and an ADMINISTRATIVE one in the live prototype (دراسة · إعلان وإحالة · سحب عمل · تسوية حسابات); `project-status` is 5 vs 3; `expenditure-category` 5 vs 3. | **الشكل 5's own worked example uses the PROTOTYPE's values** — «النوع تنفيذ أمانة»، «مرحلة التنفيذ إعلان وإحالة» — neither of which exists in our catalogue. But these codes are STORED on every project and read by eight screens, the fixture and the domain tests, so changing them is a data migration, not a screen edit. `LookupCatalog.cs` already flags the divergence at `06 §2` and cites P-12. **Nothing on الشكل 5 depends on the answer**; the register, the portfolio and the schedule do. |
| Q-F5-2 | **What derives أولوية المشروع, رقم اعتماد الموازنة and المنطقة الجغرافية?** All three are tagged «مقترح» by the document and none has a derivation rule anywhere. | See P-72. `Suggest()` fills only the code and the expenditure category, and the expenditure map is itself inferred. `Workspaces` has no governorate column to derive a region from. |
| Q-F5-3 | **Does the activity log record WHICH fields changed?** The live prototype renders per-field «من → إلى» chips and mixes in project-wide events (progress updates). | `ProjectActivityEvent` records the four attribution facts `§7` asks for and no diff. The documents give a count («6 أحداث») and no schema, so a diff would be an invented one — and it needs new columns. The timeline renders the prototype's shape using its own text branch until then. | **UPDATE — answered for the CONTRACT log by الشكل 11 (P-82): the diff is recorded and rendered. `ProjectActivityEvent` still is not, and the same treatment now applies to it whenever الشكل 5 is revisited.**
| Q-F5-4 | **Do `plannedCost` and `designerParty` belong on a screen?** Neither appears in الشكل 5, and no other screen shows them. | They are written by the create form and then invisible. Dropping the columns would lose data المسار 1 requires (`plannedCost` is its «الكلفة أكبر من صفر» clause); showing them on الشكل 5 would add fields the document does not list. |
| Q-F5-5 | **Should clearing a «مقترح» field clear it?** `Apply()` treats an empty incoming `code` / `region` / `expenditureCategory` as "leave the suggestion alone", while `priority` and `budgetApprovalNumber` — which now carry the same badge — do clear. | Pre-dates الشكل 5 and is defensible per field; it is only inconsistent now that all five wear the same tag. |
