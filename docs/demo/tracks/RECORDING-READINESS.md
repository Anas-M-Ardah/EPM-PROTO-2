# Recording readiness — 2026-09-15

The tested API build is active on localhost:5080 (process 36232). Frontend development build passed; all 599 backend tests passed. The neutral person account icon was visually verified in EPM.

## Browser checks completed in this preparation

- Selective document preparation executed on PRJ-0443 and added exactly two sample document codes.
- As university specialist, attached the supplied ST-DR-001-R2.pdf with TR-DEMO-01 and submitted R2. R1 remained in revision history and became superseded.
- As resident engineer, approved R2: approved count 1, draft count 1, with no remaining decision controls on that decided revision.
- ME-DR-002 rejection confirmation was disabled without a reason. Entered the runsheet reason and saved rejection; the reason and rejected status appeared visibly, draft count became 0.
- Repeated ST-DR-001 upload with the same file/transmittal was refused with the duplicate message; revision count stayed 3.
- After reset, created ub/UOB university and sp/SPD supply directorate through the browser. Both rows appeared with correct types and zero projects. These rehearsal workspaces were subsequently cleared for the recording opening.

## Preserved data and starting state

Before reset, backed up EpmPrototype with COPY_ONLY and CHECKSUM to:

C:\Program Files\Microsoft SQL Server\MSSQL16.MSSQLSERVER\MSSQL\Backup\EpmPrototype_before_full_flow_20260915.bak

RESTORE VERIFYONLY confirmed that backup is valid. It includes the prior demonstrations and this document rehearsal. Reset targets only the local EpmPrototype database. The original fixture loader is not called during the continuous demonstration.

Opening screen: /entities, Arabic UI, role مدير عام, no workspaces/projects. User creates both workspaces on camera, then follows FULL-FLOW-RECORDING.md and each track RUNSHEET. Selective document preparation is disclosed and performed only after the newly generated construction project exists, outside the capture.

## Limits of readiness evidence

This preparation verifies the new clean-start workspace setup and the previously deferred document actions. Earlier demonstrations and browser checks supply the evidence for the remaining tracks; the entire 01–14 chain was not rerun from blank data in this preparation. Accepted prototype exclusions remain. Do not promise that no error can occur; verify each chapter's actual end state during recording.

No recording was started or stopped by the assistant. Use one continuous user-controlled Xbox Game Bar capture and pause browser actions at chapter boundaries.
